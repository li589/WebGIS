# 代码事实同步文档（2026-07-16）

相对上一版（2026-07-15）的工程级收口事实，覆盖全平台。

## 本轮主题

执行「工程级全面审查与收口路线图」Phase 0–3 主干：安全/配置真源、legacy 脱钩、前端天气竞态、算法 materialize、观测与文档 SSOT。

## 后端

- 新增 `app/services/effective_config.py`：env+DB 投影、加密策略、执行器诚实化
- `deps.require_write_access` 读 `backend_auth` effective secret
- `/config/*` mutating、`/import/raster` 强制鉴权；import 限额 100MiB / 2GiB
- GEE 账户管理 flag 路由层强制 403
- Runtime PATCH 仅允许已接线字段；weather TTL / task_executor 走投影
- Legacy workflow / demo 路由默认关闭
- Redis metrics/status：`KEYS` → `SCAN`；API slot 本地有限降级
- 底图代理：LRU(512) + `tile_proxy_cache_ttl_seconds`
- `lab-output` catalog 文案改为样板叙事

## 算法包

- `data_access/sources/http.py`：真实下载 materialize，`ready` 仅在落盘后
- `data_access/sources/minio.py`：真实 `fget_object`，缺凭证/SDK 则抛错（不再 deferred 冒充）

## 前端

- 天气引擎静态白名单 + catalog 到达 reconcile
- 非天气层不再进入 weatherTileManager
- WorkflowStatusPanel 作业/瓦片分栏
- Overlay dispose、merge cache、watch hash 去 jobUpdatedAt 污染
- 移除 lab-output 无 catalog 豁免

## 文档 / CI

- `Doc/工程决策纪要-配置瓦片与契约.md`
- `Doc/工程收口仪表盘.md`
- `Code/docs/真实数据e2e门槛.md`
- `Code/backend/scripts/check_openapi_drift.py` + `npm run check:openapi`
- 双通道文档瓦片叙事修正

## 验证

- `pytest tests/test_config_security.py`
- `pytest tests/test_demo_routes.py` / workflow legacy 相关
- `python scripts/check_openapi_drift.py`
- 前端 `vue-tsc --noEmit`（Phase2 子任务已跑通）
