# 权威包管理器 = npm（见 package.json 的 packageManager 字段）。
# 依赖树存在既有 peer 冲突（typescript 6.x 与 openapi-typescript 7.x 的 peer ^5.x），
# 现有 package-lock.json 即以宽松 peer 解析生成；此处显式声明，保证本地与 CI 的
# `npm ci` 结果一致、无需人工加 --legacy-peer-deps 参数。
legacy-peer-deps=true
