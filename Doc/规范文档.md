# 前后端接口与字段规范文档

## 1. 文档目标

本规范用于统一当前 Web GIS 平台中前端展示层、后端接口层、后续算法层之间的字段命名、对象结构与接口语义。

补充说明：

- 本文档偏向协议与接口语义，不单独承担项目进度追踪
- 工程结构与运行时能力以 `README.md`、`Code/*/README.md`、`Doc/技术栈.md` 为准
- 带明确日期的任务清单 / 事实快照仅作历史参考

当前规范覆盖的核心对象：

- 图层目录对象
- 工作流提交 / 状态对象（主线）
- 旧任务对象（`/tasks` 兼容桥接，软下线）

主线接口：

- `GET /layers`
- `POST /workflow-runs` 及状态 / 事件 / view / cancel / retry
- `GET /unified-tiles/{layer_id}/{z}/{x}/{y}`
- `GET /artifacts/{artifact_id}`（及 preview）

兼容接口（勿作为新功能默认入口）：

- `POST /tasks`
- `GET /tasks/{task_id}`

## 2. 设计原则

### 2.1 命名原则

- HTTP JSON 字段统一使用 `snake_case`
- 标识字段统一使用 `*_id` 或 `*_key`
- 时间字段统一使用 ISO 8601 时间字符串
- 空间范围统一使用 `bbox + crs`
- 枚举值统一使用稳定的小写英文值

### 2.2 架构原则

- 前端不直接猜测算法字段，而是读取后端返回的图层与任务协议
- 后端不直接暴露算法内部结构，而是通过统一工作流 / 结果协议封装
- `shared/contracts`（及前端由 OpenAPI 生成的 `api-contracts.ts`）作为共享协议事实来源
- 工作流状态由 SQLite 持久化，异步执行走 `Celery + Redis`；旧 `/tasks` 仅桥接到 `workflow-runs`
- Celery 元数据仅支持 US-ASCII 字符，所有 `WorkflowResultReference.title` 和 `create_artifact_result_ref(title=...)` 值必须为纯英文，使用中文会导致 `ValueError: unsupported metadata value`

## 3. 核心对象模型

### 3.1 图层目录对象 `LayerDescriptor`

用于描述前端图层侧边栏、地图图层控制、模式切换与时间轴可用性。

字段约定：

- `layer_id`: 图层唯一标识，后端稳定主键
- `dataset_key`: 前后端共享的数据集键，用于前端状态与后端任务提交关联
- `display_name`: 面向用户展示的中文图层名称
- `description`: 图层说明
- `category`: 图层类别，如 `meteorology`、`remote_sensing`、`model_output`
- `source_type`: 数据来源类型，如 `demo`、`gee`、`cog`、`algorithm_output`
- `render_type`: 渲染类型，如 `raster`、`vector`、`point`、`heatmap`
- `supported_map_modes`: 支持的地图模式，取值为 `2d` 或 `3d`
- `supports_time`: 是否支持时间轴
- `is_realtime`: 是否属于实时图层
- `default_visible`: 是否默认显示
- `status`: 当前图层状态，第一版取 `available`
- `time_granularity`: 时间粒度，如 `hour`、`day`、`month`
- `default_time_offset`: 默认时间偏移量，便于前端初始化
- `extent`: 图层可用空间范围
- `style`: 前端样式提示，如色带、单位、透明度
- `tags`: 标签列表

### 3.2 空间过滤对象 `SpatialFilter`

用于任务提交时表达研究区域。

字段约定：

- `filter_type`: 第一版默认 `bbox`
- `bbox`: 边界框对象
- `region_code`: 可选区域编码
- `region_name`: 可选区域名称

### 3.3 时间范围对象 `TimeRange`

用于统一任务提交与状态查询中的时间维度。

字段约定：

- `start_at`: 开始时间
- `end_at`: 结束时间
- `granularity`: 时间粒度

### 3.4 任务提交对象 `TaskSubmitRequest`

用于前端在选定图层、时间范围、空间范围后向后端发起计算请求。

字段约定：

- `layer_id`: 目标图层 ID
- `task_type`: 任务类型，第一版支持 `layer_preview`、`analysis`、`export`
- `map_mode`: 当前地图模式，支持 `2d`、`3d`
- `spatial_filter`: 空间过滤条件
- `time_range`: 时间范围
- `parameters`: 自定义算法参数
- `requested_outputs`: 请求输出类型，如 `json`、`tile_url`、`geojson`
- `client_context`: 前端上下文信息，如页面来源、用户交互状态

### 3.5 任务状态对象 `TaskStatusResponse`

用于轮询任务进度并返回结果引用。

字段约定：

- `task_id`: 任务 ID
- `layer_id`: 关联图层 ID
- `task_type`: 任务类型
- `status`: 任务状态，第一版支持 `queued`、`running`、`succeeded`、`failed`
- `progress`: 进度百分比，0 到 100
- `message`: 当前状态说明
- `created_at`: 任务创建时间
- `updated_at`: 最近更新时间
- `spatial_filter`: 提交时使用的空间范围
- `time_range`: 提交时使用的时间范围
- `requested_outputs`: 请求输出列表
- `result_refs`: 结果引用列表
- `diagnostics`: 诊断信息列表

## 4. 字段标准化建议

### 4.1 前端现有字段与标准字段映射

当前前端原型里主要存在以下本地字段：

- `activeDataset`
- `currentHour`
- `mapMode`

建议逐步映射为：

- `activeDataset` -> `layer_id` 或 `dataset_key`
- `currentHour` -> `time_range.start_at / end_at` 的简化 UI 映射
- `mapMode` -> `map_mode`

当前实现阶段补充说明：

- 当前前端主路径已收口为 `2D-first demo`，真实三维地球模式暂未启用
- `map_mode` 在接口协议中继续保留，用于后续对接真实 `2d / 3d` 能力
- 当前时间轴仍以 `currentHour` 作为演示态映射字段，后续接接口时应优先映射到 `time_range`
- 当前图层联动优先验证字段别名映射、可用性状态与时间联动，不代表最终接口字段已经固定

### 4.2 图层展示字段规范

前端图层侧边栏至少应消费这些字段：

- `layer_id`
- `display_name`
- `category`
- `default_visible`
- `supports_time`
- `supported_map_modes`
- `status`

### 4.3 任务表单字段规范

前端任务提交面板至少应生成这些字段：

- `layer_id`
- `map_mode`
- `spatial_filter`
- `time_range`
- `task_type`
- `parameters`

## 5. 当前接口规范

### 5.1 `GET /layers`

用途：

- 获取图层目录
- 驱动前端图层列表、图层说明、模式过滤与时间轴开关

响应结构：

```json
{
  "items": [
    {
      "layer_id": "wind_field_surface",
      "dataset_key": "wind_field",
      "display_name": "风场",
      "description": "演示用近地表风场图层，支持时间轴切换和二维展示。",
      "category": "meteorology",
      "source_type": "demo",
      "render_type": "vector",
      "supported_map_modes": ["2d", "3d"],
      "supports_time": true,
      "is_realtime": true,
      "default_visible": true,
      "status": "available",
      "time_granularity": "hour",
      "default_time_offset": 0,
      "extent": {
        "west": 109.6,
        "south": 20.1,
        "east": 117.4,
        "north": 25.6,
        "crs": "EPSG:4326"
      },
      "style": {
        "palette": "blue-cyan",
        "unit_label": "m/s",
        "opacity": 0.85
      },
      "tags": ["wind", "realtime", "demo"]
    }
  ]
}
```

### 5.2 `POST /tasks`（legacy，已桥接到 workflow-runs）

用途：

- 兼容旧客户端的任务提交入口
- 内部自动桥接到 `workflow-runs` 主链；新功能请直接使用 `POST /workflow-runs`

请求示例：

```json
{
  "layer_id": "topic_group_model_output",
  "task_type": "analysis",
  "map_mode": "2d",
  "spatial_filter": {
    "filter_type": "bbox",
    "bbox": {
      "west": 112.9,
      "south": 22.2,
      "east": 114.7,
      "north": 23.9,
      "crs": "EPSG:4326"
    },
    "region_code": "GD-GZ",
    "region_name": "广州"
  },
  "time_range": {
    "start_at": "2026-06-01T00:00:00Z",
    "end_at": "2026-06-02T00:00:00Z",
    "granularity": "day"
  },
  "parameters": {
    "scenario": "demo",
    "threshold": 0.75
  },
  "requested_outputs": ["json"],
  "client_context": {
    "page": "dashboard"
  }
}
```

响应示例：

```json
{
  "task_id": "task-123456789abc",
  "status": "queued",
  "status_url": "/tasks/task-123456789abc",
  "created_at": "2026-06-30T12:00:00Z",
  "message": "任务已提交，可通过状态接口轮询进度。"
}
```

### 5.3 `GET /tasks/{task_id}`

用途：

- 查询任务执行状态
- 为前端轮询与结果区面板提供统一数据源

响应对象包含：

- 任务基本信息
- 进度与消息
- 提交时的时间/空间条件
- 结果引用
- 诊断信息

## 6. 前后端链接建议

### 6.1 前端图层侧边栏

前端的 `LayerSidebar` 不应继续只使用字符串数组，而应改为消费 `GET /layers` 返回的 `items`。

推荐最小渲染字段：

- `display_name`
- `status`
- `category`
- `supports_time`

### 6.2 前端地图区域

`MapCanvas` 后续应围绕 `layer_id` 与 `render_type` 决定不同渲染方式：

- `raster` -> 栅格瓦片
- `vector` -> 矢量图层
- `point` -> 点图层
- `heatmap` -> 热力图层

### 6.3 前端时间轴

当前 `currentHour` 是 UI 原型字段，后续应转换为：

- `time_range.start_at`
- `time_range.end_at`
- `time_range.granularity`

### 6.4 结果面板

`InfoPanel` 后续建议直接绑定：

- 当前选中图层
- 当前任务状态
- 当前结果引用
- 诊断信息

## 7. 当前阶段落地范围

当前已完成（与代码对照后的摘要）：

- `shared/contracts` 与前端 OpenAPI 类型生成
- `GET /layers`、overlay / geo transform 等目录与叠加接口
- `workflow-runs` 主链（提交 / 状态 / 事件 / view / cancel / retry）
- 旧 `/tasks` 到 `workflow-runs` 的软下线桥接
- SQLite 工作流状态仓储 + `Celery + Redis` 异步执行
- artifact 溢写、preview PNG、local/MinIO 对象存储抽象
- 天气点查、天气 DAG、统一瓦片 `/unified-tiles`
- 工作流域服务拆分（原 interaction_hub）与 API 路由按域拆分
- GEE 模块嵌入与配置路由

当前仍在推进 / 规划：

- PostGIS、Martin / TiTiler、全站 Nginx 与完整应用容器化
- Cesium 3D 主链、deck.gl 大规模叠加
- 课题组真实遥感数据规模化接入与产线硬化
- 天气瓦片 / 风场渲染体验的持续打磨

## 8. 底图瓦片源系统架构

### 8.1 设计原则

- **标准源直接用**：EPSG:3857 坐标系的标准瓦片源（ESRI / Google / Bing / OSM）前端直接加载
- **非标准源后端处理**：GCJ-02 / BD-09 坐标系源（天地图 / 百度 / 高德）标注 `needsBackendTransform: true`，由后端负责坐标转换后返回标准瓦片
- **风格分类驱动 UI**：瓦片源按 `BasemapStyle` 分组（satellite / street / dark / terrain），UI 按风格展示，用户切换风格后自动过滤该风格下的可用源

### 8.2 瓦片源配置（stores/ui.ts）

15 个瓦片源（含 `'none'` 无底图）统一在 `TILE_SOURCES` 数组中配置，每个配置包含：

| 字段 | 含义 |
|------|------|
| `id` | 瓦片源唯一标识 |
| `label` | 显示名称 |
| `style` | 风格分类 |
| `provider` | 提供商 |
| `coordSys` | 坐标系（EPSG:3857 / GCJ-02 / BD-09） |
| `urlTemplate` | 瓦片 URL 模板 |
| `tileSize` | 瓦片大小 |
| `attribution` | 版权信息 |
| `saturation / brightness / contrast` | 色彩调整参数 |
| `needsBackendTransform` | 是否需要后端坐标转换 |
| `isStandard` | 是否为前端可直接使用的标准源 |

### 8.3 状态管理

前端底图状态由 `useUiStore` 统一管理：

- `tileSourceId`：当前瓦片源 ID
- `showAdminOverlay`：是否显示行政区边界叠加层
- `adminOverlayOpacity`：叠加层不透明度
- `setTileSource(sourceId)`：切换瓦片源
- `setAdminOverlay(visible)`：切换叠加层

### 8.4 非标准瓦片源预留接口

当前标注了 `isStandard: false` 的源（天地图 / 百度 / 高德）将在后端实现坐标转换后以作业形式接入前端，接口保持不变：

- `TileSourceId` 枚举已包含全部 16 个源 ID（含 `'none'`）
- `TILE_SOURCE_MAP` 已包含全部配置
- 前端仅通过 `isStandard` 过滤决定是否在选择器中显示

### 8.5 已实现瓦片源清单

| ID | 风格 | 提供商 | 坐标系 | 状态 |
|----|------|--------|--------|------|
| none | — | — | EPSG:3857 | ✅ 无底图（纯叠加层） |
| esri-satellite | satellite | ESRI | EPSG:3857 | ✅ 标准源 |
| esri-street | street | ESRI | EPSG:3857 | ✅ 标准源 |
| esri-dark | dark | ESRI | EPSG:3857 | ✅ 标准源 |
| esri-navigation | street | ESRI | EPSG:3857 | ✅ 标准源 |
| google-satellite | satellite | Google | EPSG:3857 | ✅ 标准源 |
| google-terrain | terrain | Google | EPSG:3857 | ✅ 标准源 |
| bing-road | street | Bing | EPSG:3857 | ✅ 标准源 |
| bing-aerial | satellite | Bing | EPSG:3857 | ✅ 标准源 |
| bing-dark | dark | Bing | EPSG:3857 | ✅ 标准源 |
| osm-standard | street | OSM | EPSG:3857 | ✅ 标准源 |
| osm-cycle | street | OSM | EPSG:3857 | ✅ 标准源 |
| osm-transport | street | OSM | EPSG:3857 | ✅ 标准源 |
| tianditu-img | satellite | 天地图 | GCJ-02 | 🔒 需后端转换 |
| tianditu-vec | street | 天地图 | GCJ-02 | 🔒 需后端转换 |
| baidu-standard | street | 百度 | BD-09 | 🔒 需后端转换 |
| gaode-standard | street | 高德 | GCJ-02 | 🔒 需后端转换 |

## 9. 后续扩展建议

- 在 `shared/contracts` 中继续补 `results`、`errors`、`layers style schema`
- 在 `backend/services` 中继续完善真实异步调度与失败重试链
- 在前端新增 `services/api.ts`，统一请求 `layers/tasks`
- 在前端 store 中将 `activeDataset` 升级为 `activeLayerId`

## 10. 前后端现状梳理

### 10.1 前端现状

- 当前前端主路径已收口为 `2D-first demo`
- 地图、时间轴、图层侧栏、分析侧栏已形成稳定演示闭环
- 图层与时间联动目前主要由本地 `demo-adapter` 驱动
- 前端已经补了可对接后端的 `demo snapshot` 适配入口与统一 `runtime-api` 封装
- 当前前端尚未将主界面默认切换为真实后端数据拉取，目的是保证演示态稳定
- 底图系统已全面重构：16 个瓦片源（含无底图），风格分类 UI 替换旧三段开关

### 10.2 后端现状

- 当前后端已提供：
  - `GET /layers`
  - `GET /demo/layers/snapshots`
  - `GET /demo/layers/{layer_id}/snapshot`
  - `POST /tasks`
  - `GET /tasks/{task_id}`
- 当前后端已新增交互总线骨架：
  - `POST /workflow-runs`
  - `GET /workflow-runs/{run_id}`
  - `GET /workflow-runs/{run_id}/events`
  - `PATCH /runtime/config`
  - `GET /runtime/status`
  - `POST /frontend/commands`
- 当前后端已进一步升级为：
  - SQLite 持久化状态存储
  - 旧 `/tasks` 桥接到 `workflow-runs`
  - 结构化日志
  - artifact / chunk / object store abstraction
  - 下载链与 follow-up task 占位执行链
  - 底图瓦片源系统全面重构（16 个源，含 `'none'` 无底图，风格分类 UI）
- 当前仍未完成的部分主要在于：
  - Redis + Celery 常驻运行链
  - 下载真实抓取逻辑
  - 基础设施服务联调

## 11. 交互通道分层

### 11.1 核心工作流通道

用途：

- 前端向后端提交几乎所有核心处理指令
- 后端接收后形成统一 `run_id`
- 后续状态、日志、结果均围绕该 `run_id` 组织

接口：

- `POST /workflow-runs`
- `GET /workflow-runs/{run_id}`
- `GET /workflow-runs/{run_id}/events`

建议作为默认主通道，覆盖：

- 数据提交处理工作流
- 结果轮询
- 状态轮询
- 事件日志查询

### 11.2 配置更新通道

用途：

- 向后端提交基础设置、默认参数、运行时开关、provider 配置

接口：

- `PATCH /runtime/config`

特点：

- 频次不高
- 应与工作流参数分离
- 只提交“运行时配置”，不承担具体数据处理任务

### 11.2 日志与事件通道

用途：

- 向前端按需返回日志、状态变更、系统通知、数据准备完成提示

接口：

- `GET /workflow-runs/{run_id}/events`

事件通道：

- `status`
- `log`
- `data`
- `chart`
- `notification`
- `system`

说明：

- 当前第一版采用轮询
- 后续可平滑升级为 `SSE / WebSocket`

### 11.3 数据结果通道

用途：

- 向前端返回表格、JSON、地图图层引用、文件引用等数据结果

当前承载对象：

- `WorkflowResultReference`

当前返回位置：

- `GET /workflow-runs/{run_id}`

### 11.4 统计制图通道

用途：

- 向前端返回统计图、时间序列图、分布图、制图配置引用

当前承载方式：

- 作为 `WorkflowResultReference.result_kind = chart`
- 或作为 `events` 中的 `chart` 通道事件

### 11.5 后端状态反馈通道

用途：

- 提供系统总体状态、服务健康度、活跃任务数、当前配置快照

接口：

- `GET /runtime/status`

建议作为前端常用轮询接口之一

### 11.6 前端简单控制指令通道

用途：

- 发送懒加载、缓存清理、资源释放、目录重载、取消任务等轻量指令

接口：

- `POST /frontend/commands`

说明：

- 不负责承载重型数据处理
- 更适合界面控制与资源治理类请求

## 12. 建议补充与优化

你提出的 7 类通道已经基本完整，建议再明确补 3 层：

- `会话关联层`
  - 通过 `client_id / session_id / correlation_id / view_id` 把一次前端操作、一个工作流、若干日志和若干结果串起来
- `统一结果引用层`
  - 不要让图表、数据表、地图图层、文件下载分别长成不同返回格式
  - 建议统一收口到 `WorkflowResultReference`
- `统一事件流层`
  - 日志、状态、通知、图表准备完成、数据就绪都走 `WorkflowEvent`
  - 避免未来出现“每种功能一个单独回调接口”的碎片化问题

## 13. 推荐的使用方式

### 13.1 常用主路径

前端核心操作建议默认走：

1. `POST /workflow-runs`
2. `GET /workflow-runs/{run_id}`
3. `GET /workflow-runs/{run_id}/events`

### 13.2 低频配置路径

前端设置类操作建议走：

1. `PATCH /runtime/config`
2. `GET /runtime/status`

### 13.3 轻量控制路径

懒加载、清理、取消、刷新目录等建议走：

1. `POST /frontend/commands`

## 14. 当前阶段定位

当前这套接口还不是最终业务接口，而是：

- 一套已经接入正式后端主链的交互总线
- 一套能承接真实算法执行器与下载链的统一入口
- 一套为后续 `services -> tasks -> algorithms/providers` 持续扩展准备的稳定协议

下一阶段建议：

- 继续增强下载链、异步链和 artifact/cache 闭环
- 将 `runtime/config` 接到更正式的配置策略
- 将 `events` 从轮询进一步升级为可流式传输的事件机制
- 将 `result_refs` 接到真实算法输出、制图输出和文件输出

## 15. 天气瓦片工作流

### 15.1 渲染模式

天气图层采用标准 Web Mercator z/x/y 瓦片渲染模式，前端按视口动态请求瓦片，后端逐瓦片生成 GeoJSON。

### 15.2 接口

**生产路径（前端使用）**：

通过 `POST /workflow-runs` 提交 `weather_tile_render` 节点任务：

```json
{
  "command_type": "analysis",
  "layer_id": "wind-field",
  "requested_outputs": ["json"],
  "parameters": { "hour": 0 },
  "weather_request": {
    "workflow_id": "weather-tile-wind-field-z4-x12-y6-h0",
    "workflow": {
      "workflow_id": "weather-tile-wind-field-z4-x12-y6-h0",
      "nodes": [
        {
          "node_id": "tile-render",
          "node_type": "weather_tile_render",
          "params": {
            "layer_id": "wind-field",
            "z": 4, "x": 12, "y": 6,
            "hour": 0,
            "model": "best_match"
          }
        }
      ],
      "edges": []
    }
  }
}
```

**调试路径（REST 直连）**：

```
GET /unified-tiles/{layer_id}/{z}/{x}/{y}?hour={hour}&model={model}
```

### 15.3 前端瓦片调度

| 组件 | 职责 |
|------|------|
| `stores/weather-tile-manager.ts` | 全局并发槽位（3）、优先级队列、世代号过期 |
| `services/weather-tile-api.ts` | 坐标转换 + workflow 提交 + 轮询 |
| `services/weather-tile-utils.ts` | 多瓦片合并（去重、边界处理）|

### 15.4 数据约束

- `hour=0` 时使用 Open-Meteo `current` 段数据（含 `wind_direction_10m`），不用 `hourly[0]`（当地午夜）
- `hour>0` 时从 `hourly` 数据提取对应小时
- 修改 `WeatherLayerSpec` 字段配置后，必须清除 Redis 缓存（`weather:*` 键）和文件缓存（`.data/cache/weatherengine/`）
- 前端 429 退避 5s、503 退避 8s，最多重试 3 次
- 断路器参数：失败阈值 10 次、恢复超时 30s、半开探测 2 次
