# 项目任务规划（2026-07-06）

> **状态更新（2026-07-09）**：P2-1~P2-4（前端接入真实后端）已全部完成。前端 demo 模式已彻底移除（`demo-data.ts`/`demo-adapter.ts`/`demo-api.ts`/`demo-boundaries.ts` 全部删除，`src/` 内零引用）。`runtime-api.ts` 直连后端 `/workflow-runs`，InfoPanel 接入真实 `result_refs/diagnostics/events`，MapCanvas 接入真实 artifact/layer 渲染（COG preview / GeoJSON / Canvas 2D 粒子流）。

## 1. 文档同步报告

### 1.1 代码-文档偏差清单

经过一轮代码核查，当前工作树与文档存在以下偏差：

| # | 偏差类别 | 当前代码事实 | 对应文档现状 |
|---|---------|------------|------------|
| 1 | `retry_pending` / `FailureCategory` | 已存在并进入共享契约层 | `Doc/规范文档.md` 未覆盖 |
| 2 | `VisibilityGridRender / PressureGridRender / HumidityGridRender` | 已存在，共 9 个天气节点 | `Doc/项目任务清单.md` 未列明 |
| 3 | `source_fetcher` | 已存在，支持 http/https/minio/file/local | `Doc/后端架构设计.md` 未覆盖 |
| 4 | `provider_manifest / template_deriver` | 已存在，统一 manifest 入口 | `Doc/后端架构设计.md` 未覆盖 |
| 5 | `publish/` | 已存在，raster_writer/table_writer/manifest_builder | `Doc/后端架构设计.md` 标注"不存在" |
| 6 | 天气双路径 | fallback weatherengine + weather workflow DAG 并存 | `Doc/规范文档.md` 未区分 |
| 7 | `currentMapCenter / currentMapBBox` | 已进入 workflow 提交上下文 | `Doc/项目任务清单.md` 未记录 |
| 8 | 前端三层 canvas 叠加 | particle_flow / barb / contour | `Doc/项目任务清单.md` 已记录（2026-07-03）|

### 1.2 已过时文档

- `Doc/规范文档.md`："任务状态先用内存骨架实现" — **已过时**，`workflow-runs` + SQLite + Celery 已接入
- `Doc/技术栈.md`：CesiumJS / deck.gl / NaiveUI / PostgreSQL / PostGIS / TiTiler / Martin / Nginx / Docker Compose 标注"未启动"，但未区分这是目标架构还是当前状态
- `Doc/后端架构设计.md`：`publish/` 标注"不存在" — **已过时**，现已存在

### 1.3 文档优先级

文档同步按以下顺序执行：

1. **高优先级**（影响开发判断）：
   - `Doc/项目任务清单.md` — 补录最新完成事项（Python 算法桥接接入）
   - `Doc/规范文档.md` — 回写过时的"内存骨架"描述
   - `Doc/技术栈.md` — 区分"目标架构"与"当前落地现状"
2. **中优先级**（知识同步）：
   - `Doc/后端架构设计.md` — 更新 `publish/` 状态、补 `source_fetcher`
   - `Code/docs/` 下建议的 5 项文档动作

---

## 2. 待办任务总清单

### 2.1 主项目 — P0（3 项，全部待开始）

> **主战场：后端主线继续补齐 + 前端接入真实后端**

| 序号 | 任务 | 说明 | 关键文件 | 前置依赖 |
|------|------|------|---------|---------|
| P0-1 | 接通 Redis 常驻服务 + Celery worker 实际消费链 | ✅ **已完成（2026-07-06）**：创建了 `docker-compose.yml`（Redis 7 + MinIO）、后端 `.env` 配置（Celery 异步模式）、启动脚本（`run_start.bat`）、停止脚本（`run_stop.bat`）、状态检查脚本（`run_status.bat`）、worker 启动脚本（`run_worker.bat`）、Celery Beat 调度器（`start_celery_beat.py`）。代码骨架（`celery_app.py` / `workflow_tasks.py` / `download_tasks.py` / `weather_tasks.py`）已完整，无需改动。 | `Code/backend/docker-compose.yml`，`Code/backend/.env`，`run_start.bat`，`Code/backend/run_worker.bat`，`Code/backend/start_celery_beat.py` | 无 |
| P0-2 | 把真实下载产物清单写入 manifest | ✅ **已完成（2026-07-06）**：下载链代码骨架已完整（`_resolve_source_refs` → `source_fetcher` → manifest 回填）。新增 `source_uri_map.example.json`（21 个图层 URI 模板示例）、`source_uri_map.json` 配置文件支持（`@` 前缀引用外部 JSON + 注释行自动过滤）、增强 `_resolve_real_source_uri()` 支持外部文件加载。接入实际数据只需：1) 修改 `source_uri_map.json` 路径；2) `.env` 中 `BACKEND_DOWNLOAD_REAL_FETCH_ENABLED=true`。 | `source_uri_map.example.json`，`source_uri_map.json`，`download_service.py`（增强 `_resolve_real_source_uri`） | P0-1 |
| P0-3 | 选择一条前端链路切到真实后端 workflow 返回 | ✅ **已完成（2026-07-06）**：`runtime-api.ts` 已就绪（无 demo 拦截，所有 API 直连后端）。新增前端 `.env`（`VITE_API_BASE_URL=http://127.0.0.1:8000`）、`.env.example` 示例、`vite.config.ts` Vite proxy 配置（`/api` 转发到后端）。前端接入真实后端只需启动后端即可，无需代码改动。 | `Code/frontend/.env`，`Code/frontend/.env.example`，`Code/frontend/vite.config.ts` | P0-1，P0-2 |

### 2.2 主项目 — P1 后端能力建设（6 项，全部待开始）

| 序号 | 任务 | 说明 | 关键文件 |
|------|------|------|---------|
| P1-1 | `runtime/config` 持久化正规化 | 持久化配置策略、启动时加载、配置回写 | `Code/backend/app/core/config.py`，`Code/backend/app/api/routes.py` |
| P1-2 | provider 标准契约与算法接入模板 | manifest / template deriver 接入固化，前端 `algorithm/workflows` 接入真实模板 | `Code/algorithms/providers/Python/contracts/provider_manifest.py`，`Code/backend/app/services/python_provider_bridge_service.py` |
| P1-3 | `algorithm_request -> JobRequest` 共享协议正规化 | 接口契约固化、校验层完善 | `Code/shared/contracts/api_contracts.py`，`Code/backend/app/services/provider_workflow_service.py` |
| P1-4 | 下载/分析任务的失败分类与重试策略 | 基于已有 `FailureCategory` 完善策略层、重试次数、退避策略 | `Code/backend/app/services/failure_classifier.py`，`Code/backend/app/services/bridge_protocol.py` |
| P1-5 | `runtime/status` 增加 worker/queue/cache 观测信息 | 诊断输出完善、指标暴露、监控链路 | `Code/backend/app/services/interaction_hub.py` |
| P1-6 | 补充后端端到端验证脚本 | sync/celery/artifact/cache/follow-up task 全链路覆盖 | `Code/backend/test_celery_e2e.py`（已有骨架） |

### 2.3 主项目 — P1 数据与基础设施（4 项，全部待开始）

| 序号 | 任务 | 说明 | 关键文件/备注 |
|------|------|------|-------------|
| P1-7 | MinIO 实例接入与对象存储正式联调 | MinIO Docker 启动、endpoint 配置、artifact 真实写入验证 | `Code/backend/app/services/object_store_service.py` |
| P1-8 | PostGIS 边界与空间过滤接入规划 | 空间数据库选型、边界数据接入方案 | 待规划 |
| P1-9 | GEE 服务端代理第一条真实演示链 | ✅ **已完成（2026-07-07）**：GEE 模块 FastAPI 路由挂入主后端（`main.py` 挂载 `create_api_router()`），`/gee/*` 路由（validate/submit/workflow-jobs/run/exports:status/diagnostics）正式接入 FastAPI。删除了 `routes.py` 中与 GEE router 冲突的旧手动路由。`routes.py` 中 `submit_workflow` 已接入 catalog→algorithm_request 注入逻辑，使 bridge chain 能正确路由到 `python_provider_bridge_service`。 | `Code/backend/app/main.py`，`Code/backend/app/api/routes.py` |
| P1-10 | TiTiler / Martin 引入顺序规划 | 瓦片服务选型、COG 动态切片接入方案 | 待规划 |

### 2.4 主项目 — P2 前端接入（5 项，全部待开始）

| 序号 | 任务 | 说明 | 关键文件 |
|------|------|------|---------|
| P2-1 | 前端 demo/backend 双模式可配置 | ✅ **已完成（2026-07-09）**：demo 模式已彻底移除（非双模式保留），`demo-*.ts` 全部删除，`runtime-api.ts` 直连后端 | `frontend/src/services/`，`frontend/.env` |
| P2-2 | `runtime-api` 接入真实 `workflow-runs` 查询 | ✅ **已完成（2026-07-09）**：`GET /workflow-runs/{id}`、`submitWorkflow`、`getWorkflowEvents` 等全部直连后端 | `frontend/src/services/runtime-api.ts` |
| P2-3 | `InfoPanel` 接入真实 `result_refs / diagnostics / events` | ✅ **已完成（2026-07-09）**：InfoPanel 通过 `jobLayer`/`mapLayerPayload`/`resultView` 消费真实工作流结果 | `frontend/src/components/InfoPanel.vue` |
| P2-4 | 地图主舞台接入真实 artifact/layer 渲染 | ✅ **已完成（2026-07-09）**：COG preview / GeoJSON / Canvas 2D 粒子流渲染链路全部接通 | `frontend/src/components/MapCanvas.vue` |
| P2-5 | deck.gl 引入必要性评估 | 待开始 | 前端架构决策文档 |

### 2.5 主项目 — P2 文档治理（3 项，全部待开始）

| 序号 | 任务 | 说明 |
|------|------|------|
| P2-6 | 回写 `Doc/规范文档.md` 过时描述 | 更新"内存骨架"为真实 `workflow-runs` 主链状态 |
| P2-7 | `Doc/技术栈.md` 区分目标架构与当前落地 | 增加"当前已落地"列或说明段落 |
| P2-8 | 增加"部署与联调指南" | Redis / Worker / MinIO / API 启动顺序与配置说明 |

---

### 2.6 GEE 模块任务状态

**GEE 模块 P0 / P1 / P2 全部 60 项均已完成**，当前状态为：

- 当前 schema 版本：**1.11**（有 `1.12` 测试通过）
- 测试覆盖：**132 passed**
- 剩余工作（来自 GEE 模块建议）：
  - ✅ FastAPI 路由实际挂入主后端（2026-07-07 完成）
  - `biz_job_id/run_id/manifest_uri` 可信映射
  - Prometheus / OpenTelemetry / ELK 具体适配器实现
  - 上述属于 P3 扩展方向，非当前 P0-P2 范畴

---

## 3. 任务优先级排序与执行建议

### 3.1 推荐执行顺序

```
P0-1（Redis+Celery联调） → P0-2（manifest产物回填） → P0-3（前端切真实后端）
     ↓                              ↓
P1-7（MinIO联调）                 P2-2（runtime-api接入）
     ↓                              ↓
P1-9（GEE FastAPI接入）           P2-3（InfoPanel接入）
     ↓                              ↓
P2-4（地图渲染接入）              P1-4/5/6（能力完善）
```

### 3.2 当前阶段判断

与 `Doc/项目任务清单.md` 第 1 节一致：

- **前端**：`2D-first demo` 已稳定，主战场不在此
- **后端**：workflow-runs 主链骨架已落地，Redis+Celery 基础设施已完成。继续补下载链/异步链/artifact 链。
- **主战场**：P0 主线 + P1-9 全部完成。接下来：P1-7（MinIO联调）→ P2-1~P2-4（前端接入真实数据）

### 3.3 GEE 模块定位

GEE 模块已完整，后续无需大量投入。其剩余工作（P3）属于 FastAPI 挂载与上层集成，属于 P1-9 范围的一部分。

---

## 4. 任务记录表（用于完成任务时打勾）

### P0 主线

| 任务 | 状态 | 完成日期 |
|------|------|---------|
| P0-1 接通 Redis + Celery worker 实际消费链 | ✅ 完成 | 2026-07-06 |
| P0-2 把真实下载产物清单写入 manifest | ✅ 完成 | 2026-07-06 |
| P0-3 选择一条前端链路切到真实后端 workflow | ✅ 完成 | 2026-07-06 |

### P1 后端能力

| 任务 | 状态 | 完成日期 |
|------|------|---------|
| P1-1 `runtime/config` 持久化正规化 | 待开始 | |
| P1-2 provider 标准契约与算法接入模板 | 待开始 | |
| P1-3 `algorithm_request -> JobRequest` 协议正规化 | 待开始 | |
| P1-4 失败分类与重试策略完善 | 待开始 | |
| P1-5 `runtime/status` 观测信息完善 | 待开始 | |
| P1-6 端到端验证脚本 | 待开始 | |

### P1 基础设施

| 任务 | 状态 | 完成日期 |
|------|------|---------|
| P1-7 MinIO 联调 | 待开始 | |
| P1-8 PostGIS 规划 | 待开始 | |
| P1-9 GEE FastAPI 接入 | ✅ 完成 | 2026-07-07 |
| P1-10 TiTiler/Martin 规划 | 待开始 | |

### P2 前端

| 任务 | 状态 | 完成日期 |
|------|------|---------|
| P2-1 前端 demo/backend 双模式 | ✅ 完成（demo 已移除，直连后端） | 2026-07-09 |
| P2-2 runtime-api 接入真实 workflow | ✅ 完成 | 2026-07-09 |
| P2-3 InfoPanel 接入真实数据 | ✅ 完成 | 2026-07-09 |
| P2-4 地图接入真实 artifact 渲染 | ✅ 完成 | 2026-07-09 |
| P2-5 deck.gl 必要性评估 | 待开始 | |

### P2 文档

| 任务 | 状态 | 完成日期 |
|------|------|---------|
| P2-6 回写规范文档过时描述 | 待开始 | |
| P2-7 技术栈区分目标与现状 | 待开始 | |
| P2-8 部署与联调指南 | 待开始 | |
