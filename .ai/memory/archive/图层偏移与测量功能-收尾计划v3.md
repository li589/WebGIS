# 图层偏移修复 + 路径测量功能 — 收尾执行计划 v3

> 创建日期：2026-07-18
> 状态：收尾阶段（前序 v1/v2 计划已批准并完成 90% 工作）
> 目标：完成最后两项前端补丁 → typecheck → 重启 → 目视验证 → Git 提交 + 推送

---

## 一、当前进度盘点

### 1.1 后端（问题 1 — 图层偏移修复）— ✅ 全部完成

**[export_overlay_assets.py](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Analysis%20system/Tools/export_overlay_assets.py)**：所有 5 类根因修复均已落盘（min-figure-size / `_bounds_from_centers()` NaN 过滤 / EASE-Grid 重投影 / SMAP numpy `or` bug / `window_bounds`）。

**后台重跑已成功完成**（job-0ab8046d1c9646e7bc8807af9b5165da，exit 0），全部 15 个图层 summary OK：

| 图层 | bounds (W,S,E,N) | 状态 |
|------|------------------|------|
| dem (ETOPO) | (-180, -90, 180, 90) | ✅ |
| thematic | (72.87, 14.87, 137.13, 59.13) | ✅ |
| omega (avg) | (-180, -84.16, 180, 85.04) | ✅ EASE-Grid 重投影 |
| **smap_ts** | **(73.008, 15.287, 136.867, 58.485)** | ✅ **已修复，不再为 NaN** |
| gpcp_ts | (-180, -90, 180, 90) | ✅ |
| gebco_dem | (73.0, 15.0, 137.0, 59.0) | ✅ |
| cmfd_precip | (71.3, 15.8, 136.7, 58.6) | ✅ |
| clcd | (73.5, 18.2, 135.1, 53.6) | ✅ |
| biomass | (73.0, 15.0, 137.0, 59.0) | ✅ |
| era5_dwaa/wdaa | (73, 15, 137, 59) | ✅ |
| co2 | (-180, -62, 180, 90) | ✅ |
| soil_ddca / omega_fy / forest_ratio | (-180, -84.16, 180, 85.04) | ✅ EASE-Grid 重投影 |

### 1.2 前端（问题 2 — 路径测量功能）— ⚠️ 还差 2 处补丁

| 文件 | 状态 | 说明 |
|------|------|------|
| [ui.ts](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Analysis%20system/Code/frontend/src/stores/ui.ts) | ✅ 已完成 | `InteractionMode='move'\|'select'\|'measure'`、`MeasureState`、5 个 actions 全部就位（L8-22, L109-133） |
| [measure-geo.ts](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Analysis%20system/Code/frontend/src/components/map/measure-geo.ts) | ✅ 已完成 | `haversineDistance`、`bearing`、`formatDistance`、`formatBearing`、`computeSegments` |
| [measure-canvas.ts](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Analysis%20system/Code/frontend/src/components/map/measure-canvas.ts) | ✅ 已完成 | Canvas 2D 标注层（rAF 节流、DPI 适配、60px 阈值缩放自适应标签、虚线预览、总距离高亮） |
| [measure-module.ts](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Analysis%20system/Code/frontend/src/components/map/measure-module.ts) | ✅ 已完成 | MapLibre 事件绑定 + 3 个 GeoJSON Source/Layer 管理 + Canvas 协调 |
| [map-interaction-module.ts](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Analysis%20system/Code/frontend/src/components/map/map-interaction-module.ts) | ✅ 已完成 | `applyInteractionMode()` 在 select/measure 下都禁用 dragPan |
| [map-canvas-module-bundle.ts](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Analysis%20system/Code/frontend/src/components/map/map-canvas-module-bundle.ts) | ✅ 已完成 | `measureModule` 工厂实例化、`onInteractionModeChange` 回调同步、返回值已含 measureModule |
| [ModeToolbar.vue](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Analysis%20system/Code/frontend/src/components/ModeToolbar.vue) | ✅ 已完成 | `setInteractionMode` 三态、测量按钮（直尺图标）、清除按钮（垃圾桶图标 v-if） |
| [MapCanvas.vue](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Analysis%20system/Code/frontend/src/components/MapCanvas.vue) | ⚠️ **B.5 第二处补丁缺失** | L270-275 已传 measure props ✅；L284 已 `state.resources.measureModule = ...` ✅；L289 已 `bindEvents()` ✅；**但 L312-313 之间缺 `moduleBundle.measureModule.applyMeasureMode()` 调用** ❌ |
| [map-canvas-state.ts](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Analysis%20system/Code/frontend/src/components/map/map-canvas-state.ts) | ❌ **B.6 未做** | `resources` 类型未含 `measureModule` 字段（L34-44）；`clearResources()` 未置空（L73-83）。**MapCanvas.vue L284 已写入 `state.resources.measureModule`，TypeScript 将报错** |

### 1.3 Git 当前状态

- 分支：`dev`
- 已修改 6 个文件 + 新增 3 个 measure 文件
- 工作区还有大量未跟踪文件（`.data/`、`.trae/documents/*.md`、`Tools/ComplexityCompus/`）—— **D.1 提交时必须使用精准 `git add` 列表，禁止 `git add -A`**（项目记忆明确约束）

### 1.4 待提交文件清单（精确）

```
M  Code/frontend/src/components/MapCanvas.vue
M  Code/frontend/src/components/ModeToolbar.vue
M  Code/frontend/src/components/map/map-canvas-module-bundle.ts
M  Code/frontend/src/components/map/map-interaction-module.ts
M  Code/frontend/src/stores/ui.ts
M  Tools/export_overlay_assets.py
A  Code/frontend/src/components/map/measure-canvas.ts
A  Code/frontend/src/components/map/measure-geo.ts
A  Code/frontend/src/components/map/measure-module.ts
```

---

## 二、剩余工作详细方案

### 2.1 阶段 B 收尾：补齐前端两处补丁

#### B.5（补丁 2）— MapCanvas.vue onMapLoad 添加 applyMeasureMode 调用

**文件**：[MapCanvas.vue](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Analysis%20system/Code/frontend/src/components/MapCanvas.vue#L312-L313)

**当前代码**（L312-313）：

```typescript
      moduleBundle.mapInteractionModule.applyInteractionMode()
      presentationModule.revealMap()
```

**目标代码**：

```typescript
      moduleBundle.mapInteractionModule.applyInteractionMode()
      // 测量模式初始状态同步（与 applyInteractionMode 一致；mapInteractionModule 已处理 dragPan，measureModule 处理 doubleClickZoom/boxZoom + Canvas show）
      moduleBundle.measureModule.applyMeasureMode()
      presentationModule.revealMap()
```

**原因**：`onMapLoad` 时 map 已 loaded，需立即应用当前模式（默认 'move'，会执行 else 分支：`canvas.hide()` + 启用 doubleClickZoom/boxZoom，确保默认状态正确）。若不调用，则切换到 measure 模式前 Canvas 标注层可能未初始化、doubleClickZoom 仍可能干扰。

#### B.6 — map-canvas-state.ts 添加 measureModule 资源声明

**文件**：[map-canvas-state.ts](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Analysis%20system/Code/frontend/src/components/map/map-canvas-state.ts)

**改动 1**（L9 后新增导入）：

```typescript
import { createMeasureModule } from './measure-module'
```

**改动 2**（L43 后，在 `selectedLayerFocusModule` 字段之后、`}` 之前）：

```typescript
    selectedLayerFocusModule: ReturnType<typeof createSelectedLayerFocusModule> | null
    measureModule: ReturnType<typeof createMeasureModule> | null  // 新增
```

**改动 3**（L71 后，初始化对象中新增）：

```typescript
      selectedLayerFocusModule: null,
      measureModule: null,  // 新增
```

**改动 4**（L82 后，`clearResources()` 函数末尾新增）：

```typescript
      state.resources.selectedLayerFocusModule = null
      state.resources.measureModule = null  // 新增
```

**原因**：MapCanvas.vue L284 已通过 `state.resources.measureModule = moduleBundle.measureModule` 写入，类型必须存在否则 TS 编译错误；`clearResources` 必须同步置空以避免组件卸载后悬挂引用。

---

### 2.2 阶段 C：验证

#### C.1 前端 typecheck

```powershell
cd Code/frontend
npm run typecheck
```

**预期**：通过，无 TS 错误。重点关注：
- `state.resources.measureModule` 类型是否解析成功（依赖 B.6）
- `moduleBundle.measureModule.applyMeasureMode()` 是否解析成功（依赖 B.5 + measure-module.ts 的 MeasureModule 接口）

**失败处理**：若出现类型错误，根据错误信息修复，常见可能：
- `Property 'measureModule' does not exist on type 'MapCanvasModuleBundle'` → 检查 map-canvas-module-bundle.ts L45 是否已含字段（已确认含）
- `Property 'applyMeasureMode' does not exist on type 'MeasureModule'` → 检查 measure-module.ts MeasureModule 接口（已确认含）

#### C.2 重启后端 + 前端项目

**重启策略**：先确认当前是否有运行中的 dev server / backend 进程，若有则停止后重启。

**后端**：根据项目记忆，启动脚本为 `run_start.bat`（且 `-S redis` 已移除）。先查找工作目录下的实际启动脚本，再决定调用方式。

**前端**：

```powershell
cd Code/frontend
npm run dev
```

**执行方式**：后端用 `run_in_background: true` 后台运行；前端 dev server 也用 `run_in_background: true`，记录输出位置便于排错。等待 5-10 秒后读取输出确认就绪。

#### C.3 目视验证图层偏移修复

**验证清单**（在浏览器中操作）：

1. 打开前端页面（默认 `http://localhost:5173` 或类似，从 dev server 输出确认）
2. 切换到「选择」或「移动」模式
3. 依次启用以下图层，与底图（ESRI Street 等）对比：
   - SMAP 土壤湿度时间序列（之前 NaN 完全不渲染，现在应正确叠加）
   - CLCD 土地覆盖（之前左上偏移几十公里）
   - ESACCI Biomass（之前左上偏移）
   - ERA5 DWAA/WDAA（之前偏移）
   - Soil DDCA / Omega FY / Forest Ratio（EASE-Grid 重投影后应全球正确叠加）
4. 重点检查：图层边界与省界/国界是否对齐（如中国东南沿海、印度边境、蒙古边境）

**预期**：所有图层与底图严格对齐，无可见偏移。

#### C.4 目视验证测量功能

**验证清单**：

1. 点击工具栏「测量」按钮（直尺图标），观察：
   - 鼠标光标变化（如有）
   - 工具栏「测量」按钮高亮
   - 拖动地图不再生效（dragPan 已禁用）
   - 双击缩放不再生效（doubleClickZoom 已禁用）
2. 在地图上单击第 1 个点 → 应出现蓝色圆点 + 虚线预览到鼠标位置
3. 移动鼠标 → 虚线动态跟随 + 标注显示该段距离（m/km 自动切换）+ 角度（°）
4. 单击第 2 个点 → 实线连接 2 点 + 标注显示该段距离 + 角度；新虚线从第 2 点延伸到鼠标
5. 单击第 3、4 个点 → 路径延长，每段独立标注；总距离在最后一段附近显示
6. 缩放地图到较小尺度（路径点密集）→ 短段标注自动隐藏（60px 阈值）；放大后重新显示
7. 右键单击 → 撤销最后一个点
8. 双击 → 完成路径（虚线消失，路径固化）
9. 点击「清除」按钮（垃圾桶图标，仅在测量模式 + 有路径点时显示）→ 清空所有点
10. 切换回「移动」模式 → dragPan 恢复，但已绘制的路径仍保留显示
11. 再次切换到「测量」模式 → 路径仍可见，可继续追加新点

**预期**：全部交互符合 Google Earth 测量工具的使用直觉。

---

### 2.3 阶段 D：Git 提交 + 推送

#### D.1 Git 提交（拆分为 2 个 commit，保持历史清晰）

**Commit 1（后端图层偏移修复）**：

```powershell
git add Tools/export_overlay_assets.py
git commit -m "fix(overlay): correct layer bounds computation for China datasets

- Replace hard-coded _CHINA_BBOX with _bounds_from_centers() deriving bounds
  from actual lat/lon arrays (NaN-filtered, sort-before-diff)
- Fix matplotlib _render_png() min-figure-size clamping (max(..., 1.0) instead
  of 2.0) that forced 200px height for 176-row data
- Add EASE-Grid 2.0 9km (EPSG:6933) reprojection to WGS84 for SMAP/soil/
  omega/forest layers
- Fix SMAP numpy 'or' bug on lat/lon arrays (use explicit 'if None' checks)
- Use rasterio src.window_bounds(win) instead of xy(offset='ll'/'ur') to
  avoid 1-pixel bounds offset for ERA5/CLCD layers
- Wrap each export function in main() with try/except to prevent one failure
  from aborting all layers

Verified: all 15 layers (including 27 SMAP time steps) produce valid bounds."
```

**Commit 2（前端测量功能）**：

```powershell
git add Code/frontend/src/components/MapCanvas.vue Code/frontend/src/components/ModeToolbar.vue Code/frontend/src/components/map/map-canvas-module-bundle.ts Code/frontend/src/components/map/map-interaction-module.ts Code/frontend/src/components/map/measure-canvas.ts Code/frontend/src/components/map/measure-geo.ts Code/frontend/src/components/map/measure-module.ts Code/frontend/src/stores/ui.ts
git commit -m "feat(map): add measurement mode with dynamic geodesic distance labels

Add Google Earth-like path measurement tool as a new 'measure' interaction
mode (preserves existing 'move' and 'select' modes):

- ui.ts: extend InteractionMode to 'move' | 'select' | 'measure'; add
  MeasureState (points, isDrawing, hoverPoint) and 5 actions
- measure-geo.ts: self-implemented haversine distance + bearing computation
  with auto m/km unit switching and angle formatting
- measure-canvas.ts: Canvas 2D overlay for distance/angle labels with rAF
  throttling, DPI awareness, and zoom-adaptive label hiding (60px threshold)
- measure-module.ts: MapLibre event binding (click/dblclick/contextmenu/
  mousemove) + 3 GeoJSON sources (points/line/preview) + Canvas coordination
- map-interaction-module.ts: extend applyInteractionMode to disable dragPan
  for both 'select' and 'measure' modes
- map-canvas-module-bundle.ts: wire measureModule into bundle and trigger
  applyMeasureMode on interaction mode change
- map-canvas-state.ts: declare measureModule resource type
- MapCanvas.vue: pass measure props and call applyMeasureMode on map load
- ModeToolbar.vue: add ruler button + clear button (v-if measure + points)

Interaction:
- Click: add point
- Double-click: complete path
- Right-click: undo last point
- Mouse move: dynamic dashed preview with distance/angle labels
- Short segments auto-hide labels on zoom out; reappear on zoom in"
```

**关键约束**：
- 严禁使用 `git add -A` 或 `git add .`（项目记忆明确：根级 `.data/` 不在 `.gitignore` 中）
- 严禁添加 `.trae/documents/*.md`（工作区文档）
- 严禁添加 `Tools/ComplexityCompus/`（与本任务无关）
- 严禁添加 `.data/`（运行时数据）

#### D.2 推送到 GitHub（SSH 协议）

根据项目记忆，HTTPS 不可达，已切换为 SSH：

```powershell
git remote -v  # 确认 origin 是 git@github.com:li589/WebGIS.git
git push origin dev
```

**预期**：推送成功。若 SSH key 未配置则需用户手动执行 `! ssh-add` 或检查 `~/.ssh/id_ed25519`。

---

## 三、执行顺序

| 步骤 | 任务 | 预计耗时 |
|------|------|----------|
| 1 | B.5: MapCanvas.vue 补丁 2（添加 applyMeasureMode） | 1 分钟 |
| 2 | B.6: map-canvas-state.ts 补丁（4 处改动） | 2 分钟 |
| 3 | C.1: `npm run typecheck` | 30 秒 |
| 4 | C.2: 重启后端 + 前端 | 1 分钟（含等待） |
| 5 | C.3 + C.4: 目视验证（由用户在浏览器中确认） | 5 分钟 |
| 6 | D.1: 2 个 Git commit | 1 分钟 |
| 7 | D.2: git push origin dev | 30 秒 |

---

## 四、假设与决策

1. **不重新创建计划文件**：v1/v2 已批准并执行 90%，本 v3 仅作为收尾指引
2. **commit 拆分为 2 个**：后端 fix 与前端 feat 分离，符合现有 conventional commit 风格
3. **不提交 .trae/documents/**：避免污染仓库历史；如需保留可后续单独提交到 docs 分支
4. **目视验证由用户主导**：C.3/C.4 启动服务后由用户在浏览器中实际操作验证，agent 提供检查清单
5. **typecheck 失败时按错误信息就地修复**：不再进入重新规划流程
6. **不重启 backend 的 Celery worker**：本次后端只改了 `export_overlay_assets.py`（一次性导出脚本），不影响运行中的 backend 服务；只需重启前端 dev server 即可看到效果。**但若用户要求完整重启，则后端服务也一并重启**

---

## 五、验证完成准则

- [ ] B.5: MapCanvas.vue L312-313 之间已添加 `moduleBundle.measureModule.applyMeasureMode()`
- [ ] B.6: map-canvas-state.ts resources 类型 + 初始化 + clearResources 均含 measureModule
- [ ] C.1: `npm run typecheck` 通过，0 errors
- [ ] C.2: 前端 dev server 启动成功，浏览器可访问
- [ ] C.3: 用户在浏览器中确认所有图层与底图对齐（特别 SMAP 已正确渲染）
- [ ] C.4: 用户在浏览器中确认测量功能 11 项交互全部符合预期
- [ ] D.1: 2 个 commit 已创建，提交信息符合 conventional commit 风格
- [ ] D.2: `git push origin dev` 成功（SSH 协议）
