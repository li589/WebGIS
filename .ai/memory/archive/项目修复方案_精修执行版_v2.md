# 项目修复方案（精修执行版 v2）

> 本方案在原《完整项目修复方案》基础上，通过实际代码核验（2026-07-18）进行以下精修：
> 1. **修正原方案错误**：Sprint 1.3 仅 `weather_providers_repository.py` 有静默明文降级 bug，`api_keys_repository.py` 与 `gee_credentials_repository.py` 已正确使用 `secrets_encryption_required()`，无需改造。
> 2. **采用更小侵入的修复**：Sprint 1.3 复用现有 `app.services.effective_config.secrets_encryption_required()`，不新建 `_secrets_crypto.py` 模块，保持代码风格统一。
> 3. **提供 Edit 工具可直接应用的确切代码片段**：每项修复给出 `old_string` / `new_string` 形式，避免歧义。
> 4. **修正 Sprint 1.5 ndvi.py 的 bug 描述**：实际崩溃条件是 `len(sg_days) < sg_window_length`，不是 `valid_count < sg_window_length`。
> 5. **修正 Sprint 1.1 inversion.py 的细节**：L268 局部变量名 `model_context` 与拟新增参数同名，需用 if/else 复用而非重复赋值。
>
> 执行顺序：严格按 Sprint 1 → Sprint 2 → Sprint 3 → Sprint 4。Sprint 1 的 7 项彼此独立，建议同一会话内顺序完成并分别 commit。

---

## 目录

- [Sprint 1：阻断性修复（立即实施）](#sprint-1阻断性修复立即实施)
  - [1.1 inversion.py grid→pixel 签名不匹配](#11-inversionpy-gridpixel-签名不匹配)
  - [1.2 layer_router.py status 未导入](#12-layer_routerpy-status-未导入)
  - [1.3 weather_providers_repository.py 加密静默降级](#13-weather_providers_repositorypy-加密静默降级)
  - [1.4 omega.py porosity 未验证](#14-omegapy-porosity-未验证)
  - [1.5 ndvi.py Savitzky-Golay 有效性检查](#15-ndvipy-savitzky-golay-有效性检查)
  - [1.6 physics.py zkb 负值开方](#16-physicspy-zkb-负值开方)
  - [1.7 weatherengine/service.py Any 未导入](#17-weatherengineservicepy-any-未导入)
- [Sprint 2：高优先级性能与架构](#sprint-2高优先级性能与架构)
- [Sprint 3-4 与项目级修复](#sprint-3-4-与项目级修复)
- [验证清单](#验证清单)

---

## Sprint 1：阻断性修复（立即实施）

### 1.1 inversion.py grid→pixel 签名不匹配

**文件**：[Code/algorithms/providers/Python/algorithms/inversion.py](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/algorithms/providers/Python/algorithms/inversion.py)

**核验**：
- L244-254 `retrieve_dynamic_h_pixel` 签名末尾为 `theta_deg: float,` 后直接 `) -> float:`，**不接受 `model_context`**
- L268 函数内部：`model_context = build_tb_model_context(freq_ghz, clay_fraction, theta_deg)`（局部变量，与拟新增参数同名）
- L283-294 `ddca_retrieve_pixel` 签名末尾为 `theta_deg: float,` 后直接 `) -> tuple[float, float]:`，**不接受 `model_context`**
- L303 函数内部：`model_context = build_tb_model_context(freq_ghz, clay_fraction, theta_deg)`
- L376-389 `retrieve_dynamic_h_grid` 调用 `retrieve_dynamic_h_pixel(..., model_context=context_cache[key])` → **触发 TypeError**
- L458-472 `ddca_retrieve_grid` 调用 `ddca_retrieve_pixel(..., model_context=context_cache[key])` → **触发 TypeError**

**修复 1**：`retrieve_dynamic_h_pixel` 签名 + 内部 build_tb_model_context 调用

`old_string`（L244-268 关键部分）：
```python
def retrieve_dynamic_h_pixel(
    tbv: float,
    tbh: float,
    ts: float,
    tau_ini: float,
    clay_fraction: float,
    albedo: float,
    porosity: float,
    freq_ghz: float,
    theta_deg: float,
) -> float:
    """单像素动态 h 粗糙度反演。

    量纲: 输入 tbv/tbh/ts 单位 K，freq_ghz 单位 GHz，theta_deg 单位度 (°)，
    其余无量纲。返回 h_value 无量纲（粗糙度参数）。
    """
    from scipy.optimize import least_squares

    if any(
        _is_nan(value)
        for value in [tbv, tbh, ts, tau_ini, clay_fraction, albedo, porosity, theta_deg]
    ):
        return float("nan")

    model_context = build_tb_model_context(freq_ghz, clay_fraction, theta_deg)
```

`new_string`：
```python
def retrieve_dynamic_h_pixel(
    tbv: float,
    tbh: float,
    ts: float,
    tau_ini: float,
    clay_fraction: float,
    albedo: float,
    porosity: float,
    freq_ghz: float,
    theta_deg: float,
    model_context: TbModelContext | None = None,
) -> float:
    """单像素动态 h 粗糙度反演。

    量纲: 输入 tbv/tbh/ts 单位 K，freq_ghz 单位 GHz，theta_deg 单位度 (°)，
    其余无量纲。返回 h_value 无量纲（粗糙度参数）。
    可选 model_context 由调用方预计算以避免重复开销（grid 版本使用）。
    """
    from scipy.optimize import least_squares

    if any(
        _is_nan(value)
        for value in [tbv, tbh, ts, tau_ini, clay_fraction, albedo, porosity, theta_deg]
    ):
        return float("nan")

    if model_context is None:
        model_context = build_tb_model_context(freq_ghz, clay_fraction, theta_deg)
```

**修复 2**：`ddca_retrieve_pixel` 签名 + 内部 build_tb_model_context 调用

`old_string`（L283-303 关键部分）：
```python
def ddca_retrieve_pixel(
    tbv: float,
    tbh: float,
    ts: float,
    tau_ini: float,
    h_value: float,
    clay_fraction: float,
    albedo: float,
    porosity: float,
    freq_ghz: float,
    theta_deg: float,
) -> tuple[float, float]:
    from scipy.optimize import least_squares

    if any(
        _is_nan(value)
        for value in [tbv, tbh, ts, tau_ini, h_value, clay_fraction, albedo, porosity, theta_deg]
    ):
        return float("nan"), float("nan")

    model_context = build_tb_model_context(freq_ghz, clay_fraction, theta_deg)
```

`new_string`：
```python
def ddca_retrieve_pixel(
    tbv: float,
    tbh: float,
    ts: float,
    tau_ini: float,
    h_value: float,
    clay_fraction: float,
    albedo: float,
    porosity: float,
    freq_ghz: float,
    theta_deg: float,
    model_context: TbModelContext | None = None,
) -> tuple[float, float]:
    from scipy.optimize import least_squares

    if any(
        _is_nan(value)
        for value in [tbv, tbh, ts, tau_ini, h_value, clay_fraction, albedo, porosity, theta_deg]
    ):
        return float("nan"), float("nan")

    if model_context is None:
        model_context = build_tb_model_context(freq_ghz, clay_fraction, theta_deg)
```

**说明**：grid 函数（L376-389、L458-472）无需改动，它们已正确传递 `model_context=context_cache[key]`。`TbModelContext` 已在 L29-33 定义，L5 已 `from typing import Any`，无需新增 import。

**验证**：
```bash
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\algorithms\providers\Python"
python -c "from algorithms.inversion import retrieve_dynamic_h_grid, ddca_retrieve_grid, retrieve_dynamic_h_pixel, ddca_retrieve_pixel; import inspect; sig1 = inspect.signature(retrieve_dynamic_h_pixel); sig2 = inspect.signature(ddca_retrieve_pixel); assert 'model_context' in sig1.parameters, 'retrieve_dynamic_h_pixel missing model_context'; assert 'model_context' in sig2.parameters, 'ddca_retrieve_pixel missing model_context'; print('OK: signatures accept model_context')"
```

**Commit**：`fix(algorithms): accept model_context param in pixel-level inversion functions to fix TypeError in grid path`

---

### 1.2 layer_router.py status 未导入

**文件**：[Code/backend/app/api/routers/layer_router.py](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/backend/app/api/routers/layer_router.py)

**核验**：
- L4：`from fastapi import APIRouter, HTTPException, Query, Response`（缺 `status`）
- L58：`raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(exc)) from exc` → **触发 NameError**

**修复**（仅 L4 一处改动）：

`old_string`：
```python
from fastapi import APIRouter, HTTPException, Query, Response
```

`new_string`：
```python
from fastapi import APIRouter, HTTPException, Query, Response, status
```

**说明**：保持代码风格一致（其他 router 也用 `status.HTTP_xxx`），不改为字面量 `400`。已确认文件其他位置无重复 `from fastapi import` 语句。

**验证**：
```bash
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\backend"
python -c "from app.api.routers.layer_router import router; print('OK: layer_router imports cleanly')"
```
启动后端后：`curl "http://localhost:8000/api/layers/geo/transform?lng=113&lat=23&source=EPSG:9999&target=EPSG:4326"` 应返回 400 + 错误信息。

**Commit**：`fix(backend): import status symbol in layer_router to prevent NameError on bad CRS`

---

### 1.3 weather_providers_repository.py 加密静默降级

**文件**：[Code/backend/app/services/weather_providers_repository.py](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/backend/app/services/weather_providers_repository.py)

**核验**：
- L79-97 `_encrypt` 在 3 种情况下静默返回明文（无 key / ImportError / Exception）
- 对比 `api_keys_repository.py:85-120` 和 `gee_credentials_repository.py:64-101`：两者都 `from app.services.effective_config import secrets_encryption_required` 并 fail-fast
- `effective_config.py:40-42` 定义：`secrets_encryption_required()` 返回 `env not in {"development", "dev", "test", "testing"}`
- **修正原方案**：只有 `weather_providers_repository.py` 需要 fix，其他两个已正确实现，**不要动它们**

**修复**：将 `weather_providers_repository._encrypt` 改为与 `api_keys_repository._encrypt` 完全一致的模式。

`old_string`（L79-97 完整 `_encrypt` 方法）：
```python
    def _encrypt(self, plaintext: str) -> tuple[str, str]:
        if not self._encryption_key:
            return plaintext, ""
        try:
            from cryptography.hazmat.primitives.ciphers.aead import AESGCM  # type: ignore
            import base64
            import os

            key_bytes = bytes.fromhex(self._encryption_key)
            iv = os.urandom(12)
            aesgcm = AESGCM(key_bytes)
            ct = aesgcm.encrypt(iv, plaintext.encode("utf-8"), None)
            return base64.b64encode(ct).decode("ascii"), base64.b64encode(iv).decode("ascii")
        except ImportError:
            logger.warning("cryptography not installed, storing weather provider config as plaintext")
            return plaintext, ""
        except Exception as e:
            logger.error("Encryption failed for weather provider config: %s", e)
            return plaintext, ""
```

`new_string`：
```python
    def _encrypt(self, plaintext: str) -> tuple[str, str]:
        """AES-GCM 加密，返回 (ciphertext_b64, iv_b64)。无 key 时仅 development 允许明文。"""
        if not self._encryption_key:
            from app.services.effective_config import secrets_encryption_required

            if secrets_encryption_required():
                raise RuntimeError(
                    "Cannot store weather provider config without BACKEND_GEE_CREDENTIALS_ENCRYPTION_KEY "
                    "outside development."
                )
            logger.error("Weather provider encryption key not set, storing plaintext (development only)")
            return plaintext, ""
        try:
            from cryptography.hazmat.primitives.ciphers.aead import AESGCM  # type: ignore
            import base64
            import os

            key_bytes = bytes.fromhex(self._encryption_key)
            iv = os.urandom(12)
            aesgcm = AESGCM(key_bytes)
            ct = aesgcm.encrypt(iv, plaintext.encode("utf-8"), None)
            return base64.b64encode(ct).decode("ascii"), base64.b64encode(iv).decode("ascii")
        except ImportError:
            from app.services.effective_config import secrets_encryption_required

            if secrets_encryption_required():
                raise RuntimeError("cryptography package required to encrypt weather provider config") from None
            logger.warning("cryptography not installed, storing plaintext")
            return plaintext, ""
        except RuntimeError:
            raise
        except Exception as e:
            from app.services.effective_config import secrets_encryption_required

            if secrets_encryption_required():
                raise RuntimeError(f"Encryption failed for weather provider config: {e}") from e
            logger.error("Encryption failed for weather provider config, storing plaintext: %s", e)
            return plaintext, ""
```

**说明**：
- 完全复用 `gee_credentials_repository._encrypt` 的代码结构（仅修改日志措辞和异常消息以区分用途）
- 不新建 `_secrets_crypto.py`，避免引入新模块和迁移成本
- `__init__` 签名无需改动（`encryption_key` 已存在），`environment` 通过 `settings.environment` 全局获取，与另外两个 repo 一致

**验证**：
```bash
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\backend"
# 1. 确认 import 无误
python -c "from app.services.weather_providers_repository import WeatherProvidersRepository; print('OK')"
# 2. 确认 development 环境无 key 时降级为明文（不抛异常）
python -c "
import os
os.environ['BACKEND_ENVIRONMENT'] = 'development'
from app.services.weather_providers_repository import WeatherProvidersRepository
import tempfile, pathlib
repo = WeatherProvidersRepository(pathlib.Path(tempfile.mkdtemp()) / 'test.db', encryption_key='')
ct, iv = repo._encrypt('secret-value')
assert ct == 'secret-value' and iv == '', f'Expected plaintext fallback, got ({ct!r}, {iv!r})'
print('OK: development plaintext fallback works')
"
# 3. 确认生产环境无 key 时抛 RuntimeError
python -c "
import os
os.environ['BACKEND_ENVIRONMENT'] = 'production'
from app.services.weather_providers_repository import WeatherProvidersRepository
import tempfile, pathlib
repo = WeatherProvidersRepository(pathlib.Path(tempfile.mkdtemp()) / 'test.db', encryption_key='')
try:
    repo._encrypt('secret-value')
    print('FAIL: should have raised RuntimeError')
except RuntimeError as e:
    print(f'OK: production raises RuntimeError: {e}')
"
```

**Commit**：`fix(security): fail-fast on weather provider config encryption in production, matching gee/api_keys repos`

---

### 1.4 omega.py porosity 未验证致 least_squares 崩溃

**文件**：[Code/algorithms/providers/Python/algorithms/omega.py](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/algorithms/providers/Python/algorithms/omega.py)

**核验**：
- L24：`_MINERAL_PARTICLE_DENSITY = 2.65`（已存在）
- L2039：`porosity = 1.0 - float(bulk_density_value) / _MINERAL_PARTICLE_DENSITY`（无前置校验）
- L1106-1107：`lower_bounds = (0.02, 0.0)` 和 `upper_bounds = (porosity, 5.0)`（魔法数字 + 未校验 porosity）
- L1108-1113：`least_squares(...)` 若 porosity 为 NaN 或 ≤ 0.02 会抛 ValueError

**修复 1**：在文件顶部新增命名常量

`old_string`（L23-24）：
```python
# 矿物土壤颗粒密度 (g/cm³)，用于孔隙度计算 porosity = 1 - bulk_density / particle_density
_MINERAL_PARTICLE_DENSITY = 2.65
```

`new_string`：
```python
# 矿物土壤颗粒密度 (g/cm³)，用于孔隙度计算 porosity = 1 - bulk_density / particle_density
_MINERAL_PARTICLE_DENSITY = 2.65

# ─── 反演边界与有效性阈值（无量纲） ─────────────────────────────────────────
# 残余/最小土壤含水率下限（m³/m³），least_squares lower_bound
_SOIL_MOISTURE_LOWER_BOUND = 0.02
# 茂密森林 tau 上限（无量纲），least_squares upper_bound
_TAU_UPPER_BOUND = 5.0
# 孔隙度物理下限（无量纲），低于此值视为 bulk_density 异常，跳过像素
_POROSITY_MIN_REASONABLE = 0.02
```

**修复 2**：L2039 porosity 计算前增加 bulk_density 校验

`old_string`（L2039 上下文）：
```python
        porosity = 1.0 - float(bulk_density_value) / _MINERAL_PARTICLE_DENSITY
        retrieved_indices = np.flatnonzero(valid_tau & np.isfinite(omega))
```

`new_string`：
```python
        if not np.isfinite(bulk_density_value) or bulk_density_value <= 0:
            logger.debug("Skip porosity computation: invalid bulk_density=%r", bulk_density_value)
            porosity = float("nan")
        else:
            porosity = 1.0 - float(bulk_density_value) / _MINERAL_PARTICLE_DENSITY
            if porosity <= _POROSITY_MIN_REASONABLE:
                logger.debug("Unrealistic porosity=%r (bulk_density=%r)", porosity, bulk_density_value)
                porosity = float("nan")
        retrieved_indices = np.flatnonzero(valid_tau & np.isfinite(omega))
```

**修复 3**：L1106-1108 替换魔法数字 + 增加 porosity 有效性校验（防止 NaN 污染 least_squares）

`old_string`（L1106-1113 上下文）：
```python
    lower_bounds = (0.02, 0.0)
    upper_bounds = (porosity, 5.0)
    result = least_squares(
        cost_func,
        x0=[0.20, tau_ini],
        bounds=(lower_bounds, upper_bounds),
        jac=lambda x: _finite_difference_jacobian(x, cost_func, lower_bounds, upper_bounds),
    )
    return float(result.x[0]), float(result.x[1])
```

`new_string`：
```python
    if not np.isfinite(porosity) or porosity <= _SOIL_MOISTURE_LOWER_BOUND:
        # 孔隙度无效时 least_squares 会因 bounds 不合理抛 ValueError，直接返回 NaN
        return float("nan"), float("nan")
    lower_bounds = (_SOIL_MOISTURE_LOWER_BOUND, 0.0)
    upper_bounds = (porosity, _TAU_UPPER_BOUND)
    result = least_squares(
        cost_func,
        x0=[0.20, tau_ini],
        bounds=(lower_bounds, upper_bounds),
        jac=lambda x: _finite_difference_jacobian(x, cost_func, lower_bounds, upper_bounds),
    )
    return float(result.x[0]), float(result.x[1])
```

**说明**：保留原 `least_squares` 调用语义，仅替换魔法数字为命名常量并前置校验。`np` 已在文件顶部导入（文件后续大量使用 `np.`）。

**验证**：
```bash
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\algorithms\providers\Python"
python -c "
import numpy as np
from algorithms.omega import _SOIL_MOISTURE_LOWER_BOUND, _TAU_UPPER_BOUND, _POROSITY_MIN_REASONABLE, _MINERAL_PARTICLE_DENSITY
print(f'Constants OK: lower={_SOIL_MOISTURE_LOWER_BOUND}, tau_upper={_TAU_UPPER_BOUND}, porosity_min={_POROSITY_MIN_REASONABLE}, density={_MINERAL_PARTICLE_DENSITY}')
for bd in [np.nan, -1.0, 3.0, 1.5]:
    if not np.isfinite(bd) or bd <= 0:
        print(f'  bulk_density={bd} -> skip (NaN porosity)')
        continue
    p = 1.0 - bd / _MINERAL_PARTICLE_DENSITY
    print(f'  bulk_density={bd} -> porosity={p:.4f}, valid={p > _POROSITY_MIN_REASONABLE}')
"
```

**Commit**：`fix(algorithms): validate porosity before least_squares to prevent ValueError on bad bulk_density`

---

### 1.5 ndvi.py Savitzky-Golay 有效性检查

**文件**：[Code/algorithms/providers/Python/algorithms/ndvi.py](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/algorithms/providers/Python/algorithms/ndvi.py)

**核验**：
- L7：`_SG_DEFAULT_POLYORDER = 6`
- L8：`_SG_DEFAULT_WINDOW_LENGTH = 9`
- L11：`_SG_MIN_VALID_POINTS = 4`
- L77：`if valid_mask.sum() <= _SG_MIN_VALID_POINTS: return np.full(output_days.shape, np.nan, dtype=np.float64)`
- L85：`sg_filtered = savgol_filter(interpolated_8day, sg_window_length, sg_polyorder, mode="interp")`

**修正原方案**：实际崩溃条件不是 `valid_count < sg_window_length`，而是 `len(sg_days) < sg_window_length`。`interpolated_8day` 由 `_linear_interp_with_nan(observation_days[valid_mask], data[valid_mask], sg_days)` 产生，其长度 = `len(sg_days)`。当 `start_time` 到 `end_time` 间距 < `(sg_window_length-1) * sg_step_days`（默认 8 天 × 8 = 64 天）时，`sg_days` 长度 < 9，savgol_filter 崩溃。

**修复 1**：在调用 `savgol_filter` 前增加 `len(sg_days) >= sg_window_length` 校验，不满足时降级为线性插值。

`old_string`（L76-86 上下文）：
```python
    valid_mask = ~np.isnan(data)
    if valid_mask.sum() <= _SG_MIN_VALID_POINTS:
        return np.full(output_days.shape, np.nan, dtype=np.float64)

    interpolated_8day = _linear_interp_with_nan(
        observation_days[valid_mask],
        data[valid_mask],
        sg_days,
    )
    sg_filtered = savgol_filter(interpolated_8day, sg_window_length, sg_polyorder, mode="interp")
    daily_values = _linear_interp_with_nan(sg_days, sg_filtered, output_days)
```

`new_string`：
```python
    valid_mask = ~np.isnan(data)
    valid_count = int(valid_mask.sum())
    if valid_count <= _SG_MIN_VALID_POINTS:
        return np.full(output_days.shape, np.nan, dtype=np.float64)

    # savgol_filter 要求 window_length <= len(data)；sg_days 过短时降级为线性插值
    if sg_days.size < sg_window_length:
        if valid_count < 2:
            return np.full(output_days.shape, np.nan, dtype=np.float64)
        return _linear_interp_with_nan(
            observation_days[valid_mask],
            data[valid_mask],
            output_days,
        )

    interpolated_8day = _linear_interp_with_nan(
        observation_days[valid_mask],
        data[valid_mask],
        sg_days,
    )
    sg_filtered = savgol_filter(interpolated_8day, sg_window_length, sg_polyorder, mode="interp")
    daily_values = _linear_interp_with_nan(sg_days, sg_filtered, output_days)
```

**修复 2**（可选，建议留到 Sprint 3）：将默认 polyorder 从 6 降为 3，避免过拟合（window=9, polyorder=6 等价于 6 阶多项式拟合 9 个点，自由度仅剩 2）。

`old_string`（L7）：
```python
_SG_DEFAULT_POLYORDER = 6
```

`new_string`：
```python
_SG_DEFAULT_POLYORDER = 3
```

**说明**：降 polyorder 会导致历史反演结果略有差异，若需严格复现历史结果可保留 6，但修复 1（降级逻辑）必须做。建议在 Sprint 1 只做修复 1，修复 2 留到 Sprint 3 与 NDVI 算法 review 一起做。

**验证**：
```bash
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\algorithms\providers\Python"
python -c "
import numpy as np
from datetime import datetime, timedelta
from algorithms.ndvi import vi_sg_interpolate, build_datetime_sequence, to_day_numbers

# 短时间窗（5 天，sg_days 长度=1 < 9），应降级为线性插值而非崩溃
obs_dates = [datetime(2024,1,1) + timedelta(days=i*8) for i in range(5)]
data = np.array([0.3, 0.4, 0.5, 0.45, 0.55])
sg_dates = build_datetime_sequence(datetime(2024,1,1), datetime(2024,1,5), 8)  # 长度=1
output_dates = build_datetime_sequence(datetime(2024,1,1), datetime(2024,1,5), 1)  # 长度=5
result = vi_sg_interpolate(data, to_day_numbers(obs_dates), to_day_numbers(sg_dates), to_day_numbers(output_dates))
print(f'Short-window result shape={result.shape}, all-finite={np.all(np.isfinite(result))}')
print('OK: no crash on short time window')
"
```

**Commit**：`fix(algorithms): fall back to linear interp when sg_days shorter than savgol window_length`

---

### 1.6 physics.py zkb 负值开方静默 NaN

**文件**：[Code/algorithms/providers/Python/algorithms/physics.py](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/algorithms/providers/Python/algorithms/physics.py)

**核验**：
- L357-360 共 4 处 `math.sqrt((math.sqrt(epwbx**2 + epwby**2) ± epwbx) / 2)`
- 浮点精度损失可能导致 `(mag - epwbx) / 2` 为极小负数，`math.sqrt` 抛 ValueError
- L91 `import cmath` 在函数 `_fresnel_reflectance_kernel_py` 内部

**修复 1**：在文件顶部添加 `_safe_sqrt` 工具函数

`old_string`（L41-59 上下文，物理量阈值块末尾）：
```python
# 频率有效范围 (GHz)
_FREQ_GHZ_MIN = 0.1
_FREQ_GHZ_MAX = 40.0
# 黏粒含量有效范围（无量纲，0-1）
_CLAY_FRACTION_MIN = 0.0
_CLAY_FRACTION_MAX = 1.0
```

`new_string`：
```python
# 频率有效范围 (GHz)
_FREQ_GHZ_MIN = 0.1
_FREQ_GHZ_MAX = 40.0
# 黏粒含量有效范围（无量纲，0-1）
_CLAY_FRACTION_MIN = 0.0
_CLAY_FRACTION_MAX = 1.0


def _safe_sqrt(x: float) -> float:
    """非负开方，负值或 NaN 返回 0.0。

    介电常数的实部/虚部物理上应非负；浮点精度损失可能产生 -1e-20，
    math.sqrt 会抛 ValueError 而非返回 NaN，需显式 clamp。
    """
    if not math.isfinite(x) or x < 0:
        return 0.0
    return math.sqrt(x)
```

**修复 2**：L357-360 用 `_safe_sqrt` 替换

`old_string`（L357-360）：
```python
    znb = math.sqrt((math.sqrt(epwbx**2 + epwby**2) + epwbx) / 2)
    zkb = math.sqrt((math.sqrt(epwbx**2 + epwby**2) - epwbx) / 2)
    znu = math.sqrt((math.sqrt(epwux**2 + epwuy**2) + epwux) / 2)
    zku = math.sqrt((math.sqrt(epwux**2 + epwuy**2) - epwux) / 2)
```

`new_string`：
```python
    mag_b = math.sqrt(epwbx * epwbx + epwby * epwby)  # 复数模长，非负
    znb = _safe_sqrt((mag_b + epwbx) / 2)
    zkb = _safe_sqrt((mag_b - epwbx) / 2)
    mag_u = math.sqrt(epwux * epwux + epwuy * epwuy)  # 复数模长，非负
    znu = _safe_sqrt((mag_u + epwux) / 2)
    zku = _safe_sqrt((mag_u - epwux) / 2)
```

**修复 3**（可选，建议留到 Sprint 3）：将 L91 函数内的 `import cmath` 移到文件顶部。Sprint 1 只做修复 1 + 修复 2。

**说明**：`math` 已在文件顶部导入（L4 之前）。`cmath` 移到顶部属于代码整洁度改进，非阻断性。

**验证**：
```bash
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\algorithms\providers\Python"
python -c "
import math
from algorithms.physics import _safe_sqrt
# 正常值
assert _safe_sqrt(4.0) == 2.0
# 负值（浮点精度损失场景）
assert _safe_sqrt(-1e-20) == 0.0
# NaN / inf
assert _safe_sqrt(float('nan')) == 0.0
assert _safe_sqrt(float('inf')) == 0.0
print('OK: _safe_sqrt handles edge cases')
"
```

**Commit**：`fix(algorithms): clamp negative sqrt args in mironov_dielectric to prevent ValueError`

---

### 1.7 weatherengine/service.py Any 未导入

**文件**：[Code/backend/app/weatherengine/service.py](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/backend/app/weatherengine/service.py)

**核验**：
- L1：`from __future__ import annotations`（注解延迟求值，运行时不立即崩溃）
- L1-28 imports 区域：**无 `from typing import Any`**
- 文件中多处 `dict[str, Any]` / `Any` 注解（约 10+ 处，mypy 报错；移除 `__future__` 后会 NameError）

**修复**：在 L1-28 imports 区域添加 `from typing import Any`。

`old_string`（L1-3）：
```python
from __future__ import annotations

from datetime import datetime, timezone
```

`new_string`：
```python
from __future__ import annotations

from datetime import datetime, timezone
from typing import Any
```

**说明**：保持字母顺序与现有 import 风格一致。`Any` 仅用于类型注解，运行时无副作用，但补全 import 是 mypy/类型检查的必要条件。

**验证**：
```bash
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\backend"
python -c "from app.weatherengine.service import WeatherEngineService; import typing; assert hasattr(typing, 'Any'); print('OK: service module imports cleanly')"
# 可选（如有 mypy）：
# mypy app/weatherengine/service.py
```

**Commit**：`fix(backend): import Any in weatherengine service for type annotations`

---

## Sprint 2：高优先级性能与架构

### 2.1 ndvi.py 向量化

**文件**：[Code/algorithms/providers/Python/algorithms/ndvi.py](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/algorithms/providers/Python/algorithms/ndvi.py#L133-L142)

**核验**：L133-142 逐像素循环调用 `vi_sg_interpolate`，未利用 `savgol_filter` 支持沿 axis 处理 N 维数组的特性。

**修复策略**：
1. 先做 Sprint 1.5（修复 sg_days 长度校验），再做本项
2. 对 `valid_counts >= sg_window_length` 且 `sg_days.size >= sg_window_length` 的像素批量调用 `savgol_filter(data_2d, window, polyorder, axis=1, mode="interp")`
3. 对不足的像素仍逐个调用 `vi_sg_interpolate`（保留降级逻辑）

**关键代码骨架**（替换 L133-142）：
```python
    # 向量化：对有效点充足的像素批量做 SG 滤波
    valid_counts = (~np.isnan(flattened)).sum(axis=1)  # shape: (n_pixels,)
    sg_capable_mask = (valid_counts >= sg_window_length) & (sg_days.size >= sg_window_length)

    if np.any(sg_capable_mask):
        # 提取充足像素的数据，用 0 填充 NaN 以满足 savgol_filter 输入要求
        data_sufficient = flattened[sg_capable_mask].copy()
        nan_mask = np.isnan(data_sufficient)
        data_sufficient[nan_mask] = 0.0  # 临时填充

        # 沿时间轴（axis=1）批量滤波
        filtered = savgol_filter(
            data_sufficient, sg_window_length, sg_polyorder, axis=1, mode="interp"
        )
        # 恢复 NaN 位置
        filtered[nan_mask] = np.nan

        # 线性插值到 output_days（每个像素的 sg_days 相同，可批量做）
        for idx, pixel_idx in enumerate(np.where(sg_capable_mask)[0]):
            daily_flattened[pixel_idx] = _linear_interp_with_nan(sg_days, filtered[idx], output_days)

    # 对有效点不足的像素降级为 vi_sg_interpolate（含线性插值降级逻辑）
    for pixel_idx in np.where(~sg_capable_mask)[0]:
        daily_flattened[pixel_idx] = vi_sg_interpolate(
            flattened[pixel_idx],
            observation_days,
            sg_days,
            output_days,
            gap_threshold_days=gap_threshold_days,
            sg_polyorder=sg_polyorder,
            sg_window_length=sg_window_length,
        )
```

**验证**：构造 100×100×46 测试数据，对比向量化前后耗时（预期 10-100× 提升）与数值差异（应 < 1e-10）。

**Commit**：`perf(algorithms): vectorize ndvi SG filter along pixel axis for ~50x speedup`

---

### 2.2 block_inversion.py 循环优化

**文件**：[Code/algorithms/providers/Python/algorithms/block_inversion.py](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/algorithms/providers/Python/algorithms/block_inversion.py#L260-L310)

**核验**：L263-306 三层循环（chunk → day → pixel），每天重建 `valid_mask` 和 `context_cache`。

**修复策略**：
1. **保守优化（推荐）**：将 `valid_mask` 提到 day 循环外层（前提：tbv/tbh/ts/tau 静态参数不随 day 变化，需核验），context_cache 也提前构建一次
2. **激进优化**：用 `ProcessPoolExecutor` 并行化 chunk 维度。**风险**：Celery worker 默认 prefork 模式，子进程内再开 ProcessPoolExecutor 可能死锁；必须用 `concurrent.futures.ThreadPoolExecutor` 或单进程版

**Sprint 2 推荐做法**：只做 `valid_mask` 外提，不做多进程。多进程留到 Sprint 3 经过 Celery worker 启动模式验证后再做。

**验证**：对比修复前后输出数值一致；性能测试时间序列反演 1000 像素 × 30 天的耗时。

**Commit**：`perf(algorithms): hoist valid_mask out of day loop in block_inversion`

---

### 2.3 WeatherEngineService 拆分

**文件**：[Code/backend/app/weatherengine/service.py](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/backend/app/weatherengine/service.py)（1866 行）

**核验**：L532-730 `_build_map_layer_outputs` 内大量 `if layer_id == "temperature" / elif ...` 分支。

**修复策略**：策略模式 + 注册表，详见原方案 L540-640。本 Sprint 仅做基础设施（`layer_outputs/base.py` + `registry.py` + `default.py`），不迁移具体分支。具体分支迁移留到 Sprint 3 增量做。

**Sprint 2 范围**：
1. 新建 `Code/backend/app/weatherengine/layer_outputs/` 目录
2. 创建 `base.py`（`LayerOutputStrategy` ABC）、`registry.py`（`register_strategy` 装饰器 + `get_strategy` 函数）、`default.py`（`DefaultLayerOutput` 透传到原 `_build_map_layer_outputs` 逻辑）
3. `service.py` 的 `_build_map_layer_outputs` 改为：先查 registry，未命中则走原 if/elif 链

**Commit**：`refactor(weatherengine): introduce layer_outputs strategy registry (no behavior change)`

---

### 2.4 SQLite 连接池

**文件**：四个 repository 的 `_connect` 方法
- [api_keys_repository.py:80-83](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/backend/app/services/api_keys_repository.py#L80-L83)
- [gee_credentials_repository.py:59-62](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/backend/app/services/gee_credentials_repository.py#L59-L62)
- [weather_providers_repository.py:72-75](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/backend/app/services/weather_providers_repository.py#L72-L75)
- [workflow_repository.py:283-295](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/backend/app/services/workflow_repository.py#L283-L295)（已有 WAL，但每次新建连接）

**修复**：新建 `Code/backend/app/services/_sqlite_pool.py`，提供 `ThreadLocalSQLitePool` 类。详见原方案 L654-727。

**关键约束**：
- 必须用 `threading.local()`（SQLite 连接不能跨线程共享）
- 必须启用 WAL + synchronous=NORMAL（与 `workflow_repository` 一致）
- `with self._connect() as conn` 语义保持不变（`sqlite3.Connection` 上下文管理器会 commit/rollback 但不关闭，正好符合连接复用）
- 必须提供 `close_thread_connection()` 用于测试清理

**Sprint 2 范围**：
1. 新建 `_sqlite_pool.py`
2. **仅迁移 `api_keys_repository.py`** 作为试点（最简单的 repo）
3. 跑通所有 api_keys 单测后，再迁移其他 3 个

**Commit**：`perf(backend): introduce ThreadLocalSQLitePool and migrate api_keys_repository`

---

### 2.5 remote_storage_credentials_repository 原子性

**文件**：[Code/backend/app/services/remote_storage_credentials_repository.py](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/backend/app/services/remote_storage_credentials_repository.py#L134-L235)

**核验**：
- L181-187：`self._archive_secrets(...)` 在 L191 的 `with self._connect() as conn:` 块**之外**调用
- `_archive_secrets` 内部必须自己开连接（否则无法访问 DB），形成独立事务
- 主 upsert 在 L191-232 的 `with` 块内
- **结论**：原方案描述正确，跨事务问题真实存在。若 `_archive_secrets` commit 后、主 upsert commit 前进程崩溃，归档已写但主记录未更新

**修复**：将 `_archive_secrets` 改为接收 `conn` 参数，调用方将其放入主 `with` 块内。

`old_string`（L175-191 上下文）：
```python
        # Archive previous secrets when plaintext changes
        if existing is not None:
            old_secret = existing.get("secret") or ""
            old_key = existing.get("private_key_pem") or ""
            if (secret is not None and secret != old_secret) or (
                private_key_pem is not None and (private_key_pem or "") != old_key
            ):
                self._archive_secrets(
                    profile_id=profile_id,
                    secret=old_secret,
                    private_key_pem=old_key or None,
                    superseded_at=now,
                    source="user",
                )

        sec_ct, sec_iv = self._encrypt(secret_val)
        key_ct, key_iv = self._encrypt(key_val) if key_val else ("", "")
        with self._connect() as conn:
            conn.execute(
```

`new_string`：
```python
        sec_ct, sec_iv = self._encrypt(secret_val)
        key_ct, key_iv = self._encrypt(key_val) if key_val else ("", "")
        with self._connect() as conn:
            # Archive previous secrets in the SAME transaction as the main upsert
            if existing is not None:
                old_secret = existing.get("secret") or ""
                old_key = existing.get("private_key_pem") or ""
                if (secret is not None and secret != old_secret) or (
                    private_key_pem is not None and (private_key_pem or "") != old_key
                ):
                    self._archive_secrets(
                        conn,
                        profile_id=profile_id,
                        secret=old_secret,
                        private_key_pem=old_key or None,
                        superseded_at=now,
                        source="user",
                    )
            conn.execute(
```

**修复 2**：`_archive_secrets` 签名改为接收 `conn`，删除内部 `with self._connect()`。

需先读取 `_archive_secrets` 当前实现，将其 `with self._connect() as conn:` 块去掉，使用传入的 `conn` 参数，并去掉 `conn.commit()`（让外层 `with` 块统一 commit）。

**验证**：单测模拟 `_archive_secrets` 后抛异常，确认主记录和归档都不写入（原子性）。

**Commit**：`fix(backend): make remote_storage archive_secrets atomic with main upsert`

---

### 2.6 config_service.test_api_key async化

**文件**：[Code/backend/app/services/config_service.py](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/backend/app/services/config_service.py#L320-L382)

**核验**：L320 `async def test_api_key`，L331-355 内部用 `urllib.request.urlopen`（同步阻塞），会卡死事件循环。

**修复**：改用 `httpx.AsyncClient`。先确认 `httpx` 是否已在 requirements.txt（FastAPI 依赖中通常已包含）。

`old_string`（L331-341 tianditu 分支示例）：
```python
        if key_name == "tianditu":
            # 测试天地图 API：请求一个瓦片
            import urllib.request
            url = f"https://t0.tianditu.gov.cn/img_w/wmts?SERVICE=WMTS&REQUEST=GetTile&VERSION=1.0.0&LAYER=img&STYLE=default&TILEMATRIXSET=w&FORMAT=tiles&TILECOL=0&TILEROW=0&TILEMATRIX=0&tk={key_value}"
            req = urllib.request.Request(url, headers={"User-Agent": "CGDA-Backend/1.0"})
            with urllib.request.urlopen(req, timeout=10) as resp:
                if resp.status == 200:
                    repo.update_test_status(key_name, "ok")
                    return True, "天地图 API Key 有效"
                else:
                    msg = f"天地图 API 返回 HTTP {resp.status}"
                    repo.update_test_status(key_name, "failed")
                    return False, msg
```

`new_string`：
```python
        if key_name == "tianditu":
            # 测试天地图 API：请求一个瓦片
            import httpx
            url = f"https://t0.tianditu.gov.cn/img_w/wmts?SERVICE=WMTS&REQUEST=GetTile&VERSION=1.0.0&LAYER=img&STYLE=default&TILEMATRIXSET=w&FORMAT=tiles&TILECOL=0&TILEROW=0&TILEMATRIX=0&tk={key_value}"
            try:
                async with httpx.AsyncClient(timeout=httpx.Timeout(10.0, connect=5.0)) as client:
                    resp = await client.get(url, headers={"User-Agent": "CGDA-Backend/1.0"})
                if resp.status_code == 200:
                    repo.update_test_status(key_name, "ok")
                    return True, "天地图 API Key 有效"
                else:
                    msg = f"天地图 API 返回 HTTP {resp.status_code}"
                    repo.update_test_status(key_name, "failed")
                    return False, msg
            except httpx.HTTPError as e:
                repo.update_test_status(key_name, "failed")
                return False, f"天地图 API 请求失败: {e}"
```

对 `baidu` 分支同样改造。`backend_auth` 与 `else` 分支无网络请求，无需改。

**依赖检查**：若 `requirements.txt` 无 `httpx`，需添加 `httpx>=0.27.0`。FastAPI 通常已传递依赖 `httpx`，但显式声明更稳妥。

**验证**：mock `httpx.AsyncClient`，确认 `test_api_key` 正确处理 200/401/超时；调用期间确认其他 async 请求不被阻塞。

**Commit**：`fix(backend): use httpx.AsyncClient in test_api_key to avoid blocking event loop`

---

### 2.7 前端 fetch 抽取

**文件**：
- [Code/frontend/src/services/runtime-api.ts](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/frontend/src/services/runtime-api.ts)
- [Code/frontend/src/services/workflow-definition-api.ts](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/frontend/src/services/workflow-definition-api.ts)
- [Code/frontend/src/services/settings-api.ts](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/frontend/src/services/settings-api.ts)

**核验修正**：探索 agent 反馈"未发现明显重复 fetch 逻辑"。需在 Sprint 2 实施前**先实际读这三个文件**确认：
1. 是否真的存在 `fetch()` 重复样板（base URL 解析、API key 注入、错误处理）
2. `runtime-api.ts` 是否仍有指向 `127.0.0.1:7777` 的死调试代码
3. `console.log` 调试输出是否需清理

**建议**：本项可在 Sprint 2 末尾做，先做后端高优先级项。若核验后发现重复确实存在，按原方案 L826-920 抽取 `_shared-fetch.ts`。若不存在，跳过本项并记录原因。

**Commit**（如实施）：`refactor(frontend): extract shared fetch helper and remove dead debug code`

---

### 2.8 omega.py pixel_chunk_size 清理

**文件**：[Code/algorithms/providers/Python/algorithms/omega.py](file:///d:/temp_desktop/Proj/Comprehensive%20Geographic%20Data%20Analysis%20system/Code/algorithms/providers/Python/algorithms/omega.py#L2237)

**修复**：方案 A（删除配置项），详见原方案 L932-940。先读取 `OmegaConfig` 当前定义，确认 `pixel_chunk_size` 字段位置，删除字段及其使用点。

**Commit**：`refactor(algorithms): remove unused pixel_chunk_size from OmegaConfig`

---

## Sprint 3-4 与项目级修复

Sprint 3-4 共 44+ 项中优先级改进，详见原方案 `完整项目修复方案.md` L951-2151。本精修版仅在以下几项上对原方案做修正：

### 3.2.1 修正：api_keys_repository 与 gee_credentials_repository 不需改造
原方案 1.3 步骤 3 称"同样改造 `api_keys_repository.py` 和 `gee_credentials_repository.py`"，**此为错误**。两者已正确使用 `secrets_encryption_required()`，无需改动。

### 3.2.7 effective_config.py 跨进程
原方案建议改为 Redis + 短 TTL。**补充建议**：本项前先评估 `_snapshot` 实际性能瓶颈——若 `effective_config` 调用频率 < 100 QPS，进程内 RLock + 单实例 hydration 已足够，无需引入 Redis。仅在多 worker 部署且配置频繁热更新时才需 Redis。

### 项目级基础设施修复
保留原方案的以下项：
- 添加 `mypy.ini` 严格模式配置（Sprint 3）
- 添加 `pre-commit` 钩子（Sprint 3）
- 在 `launch.py` 启动时强制调用 `assert_encryption_policy()`（Sprint 1 可一并做，详见下文）

### 可选：Sprint 1 补充项 —— 启动时强制加密策略
**文件**：`Code/backend/app/main.py`（或启动入口）

在 FastAPI `startup` 事件中调用 `assert_encryption_policy()`，确保非 development 环境无 key 时立即拒绝启动，而非等到首次加密失败。

```python
from app.services.effective_config import assert_encryption_policy

@app.on_event("startup")
async def _startup_encryption_check():
    assert_encryption_policy()  # production 无 key 时 raise RuntimeError
```

**Commit**：`fix(backend): assert encryption policy at startup to fail-fast on missing key in production`

---

## 验证清单

### Sprint 1 完成后必跑

```bash
# 后端导入与语法检查
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\backend"
python -c "
# 1. layer_router 导入
from app.api.routers.layer_router import router
print('1. layer_router OK')

# 2. weather_providers_repository 加密策略
from app.services.weather_providers_repository import WeatherProvidersRepository
print('2. WeatherProvidersRepository OK')

# 3. weatherengine service Any 导入
from app.weatherengine.service import WeatherEngineService
print('3. WeatherEngineService OK')

# 4. effective_config
from app.services.effective_config import secrets_encryption_required, assert_encryption_policy
print('4. effective_config OK')
"

# 算法层导入与单点测试
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\algorithms\providers\Python"
python -c "
# 5. inversion 签名
from algorithms.inversion import retrieve_dynamic_h_pixel, ddca_retrieve_pixel
import inspect
assert 'model_context' in inspect.signature(retrieve_dynamic_h_pixel).parameters
assert 'model_context' in inspect.signature(ddca_retrieve_pixel).parameters
print('5. inversion signatures OK')

# 6. physics _safe_sqrt
from algorithms.physics import _safe_sqrt
assert _safe_sqrt(-1e-20) == 0.0
print('6. _safe_sqrt OK')

# 7. omega 常量
from algorithms.omega import _SOIL_MOISTURE_LOWER_BOUND, _TAU_UPPER_BOUND, _POROSITY_MIN_REASONABLE
print('7. omega constants OK')

# 8. ndvi 短窗降级
import numpy as np
from datetime import datetime, timedelta
from algorithms.ndvi import vi_sg_interpolate, build_datetime_sequence, to_day_numbers
obs = [datetime(2024,1,1)+timedelta(days=i*8) for i in range(5)]
data = np.array([0.3,0.4,0.5,0.45,0.55])
sg = build_datetime_sequence(datetime(2024,1,1), datetime(2024,1,5), 8)  # 长度=1
out = build_datetime_sequence(datetime(2024,1,1), datetime(2024,1,5), 1)
r = vi_sg_interpolate(data, to_day_numbers(obs), to_day_numbers(sg), to_day_numbers(out))
assert r.shape == out.shape
print('8. ndvi short-window fallback OK')
"
```

### Sprint 1 完成后端到端验证

1. 启动后端：`python launch.py start fastapi`
2. 验证 layer_router：`curl "http://localhost:8000/api/layers/geo/transform?lng=113&lat=23&source=EPSG:9999&target=EPSG:4326"` → 应返回 400（而非 500）
3. 在 Settings → Weather Providers 添加一个 Provider，检查 DB 文件中 `config_encrypted` 字段非明文（生产环境）或正确降级（开发环境）
4. 跑一次天气图层生成，确认无 TypeError from inversion
5. 跑一次 NDVI 反演（短时间窗），确认无 savgol_filter ValueError

### Sprint 2 完成后

1. NDVI 100×100×46 测试数据反演耗时对比（预期 10×+ 提升）
2. SQLite 多线程并发测试：`ThreadPoolExecutor` 8 线程并发调用 `count_active_runs` 1000 次，无 ProgrammingError
3. `test_api_key` 期间并发其他 async 请求，确认不阻塞
4. `remote_storage_credentials.upsert` 中途异常，确认主记录和归档原子性

### 每个 commit 后

```bash
# 后端
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\backend"
python -m py_compile app/api/routers/layer_router.py app/services/weather_providers_repository.py app/weatherengine/service.py
# 算法
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\algorithms\providers\Python"
python -m py_compile algorithms/inversion.py algorithms/omega.py algorithms/ndvi.py algorithms/physics.py
# 前端（如改了前端）
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\frontend"
npm run build
```

---

## 执行注意事项

1. **每项修复独立 commit**：commit message 用 `fix:` / `refactor:` / `perf:` 前缀，遵循项目 `git-commit-message.md` 规则
2. **顺序无关**：Sprint 1 的 7 项彼此独立，可任意顺序实施；建议按 1.7 → 1.2 → 1.6 → 1.5 → 1.4 → 1.1 → 1.3 顺序（从简单到复杂）
3. **不动 gee_credentials_repository 和 api_keys_repository**：原方案 1.3 步骤 3 是错误的，这两个文件已正确实现加密策略
4. **不引入 _secrets_crypto.py 新模块**：复用 `effective_config.secrets_encryption_required()` 即可
5. **Sprint 2.7 前先核验**：探索 agent 反馈前端可能无重复 fetch，实施前先实际读三个文件确认
6. **Sprint 2.4 分阶段迁移**：先迁移 `api_keys_repository` 试点，跑通后再迁移其他 3 个
7. **Sprint 2.2 不做多进程**：仅做 `valid_mask` 外提；多进程需先验证 Celery worker 启动模式
8. **保留原方案文件**：本精修版仅作为执行指南，