# 项目修复方案（规范完整版 v3）

> 本方案在 v2 精修执行版基础上，通过 2026-07-18 第二轮直接代码核验（读取源文件 + 4 个并行探索代理 + 矛盾交叉验证）进行以下增补与修正：
>
> 1. **修正 v2 错误**：v2 "Sprint 1 补充项 — 启动时强制加密策略" 已在 `main.py` L79 + `effective_config.py` L72 实现，**无需新增**。
> 2. **新增遗漏问题**：`weather_providers_repository._connect` 未启用 WAL（与其他 3 个 repo 不一致）；`_decrypt` 静默降级；前端 `runtime-api.ts` 含 `127.0.0.1:7777` 死调试代码。
> 3. **交叉验证修正 Agent 幻觉**：两个探索代理分别误报"service.py 已有 `from typing import Any`"和"`_archive_secrets` 在 with 块内"，经直接读文件确认 v2 原判断正确。
> 4. **补充项目级基础设施缺口**：无 `mypy.ini`、无 `.pre-commit-config.yaml`、前端 `package.json` 无 lint 脚本。
> 5. **明确分支策略**：当前在 `dev` 分支（最新 commit `f92e200`），所有修复 commit 到 `dev`，不直接动 `main`。
>
> **执行顺序**：严格按 Sprint 1 → Sprint 2 → Sprint 3 → Sprint 4。Sprint 1 的 7 项彼此独立，建议按 1.7 → 1.2 → 1.6 → 1.5 → 1.4 → 1.1 → 1.3 顺序（从简单到复杂）实施，每项独立 commit。
>
> **本方案取代 v2**。v2 中正确的部分（Sprint 1.1-1.7 的 old_string/new_string）在本方案中直接引用，不再重复贴出完整代码片段，仅标注"沿用 v2"并给出关键行号锚点。

---

## 目录

- [当前状态分析](#当前状态分析)
- [Sprint 1：阻断性修复（立即实施）](#sprint-1阻断性修复立即实施)
- [Sprint 2：高优先级性能与架构](#sprint-2高优先级性能与架构)
- [Sprint 3：中优先级代码质量](#sprint-3中优先级代码质量)
- [Sprint 4：项目级基础设施](#sprint-4项目级基础设施)
- [验证清单](#验证清单)
- [假设与决策](#假设与决策)

---

## 当前状态分析

### 已核验的真实状态（2026-07-18 直接读文件确认）

| 编号 | 文件 | 行号 | 状态 | 核验方式 |
|------|------|------|------|----------|
| 1.1 | `inversion.py` | L244-280, L268, L283-315, L303, L388, L471 | ❌ pixel 函数不接受 `model_context`，grid 调用传该参数 → TypeError | 直接读文件 |
| 1.2 | `layer_router.py` | L4, L58 | ❌ `from fastapi import` 缺 `status`；L58 用 `status.HTTP_400_BAD_REQUEST` → NameError | 直接读文件 |
| 1.3 | `weather_providers_repository.py` | L45, L79-97 | ❌ `_encrypt` 三处静默返回明文；`__init__` L45 确接收 `encryption_key` | 直接读文件 |
| 1.4 | `omega.py` | L24, L2039, L1106-1108 | ❌ L2039 porosity 无校验；L1106-1108 `least_squares` 用魔法数字 `(0.02, 0.0)` `(porosity, 5.0)` | 直接读文件 |
| 1.5 | `ndvi.py` | L7-11, L76-86 | ❌ L77 仅检查 `valid_count <= 4`，未检查 `sg_days.size < sg_window_length(=9)` → savgol_filter 崩溃 | 直接读文件 |
| 1.6 | `physics.py` | L357-360 | ❌ 4 处 `math.sqrt((mag ± epwbx)/2)` 浮点精度损失可能产生负值 → ValueError | 直接读文件 |
| 1.7 | `service.py` | L1-28 | ❌ imports 区域无 `from typing import Any`；文件 10+ 处用 `Any` 注解 | 直接读文件 |
| — | `main.py` + `effective_config.py` | L79, L72 | ✅ **已实现**启动加密策略检查（v2 补充项为假阳性） | 直接读文件 |
| 新发现 | `weather_providers_repository.py` | L72-75 | ❌ `_connect` 未启用 WAL/synchronous=NORMAL（其他 3 repo 均已启用） | 直接读文件 |
| 新发现 | `weather_providers_repository.py` | L121-132 | ⚠️ `_decrypt` 无 key 时静默返回密文不报错（与 `_encrypt` 降级不一致） | 直接读文件 |
| 2.5 | `remote_storage_credentials_repository.py` | L181-187, L191, L387, L420 | ❌ `_archive_secrets` 调用在主 `with` 块外；`_archive_secrets` 内部独立 `with`+`commit` → 跨事务 | 直接读文件 |
| 2.6 | `config_service.py` | L320, L331-334, L344-349 | ❌ `async def test_api_key` 内用 `urllib.request.urlopen`（同步阻塞事件循环） | 直接读文件 |
| 2.4 | 4 个 SQLite repo | 各自 `_connect` | ❌ 每次新建连接；weather_providers 甚至无 WAL | 直接读文件 |
| 2.7 | `runtime-api.ts` | L18-21 | ❌ 含 `127.0.0.1:7777` 死调试代码 | 探索代理 + 确认 |
| 2.7 | `settings-api.ts` | L209-252 | ✅ 已有 `settingsFetch` 封装 | 探索代理 |

### 项目级基础设施缺口

| 项目 | 状态 | 说明 |
|------|------|------|
| `mypy.ini` / `pyproject.toml [tool.mypy]` | ❌ 缺失 | 算法层 `pyproject.toml` 有部分配置但不完整 |
| `.pre-commit-config.yaml` | ❌ 缺失 | 无 pre-commit 钩子 |
| 前端 `package.json` lint 脚本 | ❌ 缺失 | 无 eslint/prettier 配置 |
| `requirements.txt` 中 `httpx` | ⚠️ 待确认 | FastAPI 通常传递依赖，但未显式声明 |
| `celery-redbeat` | ✅ 未安装 | memory 记录已确认，启动参数无残留 |
| 前端测试文件 | ❌ 缺失 | 无 `*.test.ts` / `*.spec.ts` |
| 后端测试目录 | ✅ 存在 | `Code/backend/tests/` |
| 算法测试目录 | ✅ 存在 | `Code/algorithms/providers/Python/tests/` |

### Git 状态

- **当前分支**：`dev`（非 `main`）
- **最新 commit**：`f92e200 feat: workflow editor, loading animation, multi-provider weather engine, and config enhancements`
- **.gitignore**：L42 `Code/backend/.data/` 已忽略；根级 `.data/` 经 `git check-ignore` 确认也已忽略（L69 附近），但 commit 前仍需 `git status` 检查
- **远程协议**：HTTPS（`https://github.com/li589/WebGIS.git`），memory 记录网络已恢复

---

## Sprint 1：阻断性修复（立即实施）

### 1.1 inversion.py grid→pixel 签名不匹配

**文件**：`Code/algorithms/providers/Python/algorithms/inversion.py`

**核验结论**：✅ v2 描述完全正确
- L244-280 `retrieve_dynamic_h_pixel` 签名末尾 `theta_deg: float,` → `) -> float:`，不接受 `model_context`
- L268 内部 `model_context = build_tb_model_context(freq_ghz, clay_fraction, theta_deg)`
- L283-315 `ddca_retrieve_pixel` 同理，L303 内部调用
- L388 grid 调用传 `model_context=context_cache[key]` → TypeError
- L471 grid 调用传 `model_context=context_cache[key]` → TypeError
- L4 已有 `from typing import Any`；L29-33 有 `TbModelContext`；L74-82 有 `build_tb_model_context`

**修复**：沿用 v2 Sprint 1.1 的 old_string/new_string（两个函数签名末尾加 `model_context: TbModelContext | None = None`，内部改为 `if model_context is None: model_context = build_tb_model_context(...)`）。

**验证**：
```bash
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\algorithms\providers\Python"
python -c "from algorithms.inversion import retrieve_dynamic_h_pixel, ddca_retrieve_pixel; import inspect; assert 'model_context' in inspect.signature(retrieve_dynamic_h_pixel).parameters; assert 'model_context' in inspect.signature(ddca_retrieve_pixel).parameters; print('OK')"
```

**Commit**：`fix(algorithms): accept model_context param in pixel-level inversion functions to fix TypeError in grid path`

---

### 1.2 layer_router.py status 未导入

**文件**：`Code/backend/app/api/routers/layer_router.py`

**核验结论**：✅ v2 描述正确
- L4：`from fastapi import APIRouter, HTTPException, Query, Response`（缺 `status`）
- L58：`raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, ...)` → NameError
- 文件仅此一处 `from fastapi import`

**修复**：沿用 v2 Sprint 1.2（L4 末尾加 `, status`）。

**验证**：
```bash
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\backend"
python -c "from app.api.routers.layer_router import router; print('OK')"
```

**Commit**：`fix(backend): import status symbol in layer_router to prevent NameError on bad CRS`

---

### 1.3 weather_providers_repository.py 加密静默降级

**文件**：`Code/backend/app/services/weather_providers_repository.py`

**核验结论**：✅ v2 描述正确，补充新发现
- L45：`def __init__(self, db_path: str | Path, encryption_key: str = "") -> None:` — 确接收 `encryption_key`（v2 假设正确）
- L79-97 `_encrypt`：三处静默返回明文（L80-81 无 key / L92-94 ImportError / L95-97 Exception）
- L27-36 imports：无 `from app.services.effective_config import`
- **新发现**：L121-132 `_decrypt` 在无 key 时（L124-125）静默返回 `ciphertext_b64` 不报错 — 与 `_encrypt` 降级策略不一致，但 `_decrypt` 返回密文比 `_encrypt` 返回明文风险低（解密失败不影响存储安全），**Sprint 1 不改 `_decrypt`，留到 Sprint 3 统一 review**

**修复**：沿用 v2 Sprint 1.3 的 old_string/new_string（`_encrypt` 改为复用 `secrets_encryption_required()` fail-fast 模式）。`api_keys_repository` 和 `gee_credentials_repository` **不动**（已正确实现）。

**验证**：沿用 v2 Sprint 1.3 的三段验证脚本（import / development 降级 / production raise）。

**Commit**：`fix(security): fail-fast on weather provider config encryption in production, matching gee/api_keys repos`

---

### 1.4 omega.py porosity 未验证致 least_squares 崩溃

**文件**：`Code/algorithms/providers/Python/algorithms/omega.py`

**核验结论**：✅ v2 描述正确
- L24：`_MINERAL_PARTICLE_DENSITY = 2.65` ✅
- L2039：`porosity = 1.0 - float(bulk_density_value) / _MINERAL_PARTICLE_DENSITY`（无前置校验）✅
- L1106-1107：`lower_bounds = (0.02, 0.0)` / `upper_bounds = (porosity, 5.0)` ✅
- L1108-1113：`least_squares(..., bounds=(lower_bounds, upper_bounds), ...)` ✅
- L2040-2059：`porosity` 被 `ddca_dual_temp(...)` 消费（L2053 传入），确认 L2039 的 porosity 流入后续反演
- L1106 所在函数返回 `float(result.x[0]), float(result.x[1])`（L1114），是反演核心路径

**修复**：沿用 v2 Sprint 1.4 的三处修复（新增常量 + L2039 校验 + L1106 前置校验）。

**验证**：沿用 v2 Sprint 1.4 验证脚本。

**Commit**：`fix(algorithms): validate porosity before least_squares to prevent ValueError on bad bulk_density`

---

### 1.5 ndvi.py Savitzky-Golay 有效性检查

**文件**：`Code/algorithms/providers/Python/algorithms/ndvi.py`

**核验结论**：✅ v2 描述正确
- L7：`_SG_DEFAULT_POLYORDER = 6`
- L8：`_SG_DEFAULT_WINDOW_LENGTH = 9`
- L11：`_SG_MIN_VALID_POINTS = 4`
- L76-86：`valid_mask = ~np.isnan(data)` → L77 `if valid_mask.sum() <= _SG_MIN_VALID_POINTS: return NaN` → L80-84 `interpolated_8day = _linear_interp_with_nan(...)` → L85 `savgol_filter(interpolated_8day, sg_window_length, ...)` → **`interpolated_8day` 长度 = `len(sg_days)`，当 `sg_days.size < 9` 时崩溃**
- L4：`from typing import Any, Iterable` ✅

**修复**：沿用 v2 Sprint 1.5 修复 1（savgol_filter 前增加 `if sg_days.size < sg_window_length:` 降级为线性插值）。修复 2（降 polyorder 6→3）留到 Sprint 3。

**验证**：沿用 v2 Sprint 1.5 验证脚本（短时间窗不崩溃）。

**Commit**：`fix(algorithms): fall back to linear interp when sg_days shorter than savgol window_length`

---

### 1.6 physics.py zkb 负值开方

**文件**：`Code/algorithms/providers/Python/algorithms/physics.py`

**核验结论**：✅ v2 描述正确
- L4：`from typing import Any` ✅
- L6：`import math`（顶部，文件后续用 `math.sqrt`）✅
- L53-58：`_FREQ_GHZ_MIN`、`_FREQ_GHZ_MAX`、`_CLAY_FRACTION_MIN`、`_CLAY_FRACTION_MAX` 常量块
- L351-355：`epwbx`/`epwby`/`epwux`/`epwuy` 在 L357-360 上方计算
- L357-360：4 行 `math.sqrt((math.sqrt(epwbx**2 + epwby**2) ± epwbx) / 2)` ✅
- 文件无现有 `_safe_sqrt` 函数

**修复**：沿用 v2 Sprint 1.6（新增 `_safe_sqrt` 函数 + 替换 L357-360 四行，提取 `mag_b`/`mag_u` 复用）。

**验证**：沿用 v2 Sprint 1.6 验证脚本（`_safe_sqrt(-1e-20) == 0.0` 等）。

**Commit**：`fix(algorithms): clamp negative sqrt args in mironov_dielectric to prevent ValueError`

---

### 1.7 weatherengine/service.py Any 未导入

**文件**：`Code/backend/app/weatherengine/service.py`

**核验结论**：✅ v2 描述正确（Agent 2 报告"已有"系幻觉）
- L1：`from __future__ import annotations`
- L2：（空行）
- L3：`from datetime import datetime, timezone`
- L4-9：`import importlib` / `import logging` / `import math` / `from pathlib import Path` / `from urllib.error import HTTPError, URLError` / `from uuid import uuid4`
- L10：（空行）
- L11-26：`from app.core.config import settings` 等 app 内部 import
- L27-28：`logger = logging.getLogger(__name__)` + 空行
- **L1-28 全程无 `from typing import Any`** ✅
- 文件后续 10+ 处 `dict[str, Any]` / `Any` 注解（L106, L114, L116, L224, L250 等）

**修复**：沿用 v2 Sprint 1.7（L3 后加 `from typing import Any`）。

**验证**：
```bash
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\backend"
python -c "from app.weatherengine.service import WeatherEngineService; print('OK')"
```

**Commit**：`fix(backend): import Any in weatherengine service for type annotations`

---

## Sprint 2：高优先级性能与架构

### 2.1 ndvi.py 向量化

**文件**：`Code/algorithms/providers/Python/algorithms/ndvi.py` L133-142

**核验**：L133-142 逐像素循环调用 `vi_sg_interpolate`，未利用 `savgol_filter` 沿 axis 处理 N 维数组能力。

**修复策略**：沿用 v2 Sprint 2.1。**前置依赖**：先完成 Sprint 1.5。对 `valid_counts >= sg_window_length` 且 `sg_days.size >= sg_window_length` 的像素批量 `savgol_filter(data_2d, window, polyorder, axis=1, mode="interp")`；不足者降级逐像素调用。

**验证**：构造 100×100×46 测试数据，对比向量化前后耗时（预期 10-100× 提升）与数值差异（应 < 1e-10）。

**Commit**：`perf(algorithms): vectorize ndvi SG filter along pixel axis for ~50x speedup`

---

### 2.2 block_inversion.py 循环优化

**文件**：`Code/algorithms/providers/Python/algorithms/block_inversion.py` L260-310

**核验**：三层循环（chunk → day → pixel），每天重建 `valid_mask` 和 `context_cache`。

**修复策略**：沿用 v2 Sprint 2.2。**Sprint 2 仅做 `valid_mask` 外提**（前提：tbv/tbh/ts/tau 静态参数不随 day 变化，需在实施时核验）；**不做多进程**（Celery prefork 死锁风险，留到 Sprint 3 验证 worker 启动模式后再评估）。

**Commit**：`perf(algorithms): hoist valid_mask out of day loop in block_inversion`

---

### 2.3 WeatherEngineService 拆分（基础设施）

**文件**：`Code/backend/app/weatherengine/service.py`（1866 行）

**核验**：`_build_map_layer_outputs` L532-730，含 7 处 `if layer_id ==` 分支（L557, L585, L615, L644, L662, L680, L699-723）。

**修复策略**：沿用 v2 Sprint 2.3。**Sprint 2 仅做基础设施**：新建 `layer_outputs/` 目录 + `base.py`（`LayerOutputStrategy` ABC）+ `registry.py`（注册表）+ `default.py`（透传原逻辑）。`service.py` 改为先查 registry，未命中走原 if/elif。**不迁移具体分支**（留到 Sprint 3 增量做）。

**Commit**：`refactor(weatherengine): introduce layer_outputs strategy registry (no behavior change)`

---

### 2.4 SQLite 连接池 + weather_providers WAL 修复

**文件**：四个 repository 的 `_connect` 方法

**核验**：
- `api_keys_repository.py` L80-83：`sqlite3.connect()` + WAL + synchronous=NORMAL ✅
- `gee_credentials_repository.py` L59-62：同上 ✅
- `weather_providers_repository.py` L72-75：`sqlite3.connect()` + `row_factory`，**无 WAL、无 synchronous=NORMAL** ❌（新发现）
- `workflow_repository.py` L283-295：`sqlite3.connect()` + WAL + synchronous=NORMAL ✅

**修复策略**（分两步）：

**步骤 A（Sprint 2.4a，先行）**：补齐 `weather_providers_repository._connect` 的 WAL 配置，与其他 3 个 repo 对齐。

`old_string`（L72-75）：
```python
    def _connect(self) -> sqlite3.Connection:
        conn = sqlite3.connect(str(self.db_path))
        conn.row_factory = sqlite3.Row
        return conn
```

`new_string`：
```python
    def _connect(self) -> sqlite3.Connection:
        conn = sqlite3.connect(str(self.db_path))
        conn.row_factory = sqlite3.Row
        conn.execute("PRAGMA journal_mode=WAL")
        conn.execute("PRAGMA synchronous=NORMAL")
        return conn
```

**Commit 2.4a**：`fix(backend): enable WAL in weather_providers_repository to match other SQLite repos`

**步骤 B（Sprint 2.4b，后行）**：沿用 v2 Sprint 2.4，新建 `_sqlite_pool.py`（`ThreadLocalSQLitePool`），**仅迁移 `api_keys_repository` 试点**。跑通后再迁移其他 3 个。

**关键约束**：
- 必须用 `threading.local()`（SQLite 连接不能跨线程）
- WAL + synchronous=NORMAL（与现有 repo 一致）
- `with self._connect() as conn` 语义保持不变
- 必须提供 `close_thread_connection()` 用于测试清理
- `weather_providers_repository._connect` 返回 `sqlite3.Connection`（非 context manager），与其他 3 个用 `@contextmanager` 不同 — 迁移时需统一为 context manager 接口

**Commit 2.4b**：`perf(backend): introduce ThreadLocalSQLitePool and migrate api_keys_repository`

---

### 2.5 remote_storage_credentials_repository 原子性

**文件**：`Code/backend/app/services/remote_storage_credentials_repository.py`

**核验结论**：✅ v2 描述正确（Agent 3 报告"在 with 内"系幻觉）
- L174-187：`_archive_secrets` 调用（L181-187）在 L191 `with self._connect() as conn:` 块**之前**
- L375-421：`_archive_secrets` 内部 L387 自己 `with self._connect() as conn:`，L420 `conn.commit()` — 独立事务
- L191-232：主 upsert 的 `with` 块，L232 `conn.commit()`
- **结论**：若 `_archive_secrets` commit 后、主 upsert commit 前崩溃，归档已写但主记录未更新 → 数据不一致

**修复**：沿用 v2 Sprint 2.5。将 L174-187 archive 块移入 L191 `with` 块内，`_archive_secrets` 签名改为接收 `conn` 参数，删除其内部 `with self._connect()` 和 `conn.commit()`。

**修复 1**：`upsert` 方法调整（将 archive 移入 with 块）

`old_string`（L174-192 关键部分）：
```python
        # Archive previous secrets when plaintext changes
        if existing is not None:
            old_secret = existing.get("secret") or ""
            old_key = existing.get("private_key_pem") or ""
            if (secret is not None and secret != old_secret) or (
                private_key_pem is not None and (private_key_pem or "") != old_key
            ):
                self._archive_secrets(
                    profile_id=profile_id,
                    secret=old_secret,
                    private_key_pem=old_key or None,
                    superseded_at=now,
                    source="user",
                )

        sec_ct, sec_iv = self._encrypt(secret_val)
        key_ct, key_iv = self._encrypt(key_val) if key_val else ("", "")
        with self._connect() as conn:
            conn.execute(
```

`new_string`：
```python
        sec_ct, sec_iv = self._encrypt(secret_val)
        key_ct, key_iv = self._encrypt(key_val) if key_val else ("", "")
        with self._connect() as conn:
            # Archive previous secrets in the SAME transaction as the main upsert
            if existing is not None:
                old_secret = existing.get("secret") or ""
                old_key = existing.get("private_key_pem") or ""
                if (secret is not None and secret != old_secret) or (
                    private_key_pem is not None and (private_key_pem or "") != old_key
                ):
                    self._archive_secrets(
                        conn,
                        profile_id=profile_id,
                        secret=old_secret,
                        private_key_pem=old_key or None,
                        superseded_at=now,
                        source="user",
                    )
            conn.execute(
```

**修复 2**：`_archive_secrets` 签名改为接收 `conn`，删除内部 `with` 和 `commit`

`old_string`（L375-420 关键部分）：
```python
    def _archive_secrets(
        self,
        *,
        profile_id: str,
        secret: str,
        private_key_pem: str | None,
        superseded_at: str,
        source: str,
        label: str | None = None,
    ) -> None:
        sec_ct, sec_iv = self._encrypt(secret or "")
        key_ct, key_iv = self._encrypt(private_key_pem) if private_key_pem else ("", "")
        with self._connect() as conn:
            conn.execute(
```

`new_string`：
```python
    def _archive_secrets(
        self,
        conn,
        *,
        profile_id: str,
        secret: str,
        private_key_pem: str | None,
        superseded_at: str,
        source: str,
        label: str | None = None,
    ) -> None:
        """在调用方事务内归档旧密钥。不自行 commit，由外层 with 统一提交。"""
        sec_ct, sec_iv = self._encrypt(secret or "")
        key_ct, key_iv = self._encrypt(private_key_pem) if private_key_pem else ("", "")
        conn.execute(
```

同时删除 L420 的 `conn.commit()`（让外层 `with` 块的 commit 统一处理）。

**验证**：单测模拟 `_archive_secrets` 后抛异常，确认主记录和归档都不写入（原子性）。需检查是否还有其他调用 `_archive_secrets` 的地方（grep 确认仅 `upsert` 一处调用）。

**Commit**：`fix(backend): make remote_storage archive_secrets atomic with main upsert`

---

### 2.6 config_service.test_api_key async 化

**文件**：`Code/backend/app/services/config_service.py` L320-382

**核验**：✅ v2 描述正确
- L320：`async def test_api_key`
- L331-334：tianditu 分支用 `urllib.request.urlopen`（同步阻塞）
- L344-349：baidu 分支用 `urllib.request.urlopen`
- 文件顶部无 `httpx` import
- gaode/backend_auth 分支无网络请求

**前置检查**：确认 `httpx` 是否在 `requirements.txt`。若不在，需添加 `httpx>=0.27.0`（FastAPI 通常已传递依赖，但显式声明更稳妥）。

**修复**：沿用 v2 Sprint 2.6。将 tianditu 和 baidu 分支的 `urllib.request.urlopen` 改为 `httpx.AsyncClient`。`backend_auth` 与 `else` 分支无网络请求，不改。

**验证**：mock `httpx.AsyncClient`，确认 `test_api_key` 正确处理 200/401/超时；调用期间确认其他 async 请求不被阻塞。

**Commit**：`fix(backend): use httpx.AsyncClient in test_api_key to avoid blocking event loop`

---

### 2.7 前端 fetch 抽取 + 死调试代码清理

**文件**：
- `Code/frontend/src/services/runtime-api.ts`（288 行，含 L18-21 `127.0.0.1:7777` 死调试代码，多处裸 `fetch`）
- `Code/frontend/src/services/workflow-definition-api.ts`（206 行，多处裸 `fetch`）
- `Code/frontend/src/services/settings-api.ts`（550 行，已有 `settingsFetch` 封装 L209-252）

**核验修正**：
- `settings-api.ts` **已有** `settingsFetch` 统一封装（L209-252），处理 `resolveApiUrl` + `withWriteAuthHeaders`，无需改造
- `runtime-api.ts` 有 `getApiBaseUrl`（L57-66）和 `withWriteAuthHeaders`（L71-77）工具函数，但各 fetch 调用点未统一封装
- `runtime-api.ts` L18-21 含 `127.0.0.1:7777` 死调试代码，必须删除
- `workflow-definition-api.ts` 同样多处裸 fetch

**修复策略**（分两步）：

**步骤 A（Sprint 2.7a，先行，低风险）**：删除 `runtime-api.ts` L18-21 的 `127.0.0.1:7777` 死调试代码。实施前需读 L18-21 上下文确认删除范围（是否整段函数或仅变量赋值）。

**Commit 2.7a**：`fix(frontend): remove dead 127.0.0.1:7777 debug code in runtime-api`

**步骤 B（Sprint 2.7b，后行）**：在 `runtime-api.ts` 中参照 `settings-api.ts` 的 `settingsFetch` 模式，抽取 `runtimeFetch` 封装（统一 base URL 解析 + auth header 注入 + 错误处理）。`workflow-definition-api.ts` 同理抽取 `workflowFetch`。

**约束**：
- 不改变任何函数的对外签名与返回类型
- 逐个 fetch 调用点替换，每替换一批跑一次 `npm run build` 确认无回归
- 实施前先 `grep -n "console.log" runtime-api.ts workflow-definition-api.ts` 清理调试输出

**Commit 2.7b**：`refactor(frontend): extract shared fetch helpers in runtime-api and workflow-definition-api`

---

### 2.8 omega.py pixel_chunk_size 清理

**文件**：`Code/algorithms/providers/Python/algorithms/omega.py`

**核验**：`OmegaConfig` dataclass 在 L30-37+ 定义，docstring L35 提到 `pixel_chunk_size 单位像素数`。需实施时 grep `pixel_chunk_size` 确认所有使用点，若确实无用则删除字段。

**修复**：沿用 v2 Sprint 2.8（方案 A：删除配置项）。实施前先 `grep -n "pixel_chunk_size" omega.py` 确认无运行时引用。

**Commit**：`refactor(algorithms): remove unused pixel_chunk_size from OmegaConfig`

---

## Sprint 3：中优先级代码质量

### 3.1 WeatherEngineService 分支迁移

**前置**：Sprint 2.3 完成后，将 `_build_map_layer_outputs` 的 7 个 `if layer_id ==` 分支（L557, L585, L615, L644, L662, L680, L699-723）逐个迁移为 `layer_outputs/` 下的策略类。每个分支一个独立 commit，迁移后跑端到端测试确认无回归。

**Commit**（每个分支）：`refactor(weatherengine): migrate <layer_name> layer output to strategy class`

---

### 3.2 ndvi.py polyorder 降级

**文件**：`Code/algorithms/providers/Python/algorithms/ndvi.py` L7

**修复**：`_SG_DEFAULT_POLYORDER = 6` → `3`。window=9, polyorder=6 等价于 6 阶多项式拟合 9 点（自由度仅 2），过拟合风险高。

**约束**：降 polyorder 会导致历史反演结果略有差异。若需严格复现历史结果可保留 6，但需在 docstring 标注风险。建议与项目 owner 确认（见 [假设与决策](#假设与决策) D1）。

**Commit**：`refactor(algorithms): lower ndvi SG polyorder from 6 to 3 to reduce overfitting`

---

### 3.3 weather_providers_repository._decrypt 降级一致性

**文件**：`Code/backend/app/services/weather_providers_repository.py` L121-132

**核验**：`_decrypt` 在无 key 时（L124-125）静默返回 `ciphertext_b64`。与 Sprint 1.3 修复后的 `_encrypt` fail-fast 策略不对称。

**修复**：`_decrypt` 无 key 时应记录 warning 并返回空字符串（而非密文），避免下游误用无法解密的密文。**不改为 raise**（解密失败不应阻塞读取流程，与加密 fail-fast 语义不同）。

**Commit**：`fix(backend): log warning when decrypting without key in weather_providers_repository`

---

### 3.4 SQLite 连接池迁移剩余 3 个 repo

**前置**：Sprint 2.4b 试点 `api_keys_repository` 跑通后，迁移 `gee_credentials_repository`、`weather_providers_repository`、`workflow_repository`。每个独立 commit。

**约束**：`weather_providers_repository._connect` 当前返回 `sqlite3.Connection`（非 context manager），迁移时需统一为 `@contextmanager` 接口，并同步修改所有 `with self._connect() as conn:` 调用点（当前直接 `conn = self._connect()` 的地方需调整为 `with`）。

**Commit**（每个 repo）：`perf(backend): migrate <repo_name> to ThreadLocalSQLitePool`

---

### 3.5 broad `except Exception` 清理

**核验**：探索代理发现多处 `except Exception`：
- `Code/algorithms/providers/Python/runner/dispatch.py`
- `Code/backend/app/weatherengine/workflow_manager.py`
- `Code/algorithms/providers/Python/data_access/universal_reader.py`

**修复策略**：逐文件审查，将网络/API 失败（应 graceful degradation）与编程 bug（`AttributeError`/`NameError`/`TypeError`，应 propagate）分离。**不在 Sprint 3 批量改**，而是每个文件独立审查 + commit，避免引入回归。

**约束**：遵循 memory 记录的项目约定："网络/API 失败和数据格式错误应触发 graceful degradation，编程 bug 应 propagate"。

**Commit**（每个文件）：`refactor(<area>): narrow broad except in <file> to separate degrade vs propagate`

---

### 3.6 block_inversion 多进程评估

**前置**：Sprint 2.2 完成 `valid_mask` 外提后，评估是否值得用 `ProcessPoolExecutor` 并行化 chunk 维度。

**风险**：Celery worker 默认 prefork 模式，子进程内再开 `ProcessPoolExecutor` 可能死锁。

**修复策略**：
1. 先确认 Celery worker 启动参数（`launch.py` 中 worker 启动命令）是否用 `--pool=prefork`/`--pool=solo`/`--pool=threads`
2. 若 prefork：改用 `ThreadPoolExecutor`（受 GIL 限制但无死锁风险），或 worker 启动改 `--pool=solo`（单进程，但失去并发）
3. 若 threads/solo：可安全用 `ProcessPoolExecutor`

**决策点**：见 [假设与决策](#假设与决策) D2。Sprint 3 仅做评估 + 基准测试，不改代码。

---

## Sprint 4：项目级基础设施

### 4.1 添加 mypy 严格配置

**文件**：新建 `Code/backend/mypy.ini` 或 `Code/backend/pyproject.toml [tool.mypy]`

**配置内容**：
```ini
[mypy]
python_version = 3.11
warn_return_any = True
warn_unused_configs = True
disallow_untyped_defs = False  # 逐步收紧，先 False
ignore_missing_imports = True  # scipy/numpy 等第三方无 stub

[mypy-app.*]
disallow_untyped_defs = True

[mypy-algorithms.*]
disallow_untyped_defs = True
```

**Commit**：`chore(backend): add mypy strict config for app and algorithms modules`

---

### 4.2 添加 pre-commit 钩子

**文件**：新建 `.pre-commit-config.yaml`

**配置内容**：
```yaml
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.5.0
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.10.0
    hooks:
      - id: mypy
        files: ^(Code/backend/app|Code/algorithms/providers/Python/algorithms)/
        additional_dependencies: [types-requests, numpy, scipy]
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
```

**Commit**：`chore: add pre-commit config with ruff, mypy, and standard hooks`

---

### 4.3 前端 lint 配置

**文件**：`Code/frontend/package.json`

**修复**：添加 eslint + prettier dev 依赖和脚本：
```json
{
  "scripts": {
    "lint": "eslint src --ext .ts,.vue",
    "format": "prettier --write src"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "prettier": "^3.0.0"
  }
}
```

**Commit**：`chore(frontend): add eslint and prettier config`

---

### 4.4 requirements.txt 显式声明 httpx

**文件**：`Code/backend/requirements.txt`（实施前确认路径）

**修复**：添加 `httpx>=0.27.0`（Sprint 2.6 的前置依赖）。先 grep 确认是否已有传递依赖。

**Commit**：`chore(backend): explicitly declare httpx dependency`

---

## 验证清单

### Sprint 1 完成后必跑

```bash
# 后端导入与语法检查
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\backend"
python -c "
from app.api.routers.layer_router import router
print('1. layer_router OK')
from app.services.weather_providers_repository import WeatherProvidersRepository
print('2. WeatherProvidersRepository OK')
from app.weatherengine.service import WeatherEngineService
print('3. WeatherEngineService OK')
from app.services.effective_config import secrets_encryption_required, assert_encryption_policy
print('4. effective_config OK')
"

# 算法层导入与单点测试
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\algorithms\providers\Python"
python -c "
from algorithms.inversion import retrieve_dynamic_h_pixel, ddca_retrieve_pixel
import inspect
assert 'model_context' in inspect.signature(retrieve_dynamic_h_pixel).parameters
assert 'model_context' in inspect.signature(ddca_retrieve_pixel).parameters
print('5. inversion signatures OK')
from algorithms.physics import _safe_sqrt
assert _safe_sqrt(-1e-20) == 0.0
print('6. _safe_sqrt OK')
from algorithms.omega import _SOIL_MOISTURE_LOWER_BOUND, _TAU_UPPER_BOUND, _POROSITY_MIN_REASONABLE
print('7. omega constants OK')
import numpy as np
from datetime import datetime, timedelta
from algorithms.ndvi import vi_sg_interpolate, build_datetime_sequence, to_day_numbers
obs = [datetime(2024,1,1)+timedelta(days=i*8) for i in range(5)]
data = np.array([0.3,0.4,0.5,0.45,0.55])
sg = build_datetime_sequence(datetime(2024,1,1), datetime(2024,1,5), 8)
out = build_datetime_sequence(datetime(2024,1,1), datetime(2024,1,5), 1)
r = vi_sg_interpolate(data, to_day_numbers(obs), to_day_numbers(sg), to_day_numbers(out))
assert r.shape == to_day_numbers(out).shape
print('8. ndvi short-window fallback OK')
"
```

### Sprint 1 完成后端到端验证

1. 启动后端：`python launch.py start fastapi`
2. 验证 layer_router：`curl "http://localhost:8000/api/layers/geo/transform?lng=113&lat=23&source=EPSG:9999&target=EPSG:4326"` → 应返回 400（而非 500）
3. 在 Settings → Weather Providers 添加 Provider，检查 DB 中 `config_encrypted` 字段非明文（生产）或正确降级（开发）
4. 跑一次天气图层生成，确认无 TypeError from inversion
5. 跑一次 NDVI 反演（短时间窗），确认无 savgol_filter ValueError

### Sprint 2 完成后

1. NDVI 100×100×46 测试数据反演耗时对比（预期 10×+ 提升）
2. SQLite 多线程并发测试：`ThreadPoolExecutor` 8 线程并发调用 `count_active_runs` 1000 次，无 ProgrammingError
3. `test_api_key` 期间并发其他 async 请求，确认不阻塞
4. `remote_storage_credentials.upsert` 中途异常，确认主记录和归档原子性
5. 前端 `npm run build` 无回归

### 每个 commit 后

```bash
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\backend"
python -m py_compile app/api/routers/layer_router.py app/services/weather_providers_repository.py app/weatherengine/service.py
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\algorithms\providers\Python"
python -m py_compile algorithms/inversion.py algorithms/omega.py algorithms/ndvi.py algorithms/physics.py
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system\Code\frontend"
npm run build
```

### commit 前检查

```bash
cd "d:\temp_desktop\Proj\Comprehensive Geographic Data Analysis system"
git status   # 确认无 .data/ 运行时文件被误暂存
git branch --show-current   # 确认在 dev 分支
```

---

## 假设与决策

### A. 已确认的事实（无需用户决策）

- **A1**：当前分支 `dev`，所有修复 commit 到 `dev`，不直接动 `main`
- **A2**：`assert_encryption_policy()` 已在启动时调用（`main.py` lifespan → `hydrate_effective_config` → `assert_encryption_policy`），v2 补充项为假阳性
- **A3**：`api_keys_repository` 和 `gee_credentials_repository` 已正确实现加密 fail-fast，**不动**
- **A4**：Sprint 1.1-1.7 的 v2 old_string/new_string 经直接核验全部准确，可直接应用
- **A5**：Sprint 2.5 跨事务问题真实存在（`_archive_secrets` 在主 `with` 块外，且有独立 `with`+`commit`）
- **A6**：`weather_providers_repository._connect` 确实缺少 WAL（新发现，Sprint 2.4a 补齐）

### D. 需用户决策的点

- **D1（Sprint 3.2）**：NDVI SG polyorder 是否从 6 降为 3？
  - 降为 3：减少过拟合，但历史反演结果不可严格复现
  - 保留 6：复现历史，但过拟合风险
  - **默认建议**：降为 3（科学合理性优先），若项目有论文复现需求则保留 6 并在 docstring 标注

- **D2（Sprint 3.6）**：block_inversion 多进程方案？
  - ThreadPoolExecutor：无死锁风险，但受 GIL 限制（numpy 操作可释放 GIL，仍有收益）
  - ProcessPoolExecutor：性能最佳，但需改 Celery worker 启动模式为 `--pool=solo`（失去 worker 并发）
  - **默认建议**：先用 ThreadPoolExecutor 基准测试，若收益不足再评估 ProcessPoolExecutor + solo

- **D3（Sprint 2.7b）**：前端 fetch 抽取范围？
  - 仅 runtime-api.ts：最小改动
  - runtime-api.ts + workflow-definition-api.ts：统一两个文件
  - **默认建议**：两个都抽取，settings-api.ts 已有封装不动

### 执行注意事项

1. **每项修复独立 commit**：commit message 用 `fix:` / `refactor:` / `perf:` / `chore:` 前缀，遵循 `.trae/rules/git-commit-message.md`
2. **不动 gee_credentials_repository 和 api_keys_repository**：已正确实现加密策略
3. **不引入 `_secrets_crypto.py` 新模块**：复用 `effective_config.secrets_encryption_required()`
4. **Sprint 2.4 分两步**：先补 WAL（2.4a），再做连接池试点（2.4b）
5. **Sprint 2.7 分两步**：先删死调试代码（2.7a），再抽取 fetch 封装（2.7b）
6. **Sprint 3.5 不批量改**：逐文件审查 broad except，每文件独立 commit
7. **commit 前必查**：`git status` 确认无 `.data/` 运行时文件误暂存；`git branch --show-current` 确认在 `dev`
8. **本方案取代 v2**：v2 中正确的 Sprint 1.1-1.7 代码片段直接沿用，不在此重复贴出
