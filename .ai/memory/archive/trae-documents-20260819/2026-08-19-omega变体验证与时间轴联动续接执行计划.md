# ω 变体验证与时间轴联动续接执行计划（2026-08-19 晚）

> 承接 `.trae/documents/2026-08-19-omega图层组在线反演续接执行计划.md`（下称「前计划」）。
> 用户已重启全部进程（21:19 新栈，FastAPI :8000 / Gateway :5175 均 200，含全部未提交代码）。
> 路由：staff-engineer-mode / `architecture-decisions`（图层契约 × 工作流编排 × 时间轴 × 数据管线多面决策）。

---

## 一、用户问题直接回答（本轮探索核验，承接前计划结论）

| 用户问题 | 核验结论 |
|---|---|
| omega_sf_fenkuai_* 图层移除了吗？ | **已移除**。前端 `layer-naming.ts`/`layer-display-names.ts`/`catalog.ts` 零命中；descriptor 中残留的 `omega_sf_fenkuai` 仅为 `module_name`/`dataset_key`（算法模块本名）与种子 id（工作流名），均非图层名，属有意兼容映射 |
| 风云/SMAP 反演各 2 个（动态/平均 ω）？ | **已符合**。`method-{fy,smap}-omega-doy-{dynamic,avg}` 4 个 method 图层（descriptor 实测 workflow_name 分别指向 `omega_sf_fenkuai_{fy,smap}_online` / `omega_avg_daily_{fy,smap}_online`，均含 `workflow_variants: {online, local}`，默认 online）+ 后端合并组 `fy-omega-inversion` / `smap-omega-inversion`（各 2 成员，is_merged_group=true） |
| 风云数据拼接？ | **已实现并入库**：FY3B/FY3D 拼接在 Python `ingest/fy_preprocess.py`（MWRI HDF geoloc 校正 → gdalbuildvrt+warp → EPSG:6933）；原始 Matlab 代码已提交（`4ca6fdc`）；**FY3F 为本轮阶段 P5 接入** |
| 一组三个 + 组标题进度 + 无占位？ | 机制在：`ActiveRunLayerGroup` 四态（computing/ready/failed/cancelled）+ 组标题进度条（`LayerSidebarActive.vue:100-118`）；占位成员按 `extra.outputs=["SM","VOD","OMEGA"]` 创建，终态 `cleanupUnproducedRunLayers` 清理。静态 tag 三方对齐已验证（前计划 §2.1），**需实跑确认**（P2） |
| 分析工具/图层加载正常？ | 重启前 13/13 冒烟全绿（`Test/reports/smoke-analysis-2026-08-19b.md`）；重启后需复验基线（P0） |
| 计划可行？ | 可行。X2 变体路由修复（resolver 翻译 + bridge 占位符展开 + 测试）已落盘工作区；阶段 2 代码全链完成；剩余 = 验证 + 阶段 3/4/5/6 |

## 二、当前状态分析（本轮探索事实）

### 2.1 工作区未提交改动（全部在飞）

- **后端**：`workflow_request_resolver.py`（X2 变体路由：`_resolve_variant_workflow_entry` L243 + 翻译注入 L1033）、`python_provider_request_builder.py`（种子编译后 `{YYYYMMDD}`/`{DATA_ROOT}` 展开 L89-103）、`layer_descriptors.json`（4 层变体配置）、`api_contracts.py`（WorkflowVariantDef 契约）、`config.py`/`config_service.py`
- **前端**：`workflow-runner.ts`（变体解析 `resolveVariantAlgorithmRequest` + 偏好存储）、`InfoPanel.vue`（「反演来源」分段控件 L257-273）、`useWorkflowState.ts`（变体视图/切换）、`workflow-run-domain.ts`/`selectors.ts`/`index.ts`、openapi.json、类型
- **测试**：`test_workflow_request_resolver.py`（2 个更新至新语义 + 3 个新变体测试，**未跑**）、`test_workflow_variant_readiness.py`（新文件）、`test_omega_outputs_alignment.py`（新文件，5 用例）
- **杂项**：`Tools/smoke_system_workflows.py`（AD11 通用 override 修复）、`Test/debug/` ~20 个调试脚本（按前计划 D16 保留入库）、LoginView.vue（8-19 中午布局改动）

### 2.2 关键缺口（本轮新发现/确认）

1. **前端 dist 过期**：dist 构建于 13:31，源码最后修改 14:43（`workflow-run-domain.ts`）——**变体切换控件不在当前 Gateway 服务的 dist 中**，UI 验证前必须 `npm run build` 重建。
2. **X2 修复零验证**：resolver/bridge 改动与 3 个新测试均未执行；上次 ω 实跑走裸模块路径（单模块 35min/chunk × 32）正是此缺口，修复后需 API 级验证 payload 走 online 种子图。
3. **组状态芯片无配色**：`LayerSidebar.styles.css:1026` `.group-status-chip` 四态共用 `var(--text-secondary)`，用户「颜色变化」要求未落地。
4. **ω 层无 online_temporal**：4 个 method 图层 `online_temporal` 均未配置（ndvi/ref-fy-tb 已有先例），时间轴联动无从触发。
5. **上次 ω run 终态未知**：run-65bc52ff2c5a 在重启时被杀，其状态需检查并清理（若挂在 running 需处理）。

### 2.3 服务与数据基线

- 栈：21:19 全新启动（1 FastAPI + 7 worker + beat + 1 multiprocessing），新代码已加载
- 种子：`Code/backend/workflow_seeds/system/` 50 个，omega 全家（online/single/dual × fy/smap × sf_fenkuai/avg_daily + gldas_online + block_smap_single）13 个齐全
- `products/` 已清理；生产库 63 悬空 token 维持不动（前计划 D16）

---

## 三、实施阶段

### P0 重启后基线复验 + dist 重建（~1h）

1. **dist 重建**：`cd Code/frontend && npm run build`（prebuild 含 gen:catalog）；重建后 Gateway :5175 即服务新 dist（静态目录，无需重启容器）。
2. **ω run 残留清理**：查 run-65bc52ff2c5a / run-f6b15b8181ce 终态（经 `Test/debug/check_recent_runs.py` 或状态库）；若挂 running 标记为 cancelled（重启即中断，防 UI 假进度）。
3. **分析工具复验**：`Env\Python312\python.exe Tools/smoke_system_workflows.py --report Test/reports/smoke-analysis-2026-08-19c.md` 后台全量（13 条，重启后基线）；期间并行推进 P1。
4. **UI 图层加载抽验**（浏览器自动化，Gateway :5175）：目录树加载、ω 合并组两成员可见、分析工具 Tab 可用。

### P1 X2 变体路由修复验证（~1h）

1. **单测**：`CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= CODEBUDDY_SAFE_DELETE_SANDBOX= Env/Python312/python.exe -m pytest Test/backend/test_workflow_request_resolver.py Test/backend/test_workflow_variant_readiness.py Test/backend/test_python_provider_bridge_service.py Test/backend/test_omega_outputs_alignment.py -q`（WorkBuddy shim 前缀 + `--basetemp="Test/.pytest-be"`）。
2. **API 级实跑验证**（登录态经 `Test/debug/api_admin.py` 模式）：提交 `layer_id=method-fy-omega-doy-dynamic`（layer_id-only，无显式变体）→ 断言：
   - run 的 request_json/payload 中 `algorithm_request.workflow_name == "omega_sf_fenkuai_fy_online"` 且**无** `module_name`；
   - 事件流出现 `fy_download` → `fy_daily` → `omega_sf_fenkuai` 多节点链（区别于裸模块的 chunk 事件直出）；
   - 若日期占位符路径触发，`{YYYYMMDD}` 已按 payload 时间展开为具体日期。
3. **失败处理**：任一断言失败 → 按 resolver→bridge→provider 链逐层定位（`Test/debug/check_compile_api.py` 复用），修复后重验。

### P2 阶段 1：ω 组「一组三个」实跑验证（~1.5h）

1. light-smoke 本地链：`Env\Python312\python.exe Tools/smoke_system_workflows.py --only omega_sf_fenkuai_smap_single`（不依赖凭据）。
2. **组结构验证**：提交创建 3 占位成员（SM/VOD/OMEGA）→ 终态绑定 3 个 importedRaster → `cleanupUnproducedRunLayers` 后组内恰 3 个真实图层、无占位残留（/layers 查询 + UI 截图）。
3. **瓦片渲染 + 配色验证**（用户「一个瓦片一个瓦片 + 可改配色」）：EPSG:6933 9km GeoTIFF overlay 渲染抽验（勾选 SM 成员 → 地图显示）；`InfoPanelStyleTab` 切换 palette/vmin/vmax → 渲染即时变化并回写 URL（既有 `setOverlayStyle` 链路）。
4. 若占位残留再现 → 按事件流水现场诊断（静态链已排除，聚焦 result_refs 生成顺序与清理时序）。

### P3 阶段 3：online_temporal + 时间轴联动 + 组颜色（~3h）

1. **descriptor**：`layer_descriptors.json` 4 个 method 图层加 `online_temporal`（参照 `ref-fy-tb` L773-840 与 ndvi 模式）：
   - dynamic/avg 均 `enabled: true, native_step: "1d", max_batch: 8, prefetch_depth: 1, queue_tag: "temporal-fetch", priority: "low"`；
   - coverage 保守：SMAP ≥2015-04、FY3D ≥2017-01（与种子日期模板对齐），`coverage_end: null`。
2. **orchestrator 零代码接入验证**：`triggerOnlineFetch` 读运行时 descriptor → `buildWorkflowPayloadForCatalog` 默认即 online 种子（P1 已证变体路由生效）——链路天然成立；核验 `MAX_CONCURRENT_FETCHES`、去重键 `${catalogId}:${timeKey}`、失败 60s 冷却。
3. **组状态芯片四态配色**（用户「工作流颜色变化」）：`LayerSidebarActive.vue:103` 加状态 modifier class；`LayerSidebar.styles.css:1026` 四态配色（computing 蓝 / ready 绿 / failed 红 / cancelled 灰，取主题 token），纯样式不动状态机。
4. **8 天块对齐（AD10）**：核对 ω 产物 timeKey 与 `workflow-timekey-seek.ts:20-63`（`{8digits}_{8digits}` + 8-day step）一致；不一致则修 `raster_timeseries.upsert_block_dir_timeseries` 的 native_step 投影。合并组保持 static。
5. **重放幂等（D12）**：失败（如错凭据）→ 60s 后拖动同段再触发 → 无重复运行组/重复图层成员；有重复则在 orchestrator 失败分支补去重。
6. **验证**：`npm run test -- online-temporal workflow-timekey-seek workflow-runner run-layers` + 手动时间轴联动（拖动 fetchable 段 → 组 computing 蓝 → ready 绿 → 新帧渲染；失败红 → 冷却后可再触发）+ `npm run check:catalog`（online_temporal 非目录投影字段，应无漂移）。

### P4 阶段 4：残留修复 R1/R2/R3/R5/R4（~5h，可与 P3 后期穿插）

1. **R1 esa**：`GET /workflow-runs/{run_id}/events` 定位（CDSE 401 → blocked:creds 预检；UUID/超时 → 下载器修复）；`--only open_data_esa_product_sample` 复跑。
2. **R2 h5→mat 桥**：取 `omega_avg_daily_smap_online` run events 确认缺口 → 新增 `smap_h5_to_mat` 模块（参照 `download_nodes.py:685` `GldasNc4ToMatModule`；输入以本地 `I:\Geograph_DataSet\Soil_Moisture\SMAP_Origin_Data` 真实样例为准）→ 种子图插 `nsidc_smap_download → smap_h5_to_mat → omega_avg_daily` → Test/algorithms 单测 → `--only omega_avg_daily_smap_online` 至 succeeded。
3. **R3 gldas 凭据**：凭据已核实干净（earthdata='Rejoyce'）→ 追传播链（portal store → worker 进程 env / job payload → `ingest/gldas_download.py` 消费点），使 worker 与 FastAPI 同源；**只改传播路径不动存储/加密**（高风险区约束）；`test_config_security.py` 全量回归；`--only omega_avg_daily_gldas_online` 复跑。
4. **R5 GDAL PATH**：`fy_preprocess` 的 GDAL CLI 解析链（`CGDA_GDAL_BIN → OSGeo4W → QGIS → conda → PATH`）本机定位并配置 `.env`；`--only fy_tb_online_read` 复跑解除 blocked:deps。
5. **R4 HPC**：隧道连通性探测；不可达 → 矩阵如实 blocked + 指引（不伪造）。

### P5 阶段 5：FY3F 接入 Python（~2.5h）

1. `ingest/fy_preprocess.py` FY3F 变体：SDS/波段命名与 nodata（-32767/-32768→NaN）、YYYYMMDD regexp 提取（以 `Matlab/fy拼接/B4_FY3F.m` + `FY3F_MWRI_mosaic.py` 为准）；mosaic 复用既有 buildvrt+warp 骨架。
2. `fy_download` 模块 `fy_platform` 参数支持 `"3F"`；种子 `omega_sf_fenkuai_fy_online` 加 FY3F 支路（默认 disabled，对齐 FY3B 支路先例）。
3. `Matlab/readme.txt` 补说明：FY3B/FY3D 已合并入 Python、FY3F 本轮接入、原始快照 1 字节级差异与空白规范化说明（AD9 收尾）。
4. Test/algorithms FY3F 合成 fixture 用例（HDF → 拼接 → EPSG:6933）。
5. 验证：`pytest Test/algorithms -q` + `--only omega_sf_fenkuai_fy_online`（FY3D 主链不受影响）+ `Tools/audit_workflow_seeds.py`。

### P6 阶段 6：全量回归 + 提交（~2h）

1. `Env\Python312\python.exe launch.py restart backend`（换 worker 世代；Windows 管理员终端）。
2. 全量矩阵（后台）：`Tools/smoke_system_workflows.py --report .ai/progress/2026-08-19-workflow-live-matrix-omega.md`。
3. 后端全量：`CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= CODEBUDDY_SAFE_DELETE_SANDBOX= Env/Python312/python.exe -m pytest Test/backend -p no:cacheprovider --basetemp="Test/.pytest-be"`。
4. 前端：`cd Code/frontend && npm run test && npm run lint && npm run build`。
5. `pre-commit run --all-files`（PATH 前置 `C:\Windows\System32` + `C:\FreeRulesPrograms\BasicToolkits\Git\cmd`）。
6. **提交序列**（硬约定：每次 `git add -A` 全量暂存、单提交；Test/debug/ 脚本随提交 2 纳入）：
   - 提交 1（P1+P2+P3 + 阶段 2 遗留）：`feat(omega): ω 反演图层默认在线、变体路由修复与时间轴联动组配色`
   - 提交 2（P0 复验 + P4）：`fix(workflow): 分析工具 {DATA_ROOT} 展开与冒烟残留修复(esa/h5→mat/gldas/GDAL)`
   - 提交 3（P5）：`feat(fy): FY3F MWRI 拼接接入 fy_preprocess 并补课题组代码对应说明`
7. 推送 origin/dev：**待用户明确指示**（不自动推送）。

---

## 四、假设与决策表（增量，沿用前计划 D1'-D17）

| # | 决策 | 依据 |
|---|---|---|
| D18 | dist 重建是 UI 验证前置（P0 先行），构建后无需重启 Gateway（静态目录直读） | §2.2-1：dist 13:31 < 源码 14:43 |
| D19 | X2 验证以「事件流出现 fy_download 多节点链」为种子路径铁证（区别于裸模块 chunk 直出） | §2.2-2；上次 run 误走单模块路径的事件特征已知 |
| D20 | P2 用 light-smoke（omega_sf_fenkuai_smap_single 本地链）先验组结构，UI 实跑在线链在 P1 基础上顺带覆盖 | ω 在线反演时长不可控（35min/chunk），先静态结构后长任务 |
| D21 | P3 组配色纯样式（四态 modifier class），不动 ActiveRunLayerGroup 状态机 | 前计划 D15；状态机四态已有 |
| D22 | 本轮沿用前计划全部未变更决策：D3（残留全修，HPC 如实 blocked）、D4（FY3F 支路默认 disabled）、D12（重放幂等验证）、D16（调试脚本入库、悬空 token 不动） | 前计划 §四 |

## 五、验证步骤汇总

1. P0：dist 时间戳 > 14:43；13 条冒烟全绿；ω 残留 run 清理；UI 抽验截图。
2. P1：4 个测试文件全绿；layer_id-only 提交 payload `workflow_name=omega_sf_fenkuai_fy_online` 且无 module_name；事件流含 fy_download 链。
3. P2：组内恰 3 真实成员无占位；overlay 渲染正常；配色切换即时生效。
4. P3：拖动 fetchable 段 → 组 computing（蓝）→ ready（绿）→ 新帧渲染；失败（红）60s 后可再触发且无重复组；check:catalog 无漂移；8 天块 timeKey 对齐。
5. P4：R1/R2/R3/R5 各 `--only` 至 succeeded（或 blocked+指引）；R4 如实记录；test_config_security 全量回归。
6. P5：FY3F fixture 测试过；FY3D 主链回归无影响；种子审计过。
7. P6：全量矩阵 + Test/backend 全量 + 前端 test/lint/build + pre-commit 全绿 → 提交 1/2/3。

## 六、风险登记（增量）

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| X2 变体路由在 live 环境仍有遮蔽路径（如 FE 显式键优先级） | 中 | 中 | P1 三断言（payload 键、事件链、占位符展开）；失败按链逐层定位 |
| ω 在线实跑时长拉长验证周期（download+反演） | 高 | 低 | light-smoke 先行；长任务后台轮询；P2 结构验证用本地链 |
| online_temporal 与 8 天块步长语义冲突（1d vs 8d） | 中 | 中 | P3-4 先核对 timeKey 解析；冲突则修 native_step 投影而非前端解析 |
| dist 重建引入前端回归 | 低 | 中 | npm run test + build 在 P6 全量兜底；P0 抽验即查 |
| R3 凭据传播触碰加密高风险区 | 低 | 高 | 只改传播路径；test_config_security 全量回归；不动存储/加密 |
| Windows 文件锁下 pre-commit 吞改动 | 低 | 高 | 每提交前 `git add -A` 全量暂存（硬约定） |

## 七、执行顺序

```
P0(dist 重建+基线复验+残留 run 清理) → P1(X2 验证) → P2(ω 组实跑+渲染配色)
  → P3(online_temporal+时间轴+组颜色) → P4(残留修复,可与 P3 后期穿插)
  → P5(FY3F) → P6(全量回归+提交 1/2/3)
```
