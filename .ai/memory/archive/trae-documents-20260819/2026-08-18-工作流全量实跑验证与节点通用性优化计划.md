# 工作流全量实跑验证与节点通用性优化计划（2026-08-18）

> 目标（用户原话拆解）：
> 1. **每条现有工作流可以正常运行**——51 条系统种子逐条实跑到终态，缺数据则下载或合成假数据；
> 2. **模块节点可通用、可扩展**——消除"单一化克隆"节点，未用节点类型不等于坏节点（补编译覆盖），扩展点文档化。
>
> 方法论：staff-engineer 视角——先建立**全量实跑矩阵**作为事实基线，修复由矩阵驱动（不凭猜测改代码）；重构带测试护栏且保持对外的 node_type / seed JSON 兼容。

---

## 一、现状基线（Phase 1 探索结论）

### 1.1 种子全景（`Code/backend/workflow_seeds/system/`，51 条）

| 族 | 数量 | 引擎 | 数据依赖 | 已知状态 |
|---|---|---|---|---|
| analysis（面板可见 11） | 11 | python_provider | `{DATA_ROOT}/_runtime/smoke_*.tif/geojson`（**12 个 smoke 文件已存在**于 `I:\Geograph_DataSet\_runtime\`） | 08-06 冒烟仅跑过 histogram |
| analysis（隐藏 stub_v1 9） | 9 | python_provider | 同上 smoke 文件 | 08-10 live-smoke 已过 |
| analysis（raster 2） | 2 | python_provider | `{DATA_ROOT}` 泛化输入 | 08-06 批次 C 过 |
| data_access | 13 | python_provider/common | 本地真实数据 / 在线门户 / **3 条含 REPLACE_* 占位符** | 部分：smap_local、noaa_grib 过；esa/nasa 占位符未实跑；cds 08-17 通；cdse/nomads/ndvi_online 未实跑；gee 1 条状态未知 |
| inversion | 13 | python_provider | 课题组固定目录（Soil_Moisture/、FY3D、GLDAS 等，**真实数据在 I 盘齐备**） | 08-17 审计 10 条 + 本周新增 3 条（`omega_avg_daily_fy_online`、`omega_sf_fenkuai_fy_dual`、`omega_sf_fenkuai_smap_dual`，来自 37b4fe1 GLDAS DUAL 提交）**未入审计未实跑** |
| weather demo | 2 | weather | open-meteo-local 容器 | 08-06 过 |

- 静态审计（compile + dry-validate）51/51 绿（08-17 基线 49 条 + 新 3 条需复核）。
- 凭据现状（08-17 验证）：nsmc 3 账号轮换 ✓、earthdata=Rejoyce ✓、CDS API Key + 数据集许可 ✓；GEE 账号状态未知。
- `.data/workflow_definitions/system/` 当前 17/51（陈旧，任意 `/workflow-definitions` API 调用自愈）。

### 1.2 实跑工具现状

- `Tools/smoke_system_workflows.py`（69KB）：提交+轮询+矩阵报告，但**批次硬编码仅覆盖 28/51**，缺 23 条（10 条 analysis_*、fy_tb_local/online_read、ndvi_local/online/gee_read、cds/cdse/nomads 单节点、3 条新 omega 变体）；含数据在场探测、stub_v1 fixture 生成、GLDAS 模板定位等既有特判逻辑（复用）。
- `Tools/generate_synthetic_test_data.py`：生成 daily_bundle/timeseries_bundle 合成 .mat（Tools/test_data/）。
- `Tools/audit_workflow_seeds.py`：静态审计（编译/dry-validate/注册表/图层引用/timer）。

### 1.3 节点通用性问题清单（审计发现，按价值排序）

| # | 问题 | 位置 | 影响 |
|---|---|---|---|
| N1 | 6 个标量天气渲染克隆类（dewpoint/visibility/pressure/precipitation/humidity/cloud_cover）逐行相同，仅 `_layer_id`/fallback 方法名/`artifact_type` 三处字符串差异；temperature 又走另一专有方法（不一致） | `weatherengine/nodes/*_grid_render.py` 8 文件 ~130 行/个 | 新增天气图层要克隆一个类；温/标量路径分叉 |
| N2 | `constants.py WEATHER_LAYER_SPECS` ↔ `field_mapping.py SURFACE/HEIGHT/PRESSURE_LAYER_IDS` 双登记同批图层 | `weatherengine/constants.py`、`field_mapping.py`（wind-field 15 处 / temperature_2m 9 处字面量） | 新图层漏改一处即静默错位 |
| N3 | 注册表 **29 个节点类型无任何种子使用**（config/read、data/number 等 8 个参数节点、gee/clip 等 4 个、weather/*_render 6 个、module/* 8 个、output/file 等），且无编译级最小图测试 → "注册但坏"不可见 | `node_template_registry.py`（3975 行手写表）；08-17 审计 §注册表差集 | 扩展性承诺（29 个可用算子）无证据 |
| N4 | `_classify_stage()` 按算法 stage 名 substring 匹配映射前端阶段（`omega_sf`/`ddca`/`nsidc`…），新算法默认落 "processing" | `python_provider_bridge_service.py:96-110` | 新算法接入即出现未分类阶段 |
| N5 | 校验器只查 `_meta.linked_layer_id`，**不查节点 properties.layer_id** → `open_data_esa_product_sample` 输出节点 `layer_id="remote-sensing"` 悬空逃逸校验 | `layer_workflow_validator.py` Rule 2 | 悬空引用无告警 |
| N6 | 3 条 open_data 样例种子含 `REPLACE_WITH_*` 文本占位符，用户原样运行必失败 | `open_data_esa/nasa/noaa_*.json` | "样例"不可运行，违背示范用途 |
| N7 | 门户下载节点族（cds/nomads/cdse/nsidc/gldas/fy）每节点绑一机构，`http_open_data` 已有 portal_catalog 动态 preset 机制未推广 | `node_template_registry.py` 下载段 | 结构性单一化（本轮仅记录，不重构） |
| N8 | 文档漂移：`workflow_seed_conventions.md:62` 以已删除的 `fy_tb_nas_read` 作示例 | Docs/03-规范协议 | 误导 |
| N9 | 新算法接入需手写 3975 行注册表中的模板（唯一必改的后端文件）；`template_deriver.py` 已有自动派生起点未接通 | `node_template_registry.py`、`algorithms/contracts/template_deriver.py` | 扩展成本（本轮文档化，不实现自动派生） |

### 1.4 扩展点事实（正面，写入文档即可）

新增算法 = ① `Code/algorithms/providers/Python/modules/<名>.py`（BaseModule + `@register_module_decorator`，**pkgutil 自动扫描零注册**）+ ② `node_template_registry.py` 手写模板 + ③ 可选种子/图层目录。**不需要**动 bridge/dispatch/runner/provider_registry。

---

## 二、执行计划（6 个阶段，矩阵驱动）

### 阶段 0：环境就绪与静态基线刷新（预计 0.5h）

1. 启动运行栈（**需管理员终端**）：`Env\Python312\python.exe launch.py start`（Docker + FastAPI + 7 Worker + Beat + Gateway）；若 Docker 已在跑可仅 `restart backend`。
2. 触发 `GET /workflow-definitions` → 确认 `.data/workflow_definitions/system/` 17→51 同步齐。
3. 重跑静态审计：`Env\Python312\python.exe Tools\audit_workflow_seeds.py`（覆盖新 3 条 omega 变体，预期全绿；异常即静态问题先修）。
4. 核对 smoke fixtures：`I:\Geograph_DataSet\_runtime\smoke_*.tif/geojson/mat` 12 个文件在位；缺失则用烟测器内 fixture 生成逻辑（`smoke_system_workflows.py:962`）补。

### 阶段 1：烟测器全量化改造（预计 2-3h）

改 `Tools/smoke_system_workflows.py`：

1. **目录扫描枚举**：新增 `--all` 模式，以 `SEEDS_DIR` 目录扫描为准（读每条 JSON 的 `_meta.category/engine`），批次表仅作排序提示；未识别种子归 "UNBATCHED" 组按通用路径提交（`extract_datasource_selection` 兜底 + 提交期 dry-validate 先行分类）。
2. **补 23 条缺口种子的 runner 映射**（复用既有特判模式）：
   - 10 条 `analysis_*`：直接提交（smoke 输入已在种子默认值中展开 `{DATA_ROOT}`）。
   - `fy_tb_local_read` / `ndvi_local_read` / `fy_tb_online_read`：复用 BATCH_D 的本地数据探测路径（`Soil_Moisture/FY3*` glob、NDVI 目录）。
   - `cds_era5` / `cdse_sentinel` / `nomads_gfs_grib`：小窗口参数（单变量、单日、小 bbox）。
   - `ndvi_gee_read`：先探测 GEE 配置（`GET /config/gee/accounts` 或 `/gee/config`），未配置 → 标 `blocked: gee-not-configured`（矩阵如实记录，不硬跑）。
   - 3 条新 omega 变体：复用 BATCH_F 的数据探测（`omega_avg_daily_fy_online` 含 fy_download 头；`*_dual` 需 `gldas_utc_template_global.mat`，复用 `:1397` 定位器）。
3. **占位符种子可运行化**（N6，改种子 JSON 而非工具）：
   - `open_data_esa_product_sample`：`relative_path` 换为 Copernicus 公开小样例真实路径；输出节点 `properties.layer_id` 删除或改为目录中存在的图层（一并修 N5 的悬空实例）。
   - `open_data_nasa_earthdata_sample`：`variable` 填真实变量名（如 SMAP `soil_moisture` 族或 MODIS NDVI 变量），URL 指向无需登录的公开 granule。
   - `open_data_noaa_grib_sample`：`REPLACE_VARIABLE_OR_SKIP_IF_RAW_FILE` → 真实 GFS 变量（如 TMP/t2m）或删除 convert 节点参数。
   - 若公开小样例 URL 不可得 → 合成归档（本地 zip/tar 放 `_runtime/synthetic/`，种子指向 `file://` 或 `{DATA_ROOT}` 路径），并保留原占位文本到 `_meta.notes`。
4. 工具改造后 `--dry-run` 全量过一遍（只打印计划不提交）。

### 阶段 2：全量实跑矩阵（预计 3-5h，含下载等待）

1. **本地族先行**（无外网依赖）：analysis 22 + 本地 inversion 13 + 本地 data_access 3 + weather 2 = 40 条。
2. **公开在线族**：nomads、ndvi_online（CMR 公开）、open_data_noaa/nsidc/esa/nasa、gldas_online——小窗口。
3. **凭据在线族**（复验，小窗口单日）：FY online ×2、SMAP online ×2、CDS。
4. **GEE**：按阶段 1 探测结果二选一（跑 / blocked 记录）。
5. 产出矩阵：`Env\Python312\python.exe Tools\smoke_system_workflows.py --all --report .ai/progress/2026-08-18-workflow-live-matrix.md`；每条记录 run_id / 终态 / 耗时 / blocker / 产物抽查（`GET /workflow-runs/{id}` result_refs + `GET /artifacts/{id}/preview.png` 抽 1 个）。
6. Celery 元数据保持纯英文（硬约定）。

### 阶段 3：矩阵驱动修复（时间视失败数，预留 3-6h）

对矩阵中每条非 succeeded：定位根因（events + worker 日志）→ 分类修复：

| 失败类别 | 处置 |
|---|---|
| 种子缺陷（路径/参数/悬空引用） | 修种子 JSON |
| 节点缺陷（模块逻辑/契约不匹配） | 修模块或模板，补测试 |
| 数据缺失 | 下载真实小样例，或 `Tools/generate_synthetic_test_data.py` 扩展生成合成文件放 `_runtime/`，种子参数指向 |
| 凭据/许可阻塞 | 矩阵记 blocked + 原因（不算失败，单列一节） |
| 环境/队列问题 | 修 launcher/worker 配置 |

顺带落地（独立小修，随阶段 3 一起）：
- **N5**：`layer_workflow_validator.py` 增加 Rule 6——节点 `properties.layer_id` 若存在必须命中图层目录（error 级）；`Test/backend/test_layer_workflow_validation.py` +用例。
- **N8**：`workflow_seed_conventions.md:62` 示例换成现存种子。
- 验证命令：`Env/Python312/python.exe -m pytest Test/backend/test_layer_workflow_validation.py Test/backend/test_workflow_seed_sync.py -q`。

### 阶段 4：节点通用性重构（预计 4-6h，带测试护栏，对外兼容）

> 原则：对外契约（node_type 字符串、种子 JSON、前端模板目录）**零变更**；只收敛引擎侧实现与登记真源。

1. **N1 标量渲染收敛**：
   - 新建 `weatherengine/nodes/scalar_grid_render.py`：参数化 `ScalarGridRenderNode`（`_layer_id`、fallback 构建方法名、`artifact_type` 由构造参数/spec 驱动，从 `WEATHER_LAYER_SPECS` 取 palette/单位）。
   - 6 个旧文件（dewpoint/visibility/pressure/precipitation/humidity/cloud_cover）改为薄壳：`class WeatherDewpointGridRenderNode(ScalarGridRenderNode): _layer_id="dewpoint"` 保留原类名与注册（weatherengine 节点注册表、`node_template_registry` 不动）。
   - temperature 专有 `build_temperature_geojson_from_grid` 与标量路径的差异审查：若仅为字段命名差异则并入（fallback 策略表驱动），若有实质渲染差异（如调色板插值）则保留独立类但在文档标注理由；wind_field 保留独立（粒子流矢量，有正当性）。
   - 测试：现有 `Test/backend/test_weatherengine_service.py` 等全过 + 新增标量节点参数化渲染单测（两个代表 layer 的 geojson 产出断言）。
2. **N2 双登记消除**：
   - `WeatherLayerSpec` 增加 `layer_kind: str = "surface" | "height" | "pressure"` 字段（`constants.py` 内 17 层 spec 逐层标注）。
   - `field_mapping.py` 的 `SURFACE/HEIGHT/PRESSURE_LAYER_IDS` 三个 frozenset 改为由 specs 派生（保留同名导出，消费方零改动）。
   - 测试：新增派生一致性断言（派生结果 == 现硬编码集合），防回归。
3. **N3 未用节点编译覆盖**：
   - 新增 `Test/backend/test_node_template_compile_coverage.py`：遍历 `node_template_registry` 全部模板，逐个构造"单节点最小图"（输入端口接 const/source 节点或显式缺省）过 `workflow_graph_compiler` 编译断言成功；按 engine 分流（weather 原生 / python_provider→module / gee 单独断言不进编译而走 bridge 的现状）。
   - 预期会暴露少量"模板端口定义与编译器规则不符"的真实缺陷 → 修复即本轮收益。
4. **N4 阶段分类声明化**：
   - `BaseModule` / `@register_module_decorator` 支持 `phase` 声明（复用 `template_overrides` 通道）；`_classify_stage` 优先读声明，substring 匹配降级为 fallback；现有 omega 族模块补声明。
5. **N7/N9 不动代码，文档化**：`.ai/skills/workflow-design.md` 或 `Docs/02-架构设计/` 增补"新增算法节点接入清单"（1.4 节事实 + portal preset 归一为已知候选改进），标注 N7 为 backlog。

### 阶段 5：回归 + 复跑验证（预计 2h）

1. 定向：改天气节点 → `pytest Test/backend/test_weatherengine_service.py Test/backend/test_weather_point_service.py Test/backend/test_weather_tile_service.py -q` + 阶段 4 新增测试。
2. 全量：`CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= CODEBUDDY_SAFE_DELETE_SANDBOX= Env/Python312/python.exe -m pytest Test/backend -p no:cacheprovider --basetemp="Test/.pytest-be"`。
3. 重构后复跑受影响种子实跑：weather 2 条 + 6 标量层抽 2 条（经显式 weather workflow）确认渲染无回归。
4. `pre-commit run --all-files`（本机注意 PATH 需含 `C:\Windows\System32` + `C:\FreeRulesPrograms\BasicToolkits\Git\cmd` 与 COMSPEC）。

### 阶段 6：文档定稿与提交

1. `.ai/progress/2026-08-18-workflow-live-matrix.md` 收口（矩阵终态 + 修复清单 + blocked 专节）。
2. 文档同步：`AGENTS.md`「改 X 则跑 Y」补节点模板/标量渲染行；`Docs/03-规范协议/workflow_seed_conventions.md` 修 N8 + 占位符种子说明；`.ai/skills/workflow-design.md` 补接入清单。
3. 提交（遵循硬约定：`git add -A` 全量 → 单提交；Conventional Commits；建议拆两轮：①`fix(workflow): 种子可运行化与实跑矩阵修复` ②`refactor(weather): 标量渲染节点收敛与图层登记真源统一`——若时间紧可合一轮综合提交）。

---

## 三、假设与决策

| # | 决策 | 理由 |
|---|---|---|
| D1 | 在线种子允许真实小窗口下载（单日/单变量/小区域） | 用户明确"数据没有可以下载"；08-17 已验证账号配额可用 |
| D2 | GEE 未配置则记 blocked 不算失败 | 凭据属用户资产，不为测试临时注入 |
| D3 | 重构对外零破坏（node_type/种子/前端不变） | 51 条种子与前端画布是既有契约，收敛只做内部实现 |
| D4 | N7 下载节点归一、N9 模板自动派生**本轮不做**，文档化入 backlog | 改动面大、收益主要在未来接入；本轮聚焦可验证收益 |
| D5 | 实跑矩阵是唯一验收标准；blocked（凭据/许可）单列不算失败 | 与"确保可正常运行"语义一致：阻塞点如实暴露而非掩盖 |
| D6 | worker/演示开关维持现状（demo:// 不启用） | 生产禁止演示开关是硬约定；假数据走合成文件而非 demo:// |

## 四、风险

| 风险 | 缓解 |
|---|---|
| 在线门户限流/账号配额（CDS 并发 1、NSMC 轮换） | 在线族串行跑、单变量小请求；失败重试间隔 ≥ 上游窗口 |
| 51 条实跑总时长（omega 在线单条 ~20min） | 本地族并行分组；在线族只跑增量未验证的 3+3 条，08-17 已过的 4 条仅小窗复验 |
| 标量渲染收敛引入天气图层回归 | 薄壳继承保类名；派生一致性断言；实跑抽验 2 层 |
| 编译覆盖测试暴露大量历史缺陷导致范围失控 | 时间盒 2h：只修阻断编译的缺陷，其余记 issue 入矩阵附录 |
| Windows Docker 需管理员 | 启动步骤前置说明；若 Docker 不可用则 `restart backend` 走本地 Redis（Redis 在 Docker 内，不可用即整体阻塞上报） |
