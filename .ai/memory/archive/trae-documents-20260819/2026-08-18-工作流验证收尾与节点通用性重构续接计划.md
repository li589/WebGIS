# 工作流全量验证收尾与节点通用性重构续接计划（2026-08-18）

> 承接被中断的旧任务（原计划：`.trae/documents/2026-08-18-工作流全量实跑验证与节点通用性优化计划.md`；旧会话记录：`temp.txt`）。
> 目标不变：①每条现有工作流可正常运行（缺数据则下载或合成）；②模块节点可通用、可扩展（消除单一化）。
> 本计划聚焦**续接**：验证并提交存量工作 → 修复残余失败种子 → 完成 N1/N4 重构与 N7/N9 文档 → 回归 + 最终矩阵 → 收口提交。

---

## 一、现状基线（Phase 1 探索结论）

### 1.1 旧会话进度盘点（对照旧计划 6 阶段）

| 旧计划阶段 | 状态 | 证据 |
|---|---|---|
| 阶段 0 环境就绪 | ✅ 完成 | 静态审计 50 条种子全绿；smoke fixtures 在位 |
| 阶段 1 烟测器全量化 + N6 占位符种子 | ✅ 完成 | `Tools/smoke_system_workflows.py` +559 行（目录扫描为真源、批次仅排序）；3 条 open_data 种子已编辑 |
| 阶段 2 全量实跑矩阵 | ✅ 完成 | 最终报告 `Test/reports/smoke-matrix-2026-08-18-full.md`（14:16）：**38/50 成功、6 失败、6 阻塞** |
| 阶段 3 矩阵驱动修复 + N5 + N8 | 🔶 大部分完成 | `_raster_ops.py` 取数收敛（救回 12 种子）+ `parse_bbox` RegionSpec 分支（analysis_clip）；N5 校验器 Rule 6 + 74 行测试；N8 conventions 示例修正。**残余 6 失败种子未修** |
| 阶段 4 节点通用性重构 | 🔶 N2/N3 完成，N1/N4/N7/N9 未做 | N2：`constants.py` layer_kind + `field_mapping.py` 派生 + `test_weather_layer_kind_derivation.py`；N3：`test_node_template_compile_coverage.py`。**N1（scalar_grid_render.py 不存在）、N4（`_classify_stage` 仍 substring 匹配）、N7/N9 文档未做** |
| 阶段 5 回归 + 复跑 | ❌ 未做 | — |
| 阶段 6 文档收口 + 提交 | ❌ 未做 | **工作区 995 行改动全部未提交**（18 修改 + 2 新测试文件） |

### 1.2 最终矩阵终态（42 定义 + 2 瓦片热路径 = 50 种子）

**成功 38**（含 10 条 analysis_*、9 条 stub_v1、omega 族大部分、weather 2、nomads、nasa_earthdata LP DAAC 公开 JPG）。

**失败 6**：

| 种子 | 症状 | 初步根因方向 |
|---|---|---|
| `ndvi_local_read` | submit_http_422 | 提交体缺 `datasource_selection.input_dir`（422 校验拒绝） |
| `fy_tb_local_read` | 不支持的文件格式: .hdf5 | 合成 fixture 用 `.hdf5`，FY 读取器（`ingest/fy.py`）仅认 `.HDF` |
| `fy_tb_online_read` | unknown input port: n2.data | **端口双登记不一致**：注册表 `download/fy_preprocess` 声明 `data` 口，算法侧 FyPreprocessModule PortSpec 无此口 |
| `cds_era5_reanalysis_download` | run_failed | 请求日期 T-1 过新（ERA5 滞后 5-7 天），cdsapi 排队 |
| `omega_avg_daily_gldas_online` | run_failed | worker 缺 Earthdata 用户名/密码（门户 token 在 UI 但未传播到 worker） |
| `open_data_esa_product_sample` | run_failed（run-b86d57faff0f） | 待取 events（CDSE 401 / UUID 格式？）；凭据缺失时应为 blocked:creds 语义 |

**阻塞 6**（单列不算失败，但需处置）：

| 种子 | 阻塞原因 | 处置 |
|---|---|---|
| `open_data_nsidc_smap_sample`、`ndvi_online_read`、`omega_sf_fenkuai_smap_online` | Earthdata URS 账号 **[tessa] 被锁定**（401 invalid_account_status） | 查门户凭据存储（应为 Rejoyce，08-16 已验证可用）；修正后等 10 分钟窗口复跑 |
| `omega_avg_daily_smap_online` | blocked:config（h5→mat 桥 + 非轻量冒烟） | 已知结构缺口，保持记录 |
| `cdse_sentinel_download` | 无 Copernicus 账号 | 保持 blocked（用户资产，D2 决策延续） |
| `ndvi_gee_read` | GEE 未配置（accounts=0） | 保持 blocked（D2 决策延续） |

### 1.3 未提交工作区清单（+995/−117，18 文件 + 新增）

- **烟测器与种子**：`Tools/smoke_system_workflows.py`（+559）、3 条 `open_data_*.json`
- **算法侧修复**：`_raster_ops.py`（+105：取数收敛 + parse_bbox）、`data_access_nodes.py`（+17）、`download_nodes.py`（+88：GLDAS 参数强转）、`universal_reader.py`/`data_cache_service.py`/`portal_catalog.py`（小修）
- **N2**：`weatherengine/constants.py`（+12）、`field_mapping.py`（−63 改派生）
- **N5**：`layer_workflow_validator.py`（+28）+ `test_layer_workflow_validation.py`（+74）
- **新测试**：`test_weather_layer_kind_derivation.py`（N2）、`test_node_template_compile_coverage.py`（N3）
- **N8**：`Docs/03-规范协议/workflow_seed_conventions.md` 示例修正
- **疑似无关遗留**：`Code/frontend/src/components/LoadingOverlay.vue`（compact 条带 top:96px→0 定位修复，08-17 UI 会话遗留）
- **待清理**：`Test/debug/` 新增 16 个一过性探针脚本（`_tmp_probe_fy_online.py`、`fetch_failed_events.py` 等；其中 `test_smoke_tool_helpers.py` 以 test_ 开头会被 pytest 收集，需迁移或删除）

### 1.4 运行环境

- FastAPI `:8000` 与 Gateway `:5175` **均在运行**（旧会话栈未停）；I: 盘（DATA_ROOT）已挂载；无需管理员重启 Docker。
- 代码改动后需 `launch.py restart backend` 换 worker 世代（旧会话已修复世代清扫）。

---

## 二、执行计划（5 个阶段）

### 阶段 0：存量工作验证与基线提交（预计 1h）

> 原则：995 行已完成工作先验证、先提交，防丢失；新工作独立成提交。

1. **定向验证未提交工作**（仓库根执行，环境变量前缀防 WorkBuddy shim 假阳性）：
   ```
   CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= CODEBUDDY_SAFE_DELETE_SANDBOX= Env/Python312/python.exe -m pytest Test/backend/test_layer_workflow_validation.py Test/backend/test_weather_layer_kind_derivation.py Test/backend/test_node_template_compile_coverage.py Test/backend/test_workflow_seed_sync.py -q -p no:cacheprovider --basetemp="Test/.pytest-be"
   ```
   ```
   CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= CODEBUDDY_SAFE_DELETE_SANDBOX= Env/Python312/python.exe -m pytest Test/algorithms/test_stub_modules.py Test/algorithms/test_data_access_nodes.py -q -p no:cacheprovider --basetemp="Test/.pytest-be"
   ```
   若 N5/N2/N3 测试有未写完的残缺（旧会话在追加 Rule 6 测试时中断），补完后再继续。
2. **全量后端回归**：`… -m pytest Test/backend -p no:cacheprovider --basetemp="Test/.pytest-be"`（预期 ~1992 passed 基线）。
3. **前端校验**（LoadingOverlay.vue 改动）：`cd Code/frontend && npm run test -- loading`（无匹配测试则 `npm run lint`）。
4. **清理 `Test/debug/` 探针**：删除本会话新增的一过性脚本（16 个）；`test_smoke_tool_helpers.py` 若为烟测器真实单测则迁至 `Test/tools/`，否则删除。保留旧有诊断脚本（`check_omega_results.py`、`diag_env.py` 等）。
5. **提交 1（存量基线）**：PATH 前置 System32 + Git 后 `git add -A` → 单提交：
   `fix(workflow): 种子可运行化、实跑矩阵驱动修复与图层登记真源统一`
   （涵盖烟测器全量化、open_data 种子、_raster_ops 取数收敛与 parse_bbox、N5 Rule 6、N2 layer_kind 派生、N3 编译覆盖测试、N8 文档、GLDAS 参数强转、LoadingOverlay 遗留修复、debug 探针清理）

### 阶段 1：残余失败种子修复（矩阵驱动，预计 2-3h）

> 每条流程：`GET /workflow-runs/{run_id}` events 定位根因 → 修复 → `--only <seed>` 单种子复跑 → 过则下一条。修复属通用性范畴的优先在模块侧做（不只为烟测打补丁）。

1. **`ndvi_local_read`（422）**：取 422 响应体确认缺失键；修 `Tools/smoke_system_workflows.py` 的 `ndvi_local_read` 分支——`datasource_selection` 补 `input_dir` 指向合成 NDVI 目录（`{DATA_ROOT}/_runtime/synthetic/` 下 16 天 GeoTIFF fixture，工具已具备生成逻辑）；若 422 源自种子 `data/source` 节点默认路径 `{DATA_ROOT}/NDVI` 不存在导致的提交期校验，则 runner 覆盖之。
2. **`fy_tb_local_read`（.hdf5）**：`ingest/fy.py` 扩展名匹配改**大小写不敏感并接受 `.hdf/.hdf5/.HDF` 变体**（通用性修复）；fixture 保持 `.hdf5` 以验证宽容化；补单测（`Test/algorithms/` 现有 fy 测试文件追加用例）。
3. **`fy_tb_online_read`（n2.data 端口）**：算法侧 `FyPreprocessModule`（`modules/fy_preprocess` 对应实现文件）PortSpec **补 `data` 输入端口**（kind="data"），实现中把上游 manifest/path 作为输入路径回退——与注册表模板对齐（N2 类双登记问题的对齐修复）；修后**复跑整个 FY 族**（`fy_tb_local/online_read`、`omega_avg_daily_fy_online`、`omega_sf_fenkuai_fy_single/dual/online`）防牵连回归。
4. **`cds_era5_reanalysis_download`（日期窗口）**：smoke runner cds 分支日期补丁 T-1 → **T-7 起窗口**（ERA5 滞后惯例）；复跑（cdsapi 排队分钟级，轮询耐心至终态）。
5. **`omega_avg_daily_gldas_online`（worker 凭据）**：查明 worker 侧 Earthdata 凭据解析链（portal store → worker env → job payload），对齐 08-16 已验证链路（URS token 交换复用）；若为门户凭据未传播到 worker 的结构性缺口，修复传播路径；修后单种子复跑。
6. **`open_data_esa_product_sample`**：取 `run-b86d57faff0f` events 定位（CDSE 401 / OData UUID 格式 / 下载器超时）；若凭据缺失是根因 → 恢复 preflight `blocked:creds` 拦截语义（与 D2 决策一致，不硬跑）；若有可修根因 → 修复复跑。
7. **Earthdata [tessa] 账号锁定**（3 条 blocked）：查门户凭据存储（`peek_portal_store.py` 已有探针可参考，或直接查 portal store API/DB）；earthdata profile 应为 Rejoyce（08-16 验证可用）——若存在 `tessa` 陈旧条目则修正/删除；修正后等 URS 10 分钟解锁窗口**复跑 3 条**（`open_data_nsidc_smap_sample`、`ndvi_online_read`、`omega_sf_fenkuai_smap_online`）；若 URS 侧持续锁定 → 矩阵如实记录 blocked 并注明需用户在 URS 网站解锁。
8. **保持 blocked 记录**（不动）：`cdse_sentinel_download`（无账号）、`ndvi_gee_read`（GEE 未配置）、`omega_avg_daily_smap_online`（h5→mat 桥结构缺口）——矩阵 blocked 专节如实呈现。

### 阶段 2：节点通用性重构残余 N1/N4 + N7/N9 文档（预计 3-4h）

> 原则（旧计划 D3 延续）：对外契约（node_type 字符串、种子 JSON、前端模板目录、类名与节点注册）**零变更**；只收敛引擎侧实现与登记真源。

1. **N1 标量渲染收敛**：
   - 新建 `Code/backend/app/weatherengine/nodes/scalar_grid_render.py`：参数化 `ScalarGridRenderNode` 基类——`_layer_id`、fallback 构建方法名、`artifact_type` 由构造参数 / `WEATHER_LAYER_SPECS` 驱动（palette/单位取自 spec）。
   - 6 个旧文件（`dewpoint/visibility/pressure/precipitation/humidity/cloud_cover` 的 `*_grid_render.py`）改**薄壳**：`class WeatherDewpointGridRenderNode(ScalarGridRenderNode): _layer_id="dewpoint"`，保留原类名与注册（weatherengine 节点注册表、`node_template_registry` 不动）。
   - temperature 专有 `build_temperature_geojson_from_grid` 与标量 fallback 差异审查：仅字段命名差异 → 并入（fallback 策略表驱动）；实质渲染差异 → 保留独立类并在模块 docstring 标注理由；wind_field 保留独立（矢量粒子流，正当性明确）。
   - 新增参数化渲染单测（两个代表 layer 的 geojson 产出断言）。
2. **N4 阶段分类声明化**：
   - `register_module` / `register_module_decorator` 的 `template_overrides` 通道支持 `phase` 键声明（`Code/algorithms/providers/Python/modules/registry.py`）。
   - `_classify_stage()`（`python_provider_bridge_service.py:96-110`）优先读模块 phase 声明，substring 匹配降级为 fallback。
   - 现 substring 名单涉及的模块（omega_sf / omega_avg / omega_block / sf_invert / ddca / fy_preprocess / ssh_sync / nsidc）补 phase 声明。
   - 测试：声明优先 + 无声明回退 substring 两路径断言。
3. **N7/N9 文档化（不动代码）**：`.ai/skills/workflow-design.md` 增补「**新增算法节点接入清单**」——新增算法 = ① `modules/<名>.py`（BaseModule + `@register_module_decorator`，pkgutil 自动扫描零注册）+ ② `node_template_registry.py` 手写模板 + ③ 可选种子/图层目录；**不需**动 bridge/dispatch/runner/provider_registry；portal preset 归一（N7）与模板自动派生（N9，`template_deriver.py` 起点）标注为 backlog。

### 阶段 3：回归 + 最终矩阵（预计 2-3h）

1. **定向**：`pytest Test/backend/test_weatherengine_service.py Test/backend/test_weather_point_service.py Test/backend/test_weather_tile_service.py -q` + N1/N4 新增测试 + `Test/algorithms` 全量。
2. **全量后端**：`CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= CODEBUDDY_SAFE_DELETE_SANDBOX= Env/Python312/python.exe -m pytest Test/backend -p no:cacheprovider --basetemp="Test/.pytest-be"`。
3. **换世代后实跑抽验**：`launch.py restart backend` →
   - 天气瓦片：`GET /weather/tiles/dewpoint/{z}/{x}/{y}`、`/weather/tiles/precipitation/{z}/{x}/{y}` 200（N1 抽验 2 标量层）+ temperature/wind-field 回归；
   - 阶段 1 修复过的种子族抽验。
4. **全量矩阵复跑（最终验收）**：`Env\Python312\python.exe Tools\smoke_system_workflows.py --report .ai/progress/2026-08-18-workflow-live-matrix.md`（后台跑，omega 族单条 2-6 分钟，总时长可控；Celery 元数据保持纯英文）。
5. `pre-commit run --all-files`（PATH 前置 `C:\Windows\System32` + `C:\FreeRulesPrograms\BasicToolkits\Git\cmd`）。

### 阶段 4：文档收口与提交（预计 1h）

1. **矩阵报告收口**：`.ai/progress/2026-08-18-workflow-live-matrix.md` 终态（成功/失败/blocked 三段 + 修复清单 + blocked 专节含凭据处置指引）。
2. **文档同步**：`AGENTS.md`「改 X 则跑 Y」补天气标量渲染节点行（`scalar_grid_render` → weatherengine 三件套测试）；`.ai/skills/workflow-design.md` 已含接入清单（阶段 2 产出）。
3. **提交 2**（N1+N4 完成后）：`git add -A` → `refactor(weather): 标量网格渲染参数化收敛与算法阶段分类声明化`。
4. **提交 3**（文档收口后）：`git add -A` → `docs(workflow): 工作流实跑矩阵收口与节点接入清单`。
5. **推送 origin/dev 待用户明确指示**（历次会话惯例）。

---

## 三、假设与决策

| # | 决策 | 理由 |
|---|---|---|
| D1 | 存量 995 行工作先验证、先提交，再做新改动 | 防中断丢失；提交历史清晰（存量一提交、N1/N4 一提交、文档一提交，每轮均 `git add -A` 全量暂存单提交，符合硬约定） |
| D2 | cdse / ndvi_gee 保持 blocked 记录，不算失败 | 凭据属用户资产（旧计划 D2 延续）；blocked 如实暴露而非掩盖 |
| D3 | Earthdata 账号以门户存储中 Rejoyce 为准；`tessa` 视为陈旧/误配条目 | 08-16 已验证 Rejoyce 可用（URS token 交换 + 206 下载实测） |
| D4 | FY 读取器扩展名宽容化在模块侧做（非仅 fixture 改名） | 符合「节点通用性」目标；`.hdf5` 是 HDF5 生态常见命名 |
| D5 | fy_tb_online_read 在算法侧模块补 `data` 端口（对齐注册表），不改注册表 | 注册表模板是前端画布契约（D3 对外零破坏）；算法侧补口是双登记对齐的正向修复 |
| D6 | CDS 日期窗口 T-7 | ERA5 数据滞后 5-7 天惯例；T-1 必失败 |
| D7 | LoadingOverlay.vue 随存量提交保留 | 08-17 UI 会话遗留的合理修复（compact 条带贴顶、文案下移避让顶栏），语义自洽 |
| D8 | N1 temperature 若有实质渲染差异则保留独立类 | 旧计划既定审查路径；不为收敛而收敛 |
| D9 | smap_online 的 h5→mat 桥本轮不做 | 结构性缺口超出本轮范围，矩阵已记录 |

## 四、风险

| 风险 | 缓解 |
|---|---|
| CDS cdsapi 排队分钟级 / 偶发失败 | 复跑耐心轮询至终态；失败重试间隔 ≥ 上游窗口 |
| URS 账号锁定持续不解 | 3 条种子如实 blocked + 注明用户需 URS 网站解锁；不伪造凭据 |
| fy_preprocess 补端口牵连 FY 族种子 | 修后复跑全部 6 条 FY 相关种子 |
| N1 重构引入天气图层回归 | 薄壳继承保类名；瓦片抽验 2 标量层 + temperature/wind-field；weatherengine 三件套定向测试 |
| 全量矩阵耗时长（omega 单条 2-6 分钟） | 后台跑 + 轮询；本地族与在线族分组 |
| 存量测试有中断残留（N5 用例可能未写完） | 阶段 0 先跑定向测试暴露，补完再提交 |

## 五、验证步骤汇总

1. 阶段 0：定向测试（4 后端文件 + 2 算法文件）→ 全量 Test/backend → 前端 lint/test → 提交 1。
2. 阶段 1：每条失败种子修复后 `--only <seed>` 复跑到终态；FY 族全量复跑；最终 6 失败 → 0 失败（或转 blocked 并有明确凭据指引）。
3. 阶段 2：N1 参数化渲染单测 + N4 声明化两路径测试全绿。
4. 阶段 3：weatherengine 三件套 + Test/algorithms + Test/backend 全量 → restart backend → 瓦片抽验 4 层 → 全量矩阵复跑（最终报告）。
5. 阶段 4：pre-commit 全绿 → 提交 2/3 → 工作区干净。
