# ω 双温度移植收尾 + 种子/文档同步 + 算法科普 HTML 计划（续接）

> 日期：2026-08-18 · 状态：待用户确认
> 前置：旧计划《2026-08-18-omega工作流2x2x2重构与SF双温度移植计划.md》执行约 85% 后会话中断（中断点：A3 两条 DUAL 种子 id 冲突修正完成后）。
> 本计划基于当前工作区（git 未提交改动 28 文件）+ temp.txt 尾部记录 + 代码逐项核验编写。

---

## 一、摘要

旧计划 A（种子重构）/ B（SF 双温度算法）的实现与测试文件**已全部落盘**，但从未跑过全量验证；A4/A5 收尾未做；附带产生的 hidden tag 机制与 fy_tb 种子清理缺词表/文档同步；用户核心诉求 **C（算法科普 HTML）完全未做**。本计划只做六类收尾：

1. **D1 全量测试验证**——确认 B 部分算法与 13 条种子真实全绿（含 ORIG_TS golden 回归）。
2. **D2 B7 收尾**——AVG 模块 gldas 键映射补测试用例（映射本身已加，`modules/omega_avg_daily.py` L49）。
3. **D3 A4 收尾**——`layer_descriptors.json` 4 个 method-* 条目 description 补新入口说明 → `gen:catalog` → `check:catalog`。
4. **D4 A5 收尾**——盘点文档 ω 族清单更新为 13 条新命名，data_access 表移除已删除的 2 条种子行。
5. **D5 词表同步**——`workflow_seed_conventions.md` 补 `hidden` 标记说明。
6. **D6 算法科普 HTML**（用户重点）——面向非地学背景开发者的自包含 HTML；原任务指定的 `build-web-data-visualization` 插件在当前环境**不存在**，改用 `html-report` 技能产出等效交付物。
7. **D7 提交**——`git add -A` 全量暂存 + `pre-commit run --all-files` + 单条 Conventional Commit（仓库硬约定禁止部分暂存提交，故不拆分）。

## 二、现状分析（逐项核验结论）

### 2.1 已完成（工作区未提交，来源核验）

| 项 | 证据 |
|---|---|
| B1 配置扩展 | `omega_sf.py` `OmegaSfConfig` 含 `dual_tg_mode/ct_smref/ct_exp/gldas_time_tol_hours/fy3d_desc_local_hour/...`；`_preload_chunk` L4374 DUAL 缺 gldas_mat_folder 即 ValueError |
| B2 GLDAS 预读 | `_sf_desc_local_hour`(L4089)、`_build_sf_daily_bundle_config`(L4098)、`_load_gldas_day_rows`(L4132，1-based lin_pix +1)、`_filter_tvec_by_gldas_days`(L4167，经度窗口预判)；主流程 L3193-3200 在 8 天块构建**前**过滤日期 |
| B3 数学层 | `_forward_tb_dual`(L1514)、`_resid_halpha_dual_temp`(L1586)、`_resid_omega_block_dual_temp`(L1635)、`_f_sm_dual_temp`/`_ddca_dual_temp`(L1724) |
| B4 像元接线 | L699（Step1）/L838（Step3 DDCA）/L1082（Step2 块 ω）三处 dual 分支调用已接 |
| B5 I/O 契约 | `_preload_chunk` DUAL 返回 10 键（L4452-4464），ORIG_TS 7 键不变；`test_omega_sf_io_contract.py` 已加 `_make_gldas_day` + DUAL 契约测试（+119 行） |
| B6 双温度测试 | `Test/algorithms/test_omega_sf_dual_temp.py` 新文件，13 个测试（forward parity vs omega.py、残差形状/正则尾项、GLDAS 三温度合成 roundtrip、DUAL/ORIG_TS 分支、全 NaN 短路、真值恢复） |
| B7 键映射（代码） | `modules/omega_avg_daily.py` L49 已加 `"gldas_mat_folder": ("gldas_mat_folder", "gldas_mat", "daily_mat_sources")` |
| B7 顺带 | `ingest/daily_bundle.py` `pick_gldas_file_indices` 已向量化（NumPy searchsorted） |
| A1 重命名 | 10 条 ω 种子 `_meta.name`+`name` 已全部改为「[SMAP|风云] ω [平均|动态]反演[·双温度]（…）」 |
| A2 新种子 | `omega_avg_daily_fy_online.json`（风云 ω 平均反演·在线，含 NSMC auto→NAS 回退 + FY3B 停用支路） |
| A3 新种子 | `omega_sf_fenkuai_smap_dual.json`（GLDAS 节点 id=8，已修 id 冲突）/ `omega_sf_fenkuai_fy_dual.json`（复用 fy_single 的 gldas_mat_folder 节点 id=6）；两者 algorithm_params 均含 `temp_scheme=DUAL, dual_tg_mode=PAPER_CT, ct_smref=0.3` |
| A5 部分 | 盘点文档头部计数改 47 条、尾部已记「冗余清理第二轮」「stub_v1 隐藏」两条 |
| 附带·hidden 机制 | `workflow_definition_service.py` L268+4 行：`_meta.tags` 含 `hidden` 则 `list_definitions` 跳过；9 条 `*_basic` 烟测种子已加 tag（fusion×2/preprocess×2/stats×5） |
| 附带·种子清理 | 删除 `fy_tb_nas_read.json`/`fy_tb_nsmc_online.json`；`test_system_seeds_compile.py` 守卫同步收窄并注明原因 |

### 2.2 未完成（本计划范围）

| # | 缺口 | 证据 |
|---|---|---|
| 1 | 全量测试从未验证 | 工作区 28 个改动文件无任何一次完整 pytest 记录可查 |
| 2 | B7 测试用例 | `test_omega_avg_daily_module.py` 无任何 gldas 断言（rg 零命中） |
| 3 | A4 | `layer_descriptors.json` 不在 git 修改列表；L1484/L1524/L1618/L1718/L1763/L1803 附近 4 个 method-* 条目 description 未提 dual/fy_online 新入口。**注意：`workflow_name` 字段全库一致存 id（如 `ndvi_local_read`），保持不动是正确行为**——旧计划 A4 表述有误，只改 description/notes |
| 4 | A5 ω 族清单 | 盘点文档 L29-46 仍为旧结构「D2 5 条 / SF 4 条」，缺 3 条新种子与新显示名；L54-56 data_access 表仍列已删除的 `fy_tb_nas_read`/`fy_tb_nsmc_online` 两行 |
| 5 | hidden 词表 | `Docs/03-规范协议/workflow_seed_conventions.md` rg `hidden` 零命中，而 9 条种子已用 |
| 6 | C 科普 HTML | `.trae/documents/` 仅有全景展示/策略对照两份旧 HTML，无算法详解指南 |
| 7 | 提交 | 全部改动未提交 |

## 三、变更方案

### D1. 全量测试验证（先于一切修改，确认基线）

仓库根执行（PowerShell 语法——AGENTS.md 的 bash 前缀写法需转换）：

```powershell
$env:CODEBUDDY_SESSION_ID=''; $env:CLAUDE_SESSION_ID=''; $env:CODEBUDDY_SAFE_DELETE_SANDBOX=''
Env/Python312/python.exe -m pytest Test/algorithms/test_omega_sf_dual_temp.py Test/algorithms/test_omega_sf_e2e.py Test/algorithms/test_omega_sf_io_contract.py Test/algorithms/test_omega_sf_q4_parity.py Test/backend/test_omega_avg_daily_module.py Test/backend/test_omega_avg_algorithm.py Test/backend/test_system_seeds_compile.py Test/backend/test_workflow_graph_compiler.py Test/backend/test_stub_modules.py -p no:cacheprovider --basetemp="Test/.pytest-be" -q
```

- 任何失败先修复再进入 D2；ORIG_TS golden（`test_omega_sf_e2e.py`）数值必须不变。
- 若 DUAL e2e 小网格用例暴露 `_filter_tvec_by_gldas_days` 与合成 GLDAS 时次（22:30UTC）窗口不匹配，按测试内 `_sf_desc_local_hour`（SMAP=06:00 本地时）推算合成文件时刻修正测试 fixture，不改产品代码语义。

### D2. B7 收尾：AVG 模块键解析测试用例

文件：`Test/backend/test_omega_avg_daily_module.py` 新增一个用例——合成 `datasource_selection={"gldas_mat": "<dir>", "ancillary_mat": "<dir>", ...}`（键用 `gldas_mat` 而非 `gldas_mat_folder`，正对修复的映射路径）→ 调用模块的键解析入口 → 断言 resolved 字典含非空 `gldas_mat_folder`。实现期先读该文件现有用例的 resolver 调用方式（照抄同文件模式），不新造 harness。

### D3. A4 收尾：layer_descriptors.json description 补新入口 + 重新生成 catalog

文件：`Code/backend/app/catalog_seeds/layer_descriptors.json`，4 处：

| 条目（行号） | description 追加 |
|---|---|
| `method-smap-omega-doy-dynamic`（~L1484） | 「双温度变体见 `omega_sf_fenkuai_smap_dual`（GLDAS DUAL）。」 |
| `method-fy-omega-doy-dynamic`（~L1618） | 「双温度变体见 `omega_sf_fenkuai_fy_dual`。」 |
| `method-smap-omega-doy-avg`（~L1718） | 该条已关联 dual 两种子，仅核对 description 是否点名 `omega_avg_daily_smap_dual`/`gldas_online`，缺则补一句 |
| `method-fy-omega-doy-avg`（~L1763） | 「在线入口：`omega_avg_daily_fy_online`（NSMC auto→NAS 回退）。」 |

- `workflow_name`/`workflow_id` **一律不动**（存 id 是全库契约）。
- 若条目另有 `run_readiness_notes` 且列数据源，dual 条目补 `gldas_mat`（气象 GLDAS 目录）。
- 完成后：`cd Code/frontend && npm run gen:catalog && npm run check:catalog`（prebuild 亦自动重跑 gen:catalog）。

### D4. A5 收尾：盘点文档 ω 族清单 + data_access 表

文件：`Docs/02-架构设计/工作流预设与端到端流水线盘点.md`：

1. L29-46 两节重写为按新命名的 13 条总表（或保留 D1/D2/SF 三节但补 3 行新种子并统一用新显示名 + id）：
   - D2 节补 `omega_avg_daily_fy_online` 行（风云 ω 平均反演·在线）；
   - SF 节补 `omega_sf_fenkuai_smap_dual`/`omega_sf_fenkuai_fy_dual` 行（动态反演·双温度（本地），输入含 gldas_mat）；
   - 每行「功能」列首放新显示名。
2. L54-56：删除 `fy_tb_nas_read`/`fy_tb_nsmc_online` 两行（种子文件已删，`fy_tb_online_read` auto 回退覆盖），节标题「15 条」改实际数。
3. 头部方法说明不必再动。

### D5. 词表同步：hidden 标记

文件：`Docs/03-规范协议/workflow_seed_conventions.md`——在 tag 词表节追加一行：`hidden`（运行时机制标签：`list_definitions` 过滤，`get_definition`/运行/定时器不受影响；适用 stub_v1 烟测载体与用户自定义隐藏）。实现期先读该文档词表的实际格式再插入。

### D6. 算法科普 HTML（C 部分，用户重点）

- **实现方式**：`html-report` 技能（原指定的 `build-web-data-visualization` 插件在当前插件清单中不存在，等效替代；`.trae/documents/` 已有同风格 HTML 两份可对齐观感）。
- **产出**：`.trae/documents/2026-08-18-omega-算法详解与工作流指南.html`（自包含、离线可开；`.trae/` 不入 git）。
- **版式硬约束（用户偏好）**：紧凑布局、控制横向延展、元素与上方左对齐、严谨学术术语（τ-ω 辐射传输、单次散射反照率 ω、植被光学厚度 τ/VOD、粗糙度 h/α、菲涅耳反射、DDCA 等），避免口语化。
- **内容大纲**（7 节，含 SVG 图示）：
  1. 微波遥感反演在干什么：亮温 TBv/TBh（K）、极化/入射角；反演目标 SM（m³/m³）、VOD/τ、ω、h/α。
  2. τ-ω 前向模型：单温度（Ts 单一乘子）vs 双温度（TG 土壤发射项 + TC 冠层发射/散射项）公式并排；PAPER_CT 的 Ct=(SM_ref/0.30)^0.30 幂律与 TG=Tsoil2+Ct·(Tsoil1−Tsoil2)；TSOIL1_ONLY/TSOIL2_ONLY 两子模式；GLDAS 为何必需（三温度层 Ts/Tsoil1/Tsoil2）、过境本地时→UTC 换算与 1.6 h 容差、无匹配日期取交集剔除。
  3. 三族算法谱系图：D1 `omega_block`（块反演产 h*/α* 图 + daily_omega 日缓存）→ D2 `omega_avg`（ω 固定多年 DOY 气候态，逐日轻量 DDCA 回代）→ SF `omega_sf_fenkuai`（8 天块内联合反演逐日 ω）依赖与差异表；「平均 avg / 动态 raw」与 D2/SF 的对应。
  4. SF 四步流水线图解：Step0 τ(NDVI) 初始化 → Step1 低 τ 样本 h*/α*（least_squares）→ Step2 块级 ω（λ 平滑 + 热启动）→ Step3 DDCA 逐日 SM/VOD + QC；单/双温度分支点标注（对应 `omega_sf.py` L699/L1082/L838）。
  5. 数据输入全景：SMAP L3（L 波段 1.4 GHz）/ FY3D·3B MWRI（X/K 波段 bias 匹配）/ NDVI（逐日 + DOY 气候态）/ 辅助库（IGBP 土地覆盖、砂黏土粒级、DEM、反照率…）/ GLDAS 三温度。
  6. 13 条工作流矩阵总览：2×2×2 核心格 + D1 预处理 + 3 条双温度变体；新旧名对照表；layer catalog 映射（method-*-doy-avg / -doy-dynamic）。
  7. Matlab→Python 移植状态小结：单温度全量对齐（parity/golden 测试）、双温度 2026-08-18 补齐（SF 族镜像实现 + daily_bundle 复用）、AVG 族既有 DUAL。
- 事实来源：`algorithms/omega_sf.py`、`algorithms/omega.py`、`ingest/daily_bundle.py`、Matlab 原型 `omega_sf_fenkuai.m`（L2195-2269 双温度公式）、既有对照文档 `.trae/documents/2026-08-18-omega-sf-fenkuai策略移植对照.html`。公式以代码现实现为准，不凭记忆写。

### D7. 提交

顺序（仓库硬约定：先 `git add -A` 全量暂存、禁止部分暂存/pathspec 提交）：

```powershell
$env:Path = "C:\FreeRulesPrograms\BasicToolkits\Git\cmd;$env:Path"
pre-commit run --all-files     # ruff/mypy/eslint/prettier/契约检查
git add -A
git status                     # 确认无未暂存项
git commit -m "feat(algo): SF 双温度(GLDAS DUAL)移植与 ω 工作流 2×2×2 重组命名"
```

单条提交（无法部分暂存故不拆分），message body 概括三类：双温度算法+测试、13 条种子重组+hidden 机制+fy_tb 清理、文档/描述符同步。不推送（用户未要求）。

## 四、假设与决策

1. **workflow_id / `workflow_name` 字段一律不改**——前者牵连前端正则与测试硬编码，后者全库存 id 是既有契约（旧计划 A4「更新为新显示名」的表述予以纠正）。
2. **hidden 机制与 fy_tb 清理并入本次提交**——同会话产物、已有测试守卫与文档记录，单独拆分需部分暂存（被硬约定禁止）。
3. **科普 HTML 用 html-report 技能**替代不存在的 build-web-data-visualization 插件；交付等效（自包含 HTML + 图示）。
4. **`.trae/` 不入 git**（git status 佐证），科普 HTML 无需提交。
5. D1 若发现 DUAL 实现缺陷：先修产品代码并补回归用例，再继续收尾；不回退双温度特性。
6. 2×2×2 之外的 3 条双温度变体 + 1 条 D1 预处理定位维持旧计划共识（研究变体/上游必需，不并入 8 核心格）。

## 五、实施顺序与验证

**顺序**：D1（测试基线）→ D2 → D3（gen:catalog + check:catalog）→ D4 → D5 → D6（科普 HTML）→ D7（提交）。

**最终验证清单**：

1. D1 命令全绿（含 ORIG_TS golden 不变）。
2. `cd Code/frontend && npm run check:catalog` 无漂移；`npm run test`、`npm run build` 通过。
3. `pre-commit run --all-files` 通过。
4. 联调抽查（可选，如后端在跑）：`GET /workflow-definitions` 出现 13 条 ω 新显示名、9 条 `*_basic` 烟测种子不在列表。
5. 科普 HTML 在浏览器可开、公式与代码一致，回复附 computer:// 链接。

**完成判据**：上述 1-3、5 全部满足且工作区已提交。
