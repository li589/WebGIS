# CGDA 已修改代码复审 + 旧计划归档 + 新审查修复计划

> 计划日期：2026-08-13
> 审查框架：Brooks-Lint PR Review（brooks-review 技能）
> 背景：工作区存在 178 个未提交文件改动（13272 行增 / 10110 行删），规模远超原 38 项计划，且叠加了 RBAC 权限体系、Analysis Tools、weather-tile 拆分、地图底图优化等计划外新工作。本计划对全部改动做复审，归档旧计划，并制定收尾修复方案。

---

## 一、摘要

本轮复审确认：原计划 `codebase-fix-plan.md` 的 P0 阻塞项与 P1 架构/安全高优先项**基本落地**，但存在若干"部分落地"残留与一处误提交风险；同时新增了大量计划外模块需纳入审查与文档归位。本计划分四部分：

1. **归档旧计划**：把 `codebase-fix-plan.md` 与 `codebase-health-report.html` 移入 `Docs/99-历史归档/`，并清理根目录散落临时文件。
2. **修复已确认的残留问题**（P0-2 残留、P0-4 注释不符、coverage 误提交、P2-8 阻塞语义等）。
3. **收尾原计划未落地项**（P1-4 特化方法删除、P3-3、P3-4、前端 P1-1/P1-5/P1-6 剩余部分）。
4. **计划外新工作的文档归位与最小修复**（RBAC、Analysis Tools、queue_dispatch 等）。

目标：让工作区改动以"无残留、可提交、文档归位"的状态收口，避免半成品积压。

---

## 二、当前状态分析

### 2.1 工作区改动总览

| 类别 | 数量 | 说明 |
|------|------|------|
| 修改文件 | 178 | 分布后端/前端/测试/CI/文档 |
| 新增未跟踪文件 | 约 40 | 含 10 个后端测试、前端 composables、6 个 analysis seed、security 文档等 |
| 行数变化 | +13272 / -10110 | 规模远超原 38 项计划 |

### 2.2 原计划落地核对（38 项 → 三态）

**后端（11 项核对）**

| 项 | 判定 | 证据 |
|----|------|------|
| P0-1 硬编码路径改 settings | ✅ 已落地 | `overlay_registry.py:36` `_PROVIDER_ROOT = Path(_settings.python_provider_root)` |
| P0-2 反向导入移 service 层 | ⚠️ 已迁移但残留 | `weather_sync_service.py:172` 已改导入 `weather_coverage_cache`；但 `weather_router.py:120` 仍保留同名本地函数，遮蔽 import |
| P1-2 overlay_registry 加锁 | ✅ 已落地 | `_REGISTRY_LOCK`，register/get/list 均加锁 |
| P1-3 lru_cache 失效机制 | ✅ 已落地 | `invalidate_template_cache()` + `POST /config/cache/invalidate-templates` |
| P1-4 weather_render 去重 | ⚠️ 部分落地 | 6 个特化方法已精简为委托 `build_scalar_geojson_from_grid`，但方法定义未删除（保留空委托壳） |
| P2-4 service 去 HTTPException | ✅ 已落地 | 新增 `services/errors.py` 领域异常 + `main.py` 全局处理器 |
| P2-5 auth_bootstrap 硬编码凭据 | ✅ 已落地 | 改 `os.getenv` + `.env.example` 占位符 |
| P2-9 except Exception 替换 | ⚠️ 部分落地 | 第一批文件已替换，但大量兜底场景保留 `# noqa: BLE001` |
| P2-10 config.py 嵌套 dataclass | ✅ 已落地 | 新增 Redis/Celery/Gee/Weather/Auth/Storage 分组 |
| P2-11 main.py import 副作用 | ✅ 已落地 | `register_default_providers()` 移入 `create_app()` |
| P3-3 时间列表生成去重 | ❌ 未落地 | 5 个 `_*_time_list` 函数仍独立 |
| P3-4 dotenv 静默吞异常 | ❌ 未落地 | `core/config.py:9-15` 仍 `except Exception: pass` |

**前端（5 项核对）**

| 项 | 判定 | 证据 |
|----|------|------|
| P1-1 weather-tile-manager 拆分 | ⚠️ 部分落地 | manager 1948→1415 行，新增 4 个文件；但 scheduler(队列/排序/gap-sweep) 与退避/断路器编排仍留在 manager |
| P1-5 WorkflowCanvas 抽 composables | ⚠️ 部分落地 | 1890→1215 行，新增 4 个 composable；但 lifecycle/theme 未抽，未达 ≤300 行 |
| P1-6 workflow-runner 缩窄 DI | ⚠️ 部分落地 | 测试已补(28 个)，但 DI 仅拆 5 个子接口，方法数未减少 |
| P1-7 WebGL utils 抽取 | ✅ 已落地 | 新增 `webgl-utils.ts`，两渲染器已引用 |
| P2-13 barrel export 文档 | ✅ 已落地 | `index.ts` 新增 43 行注释块 |

**CI（3 项核对）**

| 项 | 判定 | 证据 |
|----|------|------|
| P0-3 后端覆盖率阈值 | ✅ 已落地 | 50→52 |
| P0-4 前端覆盖率 | ⚠️ 注释与配置不符 | CI 注释写"40%"，实际 `vite.config.ts` 为 22/21/16/19 |
| P2-8 security-scan 阻塞 | ⚠️ 语义未达 | 已移除 `continue-on-error`，但该 job 是叶子 job，不构成真正阻塞 |

### 2.3 计划外新增工作（需纳入审查与文档归位）

1. **RBAC 资源级权限体系**：`permission_repository.py`（新）、`api/deps.py` 三角色模型、`user_repository.py`/`auth_router.py` 升级、`migrate_roles_v2.py`、配套测试 `test_resource_permissions.py` 等。
2. **Analysis Tools 分析工具链**：`analysis_router.py`/`analysis_run_service.py` 扩展、6 个 `analysis_*.json` seed、`analysis_tools.json` 注册。
3. **workflow 排队调度**：`queue_dispatch_service.py`（新），配合 5 个新测试。
4. **前端地图底图优化**：`MapCanvas.vue` 低缩放纯色抑制、`textures.css` 纹理系统、`App.vue` 全局 noise-overlay、Google Fonts。
5. **security 文档**：`Docs/02-架构设计/security-upgrade-plan.md`、`Docs/08-HTML报告/` 下 2 个报告目录。

### 2.4 已确认的残留与风险点

| 编号 | 问题 | 位置 | 严重度 |
|------|------|------|--------|
| R-1 | `coverage/` 目录(0.8MB)未 gitignore，将误提交 | `Code/frontend/coverage/` | 高 |
| R-2 | `weather_router.py:120` 同名函数遮蔽 import（P0-2 残留） | `weather_router.py:120` | 中 |
| R-3 | CI 注释"40%"与 `vite.config.ts` 实际 22/21/16/19 不符 | `ci.yml` + `vite.config.ts` | 低 |
| R-4 | 根目录散落 `codebase-fix-plan.md`/`codebase-health-report.html`/`tmp.txt`，违反"根仅 3 份文档"约定 | 仓库根 | 中 |
| R-5 | 原计划 P1-4 保留空委托壳方法 | `weather_render_service.py` | 低 |
| R-6 | P2-8 security-scan 是叶子 job，不真正阻塞合并 | `ci.yml` | 低 |

---

## 三、变更方案

### 阶段 A：归档旧计划 + 清理根目录

**A-1 归档 `codebase-fix-plan.md`**
- 移动至 `Docs/99-历史归档/codebase-fix-plan-20260813.md`
- 在计划文件头部追加一行归档标记（`> 已归档 2026-08-13，见 .trae/documents/复审与修复计划.md`）

**A-2 归档 `codebase-health-report.html`**
- 移动至 `Docs/08-HTML报告/codebase-health-report/`（与现有报告目录结构一致，含该 HTML 文件）

**A-3 清理 `tmp.txt`**
- `tmp.txt` 为空文件，直接删除（gitignore 已含 `temp.txt`，但 `tmp.txt` 未含，需一并删除）

**A-4 修正 `.gitignore`**
- 在 `.gitignore` 补 `coverage/` 规则（覆盖 `Code/frontend/coverage/`），解决 R-1

### 阶段 B：修复已确认残留

**B-1 消除 P0-2 残留（R-2）**
- 文件：`Code/backend/app/api/routers/weather_router.py:120`
- 删除第 120-145 行的本地同名函数 `invalidate_weather_coverage_cache`，改用第 35-41 行已导入的 `invalidate_weather_coverage_cache`。
- 确认 `weather_router.py` 内其余调用点（`weather_router.py:306` 附近）已使用导入符号。
- 验证：`CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= CODEBUDDY_SAFE_DELETE_SANDBOX= Env/Python312/python.exe -m pytest Test/backend/test_weather_bridge_service.py -q`

**B-2 修正 P0-4 注释与配置不符（R-3）**
- 文件：`.github/workflows/ci.yml` 中 `P0-4` 注释，改为"起步阈值 = 当前覆盖率基线 22/21/16/19，后续逐步提升至 40%+"
- `vite.config.ts` 阈值保持现状（当前基线），仅修注释。

**B-3 P1-4 特化方法删除（R-5）**
- 文件：`Code/backend/app/weatherengine/weather_render_service.py`
- 将 6 个已精简为委托的特化方法（1054/1064/1074/1084/1094/1104）删除，调用方直接改调 `build_scalar_geojson_from_grid`。
- 验证：`CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= CODEBUDDY_SAFE_DELETE_SANDBOX= Env/Python312/python.exe -m pytest Test/backend/test_weatherengine_service.py Test/backend/test_weather_tile_service.py -q`

**B-4 P2-8 security-scan 阻塞语义（R-6）**
- 文件：`.github/workflows/ci.yml`
- 在 security-scan job 的 `needs` 中不做改动（保持叶子 job），但在 P2-8 注释中澄清：此 job 作为独立质量门禁，是否阻塞合并由 GitHub 分支保护 required check 决定，CI 文件本身设置 `pip-audit`/`npm audit` 失败即 job 失败。

### 阶段 C：收尾原计划未落地项

**C-1 P3-3 时间列表生成去重**
- 文件：`Code/backend/app/services/overlay_registry.py:364-434`
- 将 `_smap_time_list`/`_gpcp_time_list`/`_doy_time_list`/`_soil_ddca_time_list`/`_date8_time_list` 提取为通用时间列表生成器 + 配置参数（start/step/format）。
- 验证：`CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= CODEBUDDY_SAFE_DELETE_SANDBOX= Env/Python312/python.exe -m pytest Test/backend/test_overlay_registry.py -q`

**C-2 P3-4 dotenv 静默吞异常**
- 文件：`Code/backend/app/core/config.py:9-15`
- `except Exception` 拆为 `except ImportError`（pass）+ `except Exception as exc`（`logging.warning`）。
- 验证：`CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= CODEBUDDY_SAFE_DELETE_SANDBOX= Env/Python312/python.exe -m pytest Test/backend/test_config_security.py -q`

### 阶段 D：计划外新工作最小修复 + 文档归位

**判断**：RBAC、Analysis Tools、queue_dispatch 等新工作已通过 119 个后端测试 + 28 个前端测试，功能完整。**不重写主体**，仅做文档归位与一致性检查。

**D-1 前端 `coverage/` 从 git 待提交中移除**
- 阶段 A-4 的 `.gitignore` 已覆盖；执行 `git reset HEAD Code/frontend/coverage/` 确保不残留已暂存索引（若已暂存）。

**D-2 security 文档索引更新**
- 检查 `Docs/README.md` 或 `Docs/02-架构设计/*` 是否已收录 `security-upgrade-plan.md`；若未收录，在对应索引补充一行。

**D-3 一致性抽查**
- 抽查 `migrate_roles_v2.py` 的 `--dry-run` 与 `permission_repository.py` 的 TTL 缓存逻辑是否有明显缺陷（Brooks 审查视角），发现即记录供后续 Iteration，不阻塞本计划。

---

## 四、假设与决策

| 假设/决策 | 说明 |
|-----------|------|
| tmp.txt 为空 | 已确认该文件无任何历史记录内容，直接删除，不归档 |
| 旧计划归档位置 | `Docs/99-历史归档/`（已确认该目录存在） |
| 旧健康报告归档位置 | `Docs/08-HTML报告/codebase-health-report/`（与现有 HTML 报告目录结构一致） |
| P0-4 阈值不调整 | 22/21/16/19 为当前真实基线，仅修注释，避免门禁误红 |
| 计划外新工作不重写 | 功能已通过测试，仅做文档归位与一致性检查 |
| P1-1/P1-5/P1-6 剩余部分 | 属"大重构未完"，列为下轮迭代，本轮不强行推进（避免风险聚集） |

---

## 五、验证步骤

完成阶段 A-D 后，按顺序执行：

```powershell
# 1. 后端测试（受影响模块）
CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= CODEBUDDY_SAFE_DELETE_SANDBOX= Env/Python312/python.exe -m pytest Test/backend/test_weather_bridge_service.py Test/backend/test_weatherengine_service.py Test/backend/test_weather_tile_service.py Test/backend/test_overlay_registry.py Test/backend/test_config_security.py -p no:cacheprovider --basetemp="Test/.pytest-be" -q

# 2. 全量后端
CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= CODEBUDDY_SAFE_DELETE_SANDBOX= Env/Python312/python.exe -m pytest Test/backend -p no:cacheprovider --basetemp="Test/.pytest-be" -q

# 3. 前端
cd Code/frontend && npm run test && npm run lint && npm run build

# 4. 契约检查
cd Code/frontend && npm run check:openapi && npm run check:catalog

# 5. Lint
pre-commit run --all-files

# 6. git 待提交确认
git status --short   # 确认无 coverage/、无 tmp.txt、无根目录散落文档
```

### 每步预期结果

- 阶段 A 后：`git status` 中 `codebase-fix-plan.md`/`codebase-health-report.html`/`tmp.txt` 消失，`Code/frontend/coverage/` 被 ignore。
- 阶段 B 后：`weather_router.py` 无重复函数定义，CI 注释与配置一致。
- 阶段 C 后：overlay_registry 时间列表去重、config dotenv 异常处理收敛。
- 阶段 D 后：所有新增文档已归位，无未收录索引。

---

## 六、下轮迭代（明确不做，留待后续）

以下项因属于大型重构且当前改动已规模庞大，本轮**不推进**，记录为下轮候选：

1. P1-1：weather-tile-manager 的 scheduler（四级优先级队列/视口排序/gap-sweep）与退避/断路器编排仍未抽出。
2. P1-5：WorkflowCanvas 的 LiteGraph 生命周期与主题 composable 未抽出，未达 ≤300 行。
3. P1-6：workflow-runner 的 DI 仅拆子接口，未真正缩窄为 5-8 个高层方法。
4. P2-9：except Exception 全量替换（当前仅覆盖第一批文件）。
5. P3-1：超长函数拆分。
6. P3-2：Tools/ 临时脚本清理。