# CGDA 完全审查问题清单 — 2026-08-22

> 范围：系统架构（粗）+ 整体代码（细）完全审查。**排除** `Code/WeiXin/`（微信小程序）。
> In scope：`Code/{backend,frontend,algorithms,shared,infra}` + `launch.py`/`launch/` + `Tools/` + `Test/`，含工作区未提交改动。
> 性质：**审计-only**（本轮不改码），问题分级 P0 致命 / P1 严重 / P2 警告 / P3 建议。
> 基线 HEAD：`d39b2d1`。去重基线：08-15 全审（F1-F9）、08-18 数值专项（12/12）、08-21 安审/算法卫生/UI 三专项（S/C/U/H 系列）、Phase1-6 评审。

---

## 一、测试基线证据（全量实跑）

| 套件 | 命令 | 结果 |
|------|------|------|
| 后端 pytest | `Env/Python312/python.exe -m pytest Test/backend`（REDIS_URL + ENVIRONMENT=test，全新 basetemp） | **1553 passed, 2 skipped**（508s） |
| 算法 pytest | `... -m pytest Test/algorithms` | **666 passed + 28 subtests**（63s） |
| 前端 vitest | `npx vitest run`（coverage 本轮禁用） | **162 文件 / 1187 tests 全 passed**（51s） |
| 前端 build | `npm run build`（先 python shutil 清 dist） | **成功**（vite 8，1.97s） |
| ruff | `ruff check Code/backend Code/algorithms Code/shared` | **8 errors**（含 2 个运行时 NameError，见 R-1/R-2） |

已知基线噪音（非新问题）：`.pytest_tmp` junction WinError 5（N-8 已知项，换 basetemp 规避）；vitest 日志含 `useBreakpoint` onMounted 警告（测试桩行为）；build 1 条 `INEFFECTIVE_DYNAMIC_IMPORT` 警告（overlay-symbology 动静态混引，P3）。

## 二、架构健康度（粗审，五维）

| 维度 | 评分 | 结论 |
|------|------|------|
| 分层与依赖 | 4/5 | 主链干净（routers→bridges→engine→repos）；残留 1 处 tasks→router 反向导入、1 处 services→tasks 侧向导入 |
| 配置治理 | 4/5 | fail-closed 链真实生效（启动断言已核）；仍有 1 处导入期绑定 settings + CWD 静默兜底 |
| 安全边界 | 3.5/5 | 鉴权/SSRF/限流主面完整；**新发现 4 处路径穿越姊妹点 + 2 类端点无鉴权**（见 P1）；写限流前缀漏 3 类端点 |
| 并发与状态 | 3/5 | 限流/会话/锁已 Redis 化；effective_config 失效、provider 覆盖、熔断器、瓦片并发槽仍为进程内态，与多 worker 支持矛盾 |
| 部署边界 | 4/5 | 生产拒启守卫齐全、compose 分离正确、gateway↔vite 代理逐项对齐；`BACKEND_RELOAD` 默认 true 未收口 |

**总体判断**：架构主链健康，无 P0。主要残余风险集中在：① 昨日批量修复（49994c4）自身引入 2 个运行时回归；② 08-21 路径穿越修复（31080a9）存在 4 处姊妹漏网点；③ 多 worker 进程内态残留；④ 前端 storeToRefs 禁令未闭环。

## 三、P1 严重问题（12 项，交付前必修）

### R 组：昨日修复引入的回归（ruff F821 实证，最高优先）

| # | 位置 | 问题 | 影响 | 修法 |
|---|------|------|------|------|
| R-1 | `app/services/_sqlite_pool.py:143` | `with contextlib.suppress(...)` 但文件仅 `from contextlib import contextmanager, suppress`（未导入模块名）；系 C-3 修复（49994c4）引入 | `_release` 在池满/已关闭路径抛 **NameError**，连接泄漏+错误掩盖 | 改用已导入的 `suppress(...)` 或补 `import contextlib` |
| R-2 | `algorithms/providers/Python/ingest/nsidc_download.py:572` | 调 `download_with_retry(...)` 但 `from ingest._http_resume import (...)` 只导了 `check_disk_space/format_size`；系 H-1 修复（49994c4）引入 | NSIDC 下载路径运行时 **NameError**，下载必崩（测试未覆盖该联网路径） | 导入列表补 `download_with_retry`；补最小单测 |

### B 组：路径穿越姊妹点（31080a9 修复漏网，均已复核确认）

| # | 位置 | 问题 | 影响 | 修法 |
|---|------|------|------|------|
| B-1 | `data_io/services/vector.py:345,352,489,513,532,551,570` | 全文件未用 `safe_import_child`，`IMPORTS_DIR / layer_id` 裸拼接，含 **写盘** 路径（patch_feature_attribute → `_write_layer`） | 已鉴权用户越界读/写任意目录 meta.json/data.geojson（`%5C` 亦可穿越） | 各入口首行 `dest = safe_import_child(layer_id)` + `..%2F`/`%5C` 回归测试 |
| B-2 | `data_io/services/document.py:42-57` | `_session_dir(session_id)` 无校验，`_save_table` 直写 | 越界读/写任意 `table.json` | 复用 `safe_import_child(session_id, root=DOC_SESSIONS_DIR)` 或白名单 `^doc-[0-9a-f]{12}$` |
| B-3 | `data_io/services/jobs.py:21-23` | `JOBS_DIR / f"{job_id}.json"` 无校验，record 回传客户端 | 读服务器任意合法 JSON（配置/凭证缓存） | 同上，白名单 `^job-[0-9a-f]{16}$` |
| B-4 | `data_io/services/export_layer.py:63,1221,1365` | layer_id 裸拼接 + 批量 zip `writestr(f"{layer_id}/{filename}")` 条目名未归一 | 越界读目录回传 + 产出 zip 含 `../` 条目（下游解压 zip-slip） | `safe_import_child` + arcname `Path(layer_id).name` 白名单化 |

### B 组续：鉴权缺失（已复核确认无 Depends）

| # | 位置 | 问题 | 影响 | 修法 |
|---|------|------|------|------|
| B-5 | `api/gee_config_routes.py:76-168` | 4 个 GET 端点零鉴权；`/gee/config/environment` 公开返回服务器绝对路径（凭证库路径等）；500 回显 `str(e)` | 匿名信息泄露辅助攻击 | router 级 `dependencies=[Depends(require_config_read_access)]`；路径字段改相对标识；500 固定文案 |
| B-6 | `api/routers/zonal_stats_router.py:43-67` | `/analysis/zonal-stats/sync` 无鉴权；CPU/IO 密集的 `compute_zonal_stats` 在 async handler 同步调用；500 泄露 | 匿名重计算 DoS（阻塞事件循环） | 加 `Depends(require_write_access)`；`anyio.to_thread.run_sync` 卸载；500 固定文案 |

### C/D/E 组：其余 P1

| # | 位置 | 问题 | 影响 | 修法 |
|---|------|------|------|------|
| C-1 | `services/config_weather_providers.py:280` | 天气 provider 运行时覆盖只改本进程 registry；DB 覆盖仅在进程启动时回放 | workers=2 时禁用/换源只对单个 worker 生效，Celery 侧更不生效 | Redis 版本戳广播失效，或路由前短 TTL 读 DB；至少标注"重启生效" |
| D-1 | 前端 22 文件存量 `storeToRefs`（DashboardView:3、MapCanvas:2、ModeToolbar:4、WorkflowStatusPanel:3、settings/* 10 面板等；selectors.ts 已修） | d39b2d1 只修了 selectors.ts，禁令未闭环。Pinia 3.0.4 对 undefined 裸属性访问 `.effect` 抛 TypeError 的机制仍在 | 当前未引爆（已证伪核实各 store 返回 RefImpl），但任何 store 新增 undefined 初值属性 → 整页白屏，与 08-22 报障同型 | 全量 codemod 为 `toRef(store, key)`；eslint `no-restricted-imports` 禁 storeToRefs + CI grep 门禁 |
| E-1 | `algorithms/providers/Python/data_access/universal_reader.py:324-334` | 0-360 经度网格跨缝 bbox 只取单段连续切片 | ERA5 等数据跨缝（west=-10,east=10 或全球）时静默丢半幅栅格，无报错 | 跨缝拆两段切片 concatenate，或先 roll 到 -180..180；空选择 raise 对齐 HDF5 分支 |
| E-2 | `algorithms/adapters/provider_adapter.py:15` | `_MAX_PARAMETER_KEYS = 64`（P0-5 DoS 防护）声明后全仓无引用，未接线 | 宣称的安全控制不存在，超大 parameters 直达算法入口 | adapter 入口校验 `len(parameters) <= 64` 并接线常量 |

## 四、P2 警告（15 项，按主题归组）

**多 worker 进程内态残留**（架构维度 4 的主体）：
- A-2 `effective_config.invalidate_effective_config` 仅翻转进程内标志；`circuit_breaker.py` 熔断器进程内 dict —— workers>1 时配置/熔断状态分裂（phase1 已知项残留）
- C-2 瓦片并发槽位 `asyncio.Semaphore(6)+threading.Semaphore(6)` 均进程内（tile_service.py:301-303）—— 2 worker 对上游并发翻倍至 12，429 风险
- C-4 tile_service 进程内 LRU（OrderedDict）无锁，事件循环线程与 anyio 线程池并发读写 → `RuntimeError: mutated during iteration`

**并发/长任务**：
- C-3 派发不确定（apply_async 8s 超时但可能已投递）+ Beat 2min 重派 → 同 run 双消息并发执行（疑似，需集成测试验证）
- C-5 Open-Meteo sync subprocess 超时只杀客户端，容器孤儿与下轮 sync 并发写同一 volume（疑似）
- B-8 `resumable_upload.py` 5 处 `STAGING_DIR / upload_id` 裸拼接未走 safe_import_child（可利用性低，纵深防御缺口）

**错误处理/信息泄露**：
- B-7 多处 5xx detail 回显 `str(exc)`：`layer_router.py:294`、`unified_tile_router.py:62`、`weather_tile_routes.py:91`、`config_routes.py:905` —— 违背异常收窄 PRD「500 固定文案」约定

**配置/部署**：
- A-3 `data_io/services/paths.py:38-47` 导入期绑定 settings + `output_root` 空时 CWD 静默兜底（导入产物可落入仓库目录）
- A-4 `BACKEND_RELOAD` 默认 true 且 start_fastapi 无生产守卫（已知未修，交付演练 #12）

**前端**：
- D-2 drawModule/measureModule 的 `dispose()` 从未被 teardownBinder 调用（ResizeObserver/RAF 卸载泄漏）
- D-3 `_http.ts:221` 外部 signal 优先于超时 signal 且不组合，`getWeatherCoverage` 等 8s 超时静默失效（`AbortSignal.any` 一行修复）
- D-4 导入矢量 GeoJSON（上限 80MB）全量深响应式，stores 零 `markRaw/shallowRef`

**算法**：
- E-3 `raster_writer.py` 未强制 matplotlib Agg 后端（worker 线程 GUI 后端崩溃风险）+ pyplot 非线程安全（node_parallelism>1）+ `plt.close` 不在 finally
- E-4 `_normalize_array` 3D 波段启发式：band-first `(3,H,3)` 被误转置（窄条带栅格静默错）
- E-5 `cache_store.put_file` tmp 名仅含 pid，同进程并发同 key 写冲突 → 坏文件入缓存
- E-6/E-7 **非原子 JSON 写疑似复发**：`omega_sf.py:2027` checkpoint 直写（截断后 resume 静默全重算 + allow_nan 非法 JSON + O(n²) 重写）；`service/async_jobs.py:227`、`service/job_queue.py:78` 同款（一个坏 pending 文件炸整个 dequeue 循环；inflight 无回收）

## 五、P3 建议（归组摘要，25+ 项，明细见 .ai 工作版）

- **分层异味**：tasks→router 反向导入（open_meteo_sync_tasks.py:316，P0-2 遗留尾巴）；services→tasks 侧向导入（weather_sync_service.py:121）；algorithms 4 处 lazy 导入 app.services 灰区
- **限流/状态**：写限流前缀漏 /workspace、/analysis、/export；/runtime/status 暴露 redis_url（可含密码）+ 每次全量 SCAN；API 槽位 TTL 60s 短于长 fetch
- **工作流引擎**：不识别节点 `enabled=false`（latent，当前无触发路径）；无节点间取消检查；`retry_limit`/`max_retries_per_node` 字段定义未接线；download follow-up 无 acks_late/幂等收口；`handle_workflow_timeout` 引用错误限时值（300s vs 实际 7200s）
- **天气**：fetch_gateway 非 pinned 路径宽 except 静默换源 + auto 缓存键错位
- **前端**：5 个"未提交修改"实为 CRLF-only 噪音（唯一真实变更 package.json 精确锁版；建议 .gitattributes 固化 eol=lf）；NodeCacheEntry 手写接口与生成契约重复；shpjs 主线程解析 80MB 无 Worker + papaparse 死依赖；layers/index.ts 84+ 成员扁平 god-facade 过渡债；useWorkflowState 前向引用
- **算法**：`_topological_sort` 死代码 + 并行快照浅拷贝（靠纪律防原地改）；动态导入 dunder 加固缺口 + 输出无大小限制；`TimeRange.granularity`→`step` bridge 丢失（疑似）；`output/__init__.py:795` 手写 `file://` URI 绕开 path_utils【疑似复发】；http sidecar 非原子写
- **杂项**：Matlab provider 硬编码 `C:\OSGeo4W`；start_fastapi docstring workers 默认值漂移；import_jobs_list limit 无上限；静默吞异常 3 处（cleanup_router/zonal_stats 空 except）；`Test/.pytest-*` 临时目录残留（P2-10 已知）
- **ruff 小项**：3× F401 未用导入 + 1× F541 无占位 f-string + 2× TYPE_CHECKING 注解缺导入（source_fetcher.py:696、shared/remote_sources/download.py:23，字符串注解无运行时风险）

## 六、去重与复发分析

**新增**：本轮绝大多数发现为新增（昨日/近日密集改动引入）。
**疑似复发 5 项**：D-1（storeToRefs 修复不完整）、E-6/E-7（非原子 JSON 写模式漏网）、E-11（file:// URI 同类漏网）、A-3（导入期绑定 settings 模式）。
**修复引入回归 2 项（本轮最重要发现）**：R-1/R-2 均为 49994c4（三专项 P1 批量修复）引入的运行时 NameError——**批量修复缺全量 lint 门禁**是直接根因，建议将 `ruff check` 纳入提交门禁（当前 pre-commit 未拦 F821）。
**已证伪排除**：compiler disabled 输出源"缺回归测试"指控不成立（`Test/backend/test_seed_disabled_output_source.py` 存在）；SSRF 13 处全走 safe_urlopen；31080a9 原 8 处穿越未见复发；前端 401 跳转/瓦片缓存无界/litegraph 清理等均复核无恙。

## 七、修复优先级路线图（建议另起修复轮）

| 批次 | 内容 | 预估 |
|------|------|------|
| 批次 1（立即） | R-1/R-2 两个 NameError 回归 + ruff 入提交门禁 | 小 |
| 批次 2（交付前） | B-1~B-4 路径穿越姊妹点（同一 safe_import_child 模式一次清）+ B-5/B-6 鉴权补齐 | 中 |
| 批次 3（交付前） | E-1 跨缝丢数据 + E-2 64 键接线 + D-1 storeToRefs codemod+lint 门禁 | 中 |
| 批次 4（可排期） | C-1 provider 覆盖广播 + A-2/C-2/C-4 多 worker 进程内态收敛 + B-7 500 文案 | 中 |
| 批次 5（ backlog） | 其余 P2/P3 | — |

## 八、正向确认（复核通过项）

异常收窄 PRD（33+19 处）已全部落地无残留；SSRF 防护（IP 钉死+逐跳重定向校验）、archive_safe 五重防护、secret_cipher fail-closed、deps/credential_resolver 鉴权链、双瓦片入口分离、配置四层治理链、omega_sf 守卫、FY TB 3D 通道抽取、前端 X-Api-Key 存储（sessionStorage 优先+无 v-html）——均质量良好，无需整改。

---

## 九、修复闭环（2026-08-22 晚 ~ 08-23 凌晨执行，全部已推送 origin/dev）

- **批次1 `1baa740`**：R-1/R-2 两个 F821 NameError 回归修复 + 其余 6 个 ruff 存量清零 + pre-commit ruff lint 扩面 `Code/shared`+`Code/backend/scripts`（原正则漏覆盖正是 49994c4 回归漏网之根因）。ruff 三目录 0 errors。
- **批次2 `dbbb4f6`**：B-1~B-4/B-8 穿越姊妹点统一收口 `safe_import_child`（vector 7 处/document/jobs/export_layer/resumable_upload）+ zip arcname 归一防 zip-slip；B-5 gee_config 四端点 router 级 `require_config_read_access` + 500 固定文案；B-6 zonal sync 补 `require_workflow_run_access` + `anyio.to_thread` 卸载 + 500 固定文案。新增 `Test/backend/test_path_traversal_guards.py` **42 条回归全绿**。
- **批次3 `7189e93`+`b766f7c`**：E-1 NetCDF 0-360 跨缝两段读+拼接（含全球环绕）；E-2 参数键 64 上限收口 `ProviderExecutionPayload.__post_init__`（构造期封顶，不再依赖 backend bridge 单点）；D-1 storeToRefs 26 调用点全量转 `toRef`（22 文件）+ eslint `no-restricted-imports` 永久禁令（负向探针验证生效）。**附带发现并已修 fdd6833 同类活雷**：`autoAttachProductsForNewLayer` 在 workflow-run-domain return 漏字段 → LayerSidebar 每次添加图层必 TypeError，三跳接线补齐 + 新增接线契约测试。前端 vitest 163 文件/1189 全过、eslint 0 错误、format:check 全绿。
- **证伪记录**：算法全量 4 个 native module 失败经 worktree HEAD~1 对照实验证明为**预存在顺序污染 + 今晚本机 ~/.geooutput 写入被防护拒绝**（环境性），与改动无关；测试隔离缺陷记入批次5待办。
- **批次4 `aa980b7`（C-1，最后一个 P1，2026-08-23 03:10）**：天气 provider 配置跨进程传播——`update/delete_weather_provider` 写库后 bump Redis 版本戳；`fetch_gateway` 取数入口（resolve/list_providers_for_layer）5s TTL 检查版本戳、变化即重放 DB 覆盖。多 worker（含 Celery）下禁用/换源 ≤5s 生效；Redis 不可用降级为原"重启生效"语义。新增 `Test/backend/test_weather_provider_cross_process.py` **6 条回归全绿**；天气相关套件 39 过（phase_a 4 error / frontend_compat 1 error 经 HEAD 对照实验证实为环境性写入拒绝，与改动无关）。
- **批次4 收官 `beb2c55`（A-2/C-2/C-4/B-7，2026-08-23 03:55）**：
  - **A-2 配置快照跨进程传播**（复用 C-1 版本戳模式，架构拍板：Redis 版本戳+TTL）：`upsert/delete/toggle_api_key` 写路径本进程 rehydrate 后 bump Redis 版本戳；`get_runtime_snapshot` 入口 5s TTL 检查、变化即 rehydrate——`get_backend_auth_key`（鉴权热路径）全进程 ≤5s 生效。Redis 不可用降级原语义。调研发现：`invalidate_effective_config` 系死代码（零调用，本进程也从不失效）；熔断器 registry 未接线且 docstring 已文档化为进程内有意设计（单 worker 防雪崩）——两者无需改码。
  - **C-2 瓦片并发槽配置化**（架构拍板：配置化+文档，弃 Redis 分布式信号量——上游为自托管 Open-Meteo 容器、429 风险低，分布式锁的续期/宕机释放复杂度不划算）：`weather_tile_max_concurrent`（env `BACKEND_WEATHER_TILE_MAX_CONCURRENT`，默认 6，重启生效）+ settings/tile_service 注释写明「总上游并发 = 槽位 × 持有进程数」。
  - **C-4 tile LRU 并发安全**：`OrderedDict` 缺锁（事件循环线程 × anyio/Celery 线程池并发读写 → `RuntimeError: mutated during iteration` 风险），统一 `threading.Lock` 保护读写/迭代/驱逐三处。
  - **B-7 四处 5xx 固定文案**：layer_router(500)/unified_tile_router(503)/weather_tile_routes(503)/config_routes(500+补日志) 不再回显 `str(exc)`；已验证前端对这四个 detail 字符串零消费（422 "no usable data" 前端契约未动）。
  - 新增 `Test/backend/test_batch4_cross_process.py` **11 条回归全绿**（版本戳 bump/TTL 节流/版本变化 rehydrate/Redis 降级×2/槽位配置×3/LRU 并发压测/驱逐正确性）；相关既有套件 65 过。
- **批次5 `76352c4`（P2 残留 + 高价值 P3 扫尾，2026-08-23 04:40）**——10 项，15 文件 +249/-43：
  - **算法侧**：E-3 raster_writer/output 补 `matplotlib.use("Agg")` + 三处 `plt.close` 入 finally（savefig 异常不泄漏 figure；pyplot 全局态并发留 backlog）；E-4 `_normalize_array` 启发式收紧——仅「末位像通道且首位明显不是」才转置，`(3,H,3)` 窄条带 band-first 不再误转置；E-5 cache_store tmp 名加 uuid（同进程并发写同 key 不互踩）；E-6/E-7 非原子 JSON 写三处（omega_sf checkpoint / async_jobs snapshot / job_queue pending）统一 tmp+`os.replace`（截断崩溃不再产生坏文件；omega_sf O(n²) 全量重写留 backlog）。
  - **后端**：A-3 `_resolve_output_root()`——production 空 `BACKEND_OUTPUT_ROOT` fail-fast 拒启，dev CWD 兜底改显式 warning（测试 conftest 已自设值不受影响）；A-4 start_fastapi 生产守卫——非 development 一律禁用 reload（`BACKEND_RELOAD=true` 不再影响生产）+ docstring 默认值漂移修正；B-9 cleanup_router terminal 统计吞错改 `logger.exception`（zonal 两处已于批次2 清）。
  - **C-6（工作版）**：NodeSpec 加 `enabled: bool = True`（此前 pydantic 丢 extra）+ executor 循环跳过停用节点（compiler 侧 enabled 剥离/输出过滤/悬挂边剔除本已完备，仅缺执行侧）——新增 `test_workflow_engine_disabled_node.py` 4 条回归（跳过/默认值/全停用/spec 解析）。
  - **前端**：D-3 `_http.ts` 外部 signal 与超时 signal 经 `AbortSignal.any` 组合（带特性检测降级），传 signal 时超时不再静默失效；D-2 teardownBinder 补 drawModule/measureModule 的 dispose 调用（叠加 canvas 的 RAF/ResizeObserver 卸载泄漏，3D 视图切换重建即触发）。
  - 验证：算法相关 23 过 + 新增 4 过 + paths/cleanup 回归 49 过；前端 vitest 1189/1189 + build 成功；ruff lint/format 全绿。
- **未入本轮（留 backlog）**：C-3 派发不确定 / C-5 sync 孤儿（疑似，需集成测试设计）；D-4 GeoJSON 深响应式（大改造）；pyplot 并发隔离；omega_sf checkpoint 增量化；P3 摘要其余（分层异味/限流前缀/god-facade/Worker 化等）；native 测试隔离修复（N-8 家族）。
- **backlog 推进（2026-08-23 04:42~05:15）**：
  - **测试基线根治 `4d1ec88`**：所谓"顺序污染/预存失败"全部证伪——真因是会话 481KB `ACC_PRODUCT_CONFIG_V3` 巨型 env（patch.dict 写回炸 Windows 32K 上限，波及 cds/storage/grib 10 项）。两处 conftest 固化防御（剔除 >32760 字符变量），算法全量 **670/670 全绿**。
  - **C-3 派发确定性 `91aff8a`**：确认问题真实存在——`apply_async` 8s 限时只放弃等待不撤销投递（慢 broker 迟到消息）+ Beat 2min 重派无 task_id 的 queued run → 同 run 双消息；worker 端 running 重投原逻辑"警告后从头执行"→ 与原 worker 并发。修复 = `execution_lock.py`（Redis `SET NX` 执行锁：双消息跳过/持有者同机 pid 探活死亡则 CAS 接管保留 acks_late 语义/Lua 值匹配安全释放/Redis 故障 fail-open；TTL=7800s 兜底）。新增 `test_workflow_execution_lock.py` **15 条回归**（互斥/接管/接管竞态/跨机保守/释放语义×2/降级×2/探活×3/集成场景×3）；submission 相关既有 39 过。C-5 仍留 backlog。
  - **C-5 sync 孤儿 `fa1ac55`**：docker 实验双重实证——(1) `subprocess.run` 超时/客户端被杀后 run 容器在 daemon 继续 Up（孤儿真）；(2) 报告建议的 `docker compose stop/rm <service>` 对 run 创建的容器**无效**（证伪，compose 只管理 up 容器）。修复 = `kill_orphan_sync_containers()`：按 compose project/service label `docker ps -q` 定位 + `docker rm -f`；两个调用点均在持锁状态（互斥锁保证不误杀进行中的合法 sync）——超时路径先清孤儿再抛错（finally 释锁前），每次 sync 拿锁成功后防御性清理残留（覆盖 worker 被 hard time_limit 杀的场景）。新增 `test_open_meteo_sync_orphan_cleanup.py` **7 条回归**；既有 sync 套件 27 过。**backlog 仅剩 D-4 与 P3 摘要项**。
  - **D-4 GeoJSON 深响应式 `ebebef1`（2026-08-23 05:55）**：调研证实改造条件成熟——更新链已全部"不可变替换 + revision 驱动"（渲染模块 `dataRef !== geojson` 引用比较；全 src 零就地修改 geojson 命中；draw-store 的就地更新走独立 deep watch + 整体替换管道）。最小安全改造 = **只豁免 geojson 子树**：`buildImportedVectorPayload`（导入/绘制草稿/hydrate 恢复三入口共用）与 `updateImportedVectorGeojson` 替换点两处 `markRaw(geojson)`，activeLayers 容器保持深 ref 不动（`importedVector` 对象本身仍 proxy，geojson 引用替换照常触发响应）。百万级 features/coordinates 不再逐个建 Proxy。新增 `imported-vector-markraw.test.ts` **5 条回归**（raw 豁免/深容器内子树依然 raw/引用替换触发 computed/style 字段仍响应/toRaw 引用相等）；前端全量 **vitest 1194/1194 + build 成功**。**P2 全部清零，审查 backlog 仅剩 P3 摘要项**。
  - **P3 小件清扫 `7211436`（2026-08-23 07:15）**：9 项修复 + 2 项证伪 + 2 个 flaky 测试根治——(1) `/runtime/status` redis_url 脱敏（密码 `***`）+ stats 10s TTL（原每次全量 SCAN）；(2) `handle_workflow_timeout` 限时值对齐 per-task 实际 7200s（原引用全局默认 300s，文案错位）；(3) 写限流补 `/workspace` `/analysis` 前缀；(4) **算法包 output 17 处手写 `file://` 全量收口** `local_path_to_uri`（CC-4 复发漏网，Windows 下 `file://D:\` 非法 URI）；(5) http sidecar 原子写（tmp+os.replace）；(6) `_topological_sort` 死代码清除；(7) zonal seed 加载失败日志 debug→warning；(8) papaparse 死依赖移除（src 零引用）；(9) 顺带根治两 flaky——`test_analysis_panel` 残留 `raster_register.IMPORTS_DIR` patch（批次2 路径收敛后属性已不存在）、`test_config_security` 的 `_maybe_refresh_snapshot` TTL 过期后 Redis 版本戳 rehydrate 覆盖 monkeypatch 的 `_snapshot`（单跑必败的顺序敏感）。**证伪跳过 2 项**：OSGeo4W 已是环境变量探测非硬编码；export 路由不存在（报告笔误）。后端全量 1637 过、算法 670 过、前端 build 过。**P3 剩余仅大活**：pyplot 并发隔离、omega_sf checkpoint 增量化、shpjs Worker 化、god-facade/分层异味、fetch_gateway 宽 except（行为变更需数据验证）。
  - **P3 pyplot 并发隔离 `65057e0`（2026-08-23 07:55）**：确认风险真实——pyplot 全局状态机（figure 栈）+ 工作流节点级线程并行（executor ThreadPoolExecutor 拓扑分层）= 同层两个出图节点竞态（串图/关错图/"figure has been closed"）。修复 = 新模块 `viz_lock.py`（进程内 `threading.Lock` + `@locked_plot` 装饰器，覆盖创建→绘制→保存→close 全程；多 worker 进程天然隔离故只需进程内互斥；出图毫秒~秒级串行化代价可忽略）。**五个出图入口全覆盖**：`visualization.py` 4 个 plot 方法（装饰器）、`viz_chart_generate._maybe_write_png`（装饰器）、`output._generate_preview_from_array`（装饰器——RasterPublisher 内部调用它，只锁叶子防嵌套死锁）、`raster_writer.generate_bytes`（装饰器）、`gis_ops` contour 出图段（`with plot_lock:` 只包 pyplot 段，等值线几何计算留锁外保持并行）。新增 `test_viz_lock.py` **3 条并发回归**（锁内仅见自有 figure/双线程互斥证据/串行化时序证据）；算法全量 **673/673**。**P3 剩余**：omega_sf checkpoint 增量化、shpjs Worker 化、god-facade/分层异味、fetch_gateway 宽 except。
