# CGDA 全面代码审查问题清单（2026-08-15）

> 触发：`/plan 熟悉项目内容，并起草全面代码审查计划` → 执行模式升级为**审查 + 修复**（用户指令）。
> 范围：域 A 安全专项 / 域 B 后端核心与并发 / 域 C 前端状态与可维护性 / 域 D 基础设施与治理。
> 策略：P0/P1 最小变更修复（TDD，红→绿），P2/P3 仅留痕不修复。
> 配套报告：`Docs/08-HTML报告/comprehensive-code-review-2026-08-15/comprehensive-code-review-2026-08-15.html`。

---

## 1. 基线与终态（回归验证）

| 检查 | 基线（T1） | 终态（T11，第一轮 22:5x） | 终态（R2，第二轮 08-16 0:xx） |
|------|-----------|--------------------------|------------------------------|
| 后端 pytest（Test/backend） | 13 failed + 12 errors / 1164 passed | **1193 passed, 2 skipped, 0 failed / 0 errors**（含 2 个新增回归测试） | **1217 passed, 2 skipped, 0 failed / 0 errors**（281.89s；净增 24 = +19 加密单测 +10 并发加固 −5 随 legacy 路由消亡；期间修复 1 个测试顺序依赖，见附表 3） |
| 算法 pytest（Test/algorithms） | 通过 | **411 passed + 28 subtests，全绿** | **411 passed + 28 subtests，全绿**（63.03s） |
| 前端 vitest（串行） | 130 文件 / 733 全绿 | **131 文件 / 736 全绿**（含 3 个新增测试；10 文件因满载机器 worker 启动超时改单独批次验证通过，属环境性非测试失败） | **131 文件 / 736 全绿**（主批 121 文件/663 + 10 文件单独批次 73；A2 迁移改动复验，无新增测试文件） |
| 前端 lint | —（基线未跑） | 0 errors / 2 warnings（既有：`MultiOverlayBarChart.vue` no-explicit-any ×2，非本次改动） | **0 errors / 2 warnings**（既有，未变化） |
| 前端 build | —（基线未跑） | ✓ built in 6.1s | **✓ built in 5.79s** |
| check:openapi | 零漂移 | **OK**（未改任何 Pydantic schema） | **OK**（A2 重生成后零漂移，190 paths） |
| check:catalog | 同步 | **OK**（55 items + 7 categories） | **OK**（55 items + 7 categories） |

> 环境注记：本机测试期间存在 45 node + 21 python 进程（运行中服务栈），三组测试并行会触发 basetemp `WinError 5` 与 vitest worker 启动超时；全部改为串行 + 独立 basetemp（`%TEMP%\cgda-*`）后稳定。

---

## 2. 本次修复清单（P1，全部红→绿验证）

| # | 域 | 严重度 | 问题（文件:行） | 根因与修复 | 验证 |
|---|----|--------|----------------|-----------|------|
| F1 | B | **P1 产品缺陷** | `app/data_io/services/archive_safe.py`（7z `l -slt` 解析） | `7z l -slt` 首块 `Path = <压缩包绝对路径>`（包自身头）未被过滤，basename 比对必然失配 → 合法 .7z 误拒「非法压缩包路径」；CI 无 7z CLI 被 skip 掩盖。修复：新增 `_parse_7z_slt_listing`，`----------` 分隔符后才开始收集条目 | `test_archive_safe.py` 22/22（新增 2 回归：绝对路径头、相对头+Folder） |
| F2 | C | **P1 产品缺陷** | `Code/frontend/src/data-manager/core/api.ts`（`writeFetch`） | data-manager 独立 HTTP 层缺 401 会话过期处理（与 `services/_http.ts` 双轨不一致）→ 会话过期后静默失败、不跳登录。修复：401（非 `/auth/*`）→ `handleSessionExpired` + 抛 `SessionExpiredError`；分块上传立即中止、无重试风暴 | 新增 `Test/frontend/data-manager/core/api-401.test.ts` 2 例（红→绿）；相关 7 文件 21 测试全绿 |
| F3 | B/D | P1 测试漂移 | `Test/backend/test_weather_coverage.py`、`test_weather_engine_settings_phase_a.py`（12 ERROR） | 测试桩引用 RBAC v2（`10b7eb1`）已移除的 `weather_router.get_redis_client`。修复：fixture 改从真源 `app.services.weather_coverage_cache` 取 `get_redis_client`/`COVERAGE_REDIS_PREFIX`，断言零改动 | 19/19 通过 |
| F4 | B | P1 测试漂移 | `Test/backend/test_frontend_call_simulation.py`（3 failed + 1 单文件运行序） | 匿名调用 workflow 端点遇 fail-closed 401（鉴权语义已收紧，测试未跟）；另 tile 注册表重建顺序问题。修复：新增 `_admin_cred()` 显式携带有效凭据（不绕过 fail-closed）；tile 503 测试先 `create_app()` 再插入失败 provider | 30/30 通过 |
| F5 | B | **P1 系统性产品缺陷** | `app/main.py`（`validation_exception_handler`） | `exc.errors()` 中 model_validator 抛出的 `ValueError` 对象不可 JSON 序列化 → 422 变 **500**；`workflow_engine/models`、`data_io/router`、`import_router` 3 处生产 validator 均可触发。修复：改用 `jsonable_encoder(exc.errors())`；顺带对齐 2 处测试断言与现行 openapi 契约（均 422） | 新增回归测试（红→绿）；`test_import_raster_crs`+`test_error_handlers`+`test_crs_detector` 62/62；openapi 零漂移 |
| F6 | B | **P1 架构缺陷** | `app/services/dataset_registry_service.py`、`workflow_request_resolver.py` | **settings split-brain**：`from app.core.config import settings` 导入期绑定；测试用 `monkeypatch.setattr("app.core.config.settings", replace(...))` 换对象时，服务模块首次导入落在 swap 窗口即永久持有临时对象 → 此后属性补丁全部无效（sync=45≠1 等 6 failed，另污染真实 `.data` 注册库判读）。修复：两模块改动态读 `config.settings.X`（与 credential_resolver/deps 既有约定一致） | 新增回归测试 `test_rescan_reads_current_settings_object`（红：`11 == 1` 与全量失败同值 → 绿）；26/26 通过 |
| F7 | B | P1 测试隔离 | `Test/backend/test_workflow_request_resolver.py`（1 failed，数据盘漂移） | 依赖机构数据盘 `<DATA_ROOT>/Soil_Moisture/FY3D\|FY3B` 真实存在性（`I:\test` 下已不存在）。修复：tmp 数据根自建全部候选目录 + `object.__setattr__` 补丁 + 前后 `invalidate_template_cache()` 清 lru_cache 双向泄漏 | 5/5 通过 |
| F8 | A | **P1 功能断链** + P2 安全 | `Code/infra/gateway/nginx.conf`、`Code/frontend/vite.config.ts` | 网关与 Vite 均未代理 `/api` 前缀 → remote browser（`/api/remote/list\|test\|servers`，RemoteDirBrowser/SshSyncForm）两条链路全断（返回 index.html）；网关无 CSP 等安全响应头。修复：`api` 加入 nginx 反代白名单 regex；server 级新增 `Content-Security-Policy`（内联主题脚本/运行时样式约束下保留 `'unsafe-inline'`；img-src https: 支持外部瓦片；worker-src blob: 支持 MapLibre）+ `X-Content-Type-Options: nosniff` + `Referrer-Policy`；vite 补 `/api` 代理 | nginx -t 通过（配置语法）；前端 build OK |
| F9 | C | P3 遥测失真 | `stores/weather-tile-manager.ts`（`clearLayer`） | 漏清 `viewportFillStartedAt` → 同 id 图层重建继承旧填充起点，viewport-fill 指标失真（实测可达 60000ms）。修复：补 `viewportFillStartedAt.delete(layerId)`；回归测试以可中止 fetch mock + `performance.now` spy 复现（红）→ 修复后 <30s（绿） | weather-tile 9 文件 43/43 通过 |

---

## 3. 留痕问题清单（P2/P3，本次不修复）

| # | 域 | 级 | 问题 | 位置 | 处置建议 |
|---|----|----|------|------|---------|
| N-1 | A | P2 | 5 个凭据仓库各自内联 AESGCM 加解密（重复实现，密文格式/错误处理可能漂移） | `api_keys_repository.py:117`、`gee_credentials_repository.py`、`portal_credentials.py`、`remote_storage_credentials_repository.py`、`weather_providers_repository.py` | ✅ **已闭环（W-A1，见 §7.1）**：收敛到 `app/services/secret_cipher.py`，密文格式零变化 |
| N-2 | A | P2 | remote browser 新旧权限模型不统一：legacy `/api/remote/*` 走 router 级 `require_write_access`，profile 走 `/config/remote-storage/{id}/browse` read 权限 + profile 校验 | `remote_browser_router.py:86-89` vs `config_routes.py:677` | ✅ **已闭环（W-A2，见 §7.2）**：legacy 路由整体下线，前端迁移到 `/config/remote-storage/*` |
| N-3 | A | P2 | `X-Api-Key` 由操作员运行时写入 localStorage（XSS 可读取；CSP 已补但 `unsafe-inline` 仍在） | `src/services/backend-auth.ts:29` | 中期：写操作迁会话 Cookie + HttpOnly；近期：收紧 CSP inline 白名单 |
| N-4 | C | P2 | 双 HTTP 层并存：`services/_http.ts` 与 `data-manager/core/api.ts` 行为需人工对齐（本次 401 已对齐一处） | 两文件 | 401/超时/重试语义合并为共享内核或显式差异文档 |
| N-5 | C | P2 | 前端覆盖率门偏低（lines 22% / branches 16%），鉴权、地图、天气调度关键路径覆盖不足 | `vitest` coverage 配置 | 按域分阶段提门槛（先 30%），优先补鉴权与 weather-tile-manager |
| N-6 | B | P2 | SQLite 单写者 + Windows Celery `solo` 池退化（无任务级并行），并发上界受部署平台约束 | `_sqlite_pool.py`、`celery_app.py` | 部署目标定 Linux + prefork；或评估 Postgres 迁移时机（与 L-14 同源） |
| N-7 | B | P2 | `_LOCAL_SYNC_JOBS` 模块级内存 dict，多进程部署不共享（当前单 uvicorn 进程下影响有限） | `weather_sync_service.py:34` | 若 FastAPI 转多 worker，需迁 Redis；当前留痕 |
| N-8 | D | P2 | `Test/.pytest-be*` basetemp 残留目录 Windows 下锁定（`WinError 5`），叠加本环境删除策略后无法清理，导致后续 run ERROR | `Test/.pytest-be`（本次已绕行 `%TEMP%\cgda-*`） | 本地手动（管理员）清理；CI 无此问题 |
| N-9 | D | P3 | CI actions 全部 tag 引用未 pin sha（`checkout@v4` 等 8 处）；requirements pin 混用 `==`/`>=` | `.github/workflows/ci.yml`、`requirements*.txt` | 供应链加固：pin 到 commit sha + 全量 `==` |
| N-10 | D | P3 | coverage 门偏低（后端 52%）+ 4 个 CI job 重复 `pip install`/起 redis | `.github/workflows/ci.yml` | 合理化 job 复用；后端门分阶段提升 |
| N-11 | B/C | P3 | confirm 端点 Mercator 往返 ~0.013° 偏差（08-07 遗留，本次未复检数值） | `import_router.py`（`/import/raster/confirm`） | 复检数值后决定是否重设计 bounds 语义 |
| N-12 | C | P3 | 时间轴 coverage 时区一致性（08-07 遗留；timer 域已显式 `TIMER_TZ = Asia/Shanghai`，timeline 域未复检） | `workflow_timer_service.py:41`（已闭环部分） | 探针改统一显式时区断言 |
| B-N1 | B | P2 | `_LOCAL_SYNC_JOBS` 本地降级路径无锁遍历+插入，并发记录 job 可触发 `RuntimeError: dictionary changed size during iteration`（罕见） | `weather_sync_service.py:49-65` | 加模块级 `threading.Lock` 或改 `copy` 后遍历（随 N-7 一并处理） |
| B-N2 | B | P2 | 并发双 retry 同一 run 复用同一 `reuse_output_dir` → 产物缓存块并发写风险 | `retry_dispatcher.py:70-76`、`reuse_cache.py` | retry 输出目录加代次子目录或终态 CAS 检查（需设计，超出最小变更） |
| B-N3 | B | P2 | sync 锁按域集合整体互斥，部分重叠集合（`sync:a` vs `sync:a,b`）仍可并发写共享域文件（B-R3 已消灭顺序漂移，子集重叠仍在） | `open_meteo_sync_tasks.py:43` | 改为按单域加锁（all-or-nothing），或文档化「域集合需显式不重叠」 |
| B-N4 | B | P3 | event 定时器并发 emit 同一事件 → 匹配 timer 各自提交一次（无 claim）；语义上可辩护为事件语义 | `workflow_timer_service.py:871-911` | 如需 exactly-once 再引入 claim；当前留痕 |
| B-N5 | B | P3 | SQLite 池 `_pool.get()` 无界阻塞（连接泄漏时请求线程永久挂起）；`close_all` 只关空闲连接 | `_sqlite_pool.py:125` | 获取加超时 + 泄漏自愈；低概率 |
| B-N6 | B | P3 | API 槽位 TTL 60s 内持槽调用未完成时计数漂移 → 瞬态 over-admission；释放 DECR 为负时重置 0 会抹掉其它持有者 | `redis_client.py`（acquire/release） | 长调用场景评估延长 TTL 或改锁语义 |
| B-N7 | D | P2 | `test_weatherengine_service.py` 单文件运行需 `PYTHONPATH=Code/algorithms/providers/Python`（COG 导入链 `publish/__init__ → output_manager → path_utils` 依赖运行时路径注入）；全量套件下靠其它测试的 sys.path 污染才通过 | `weather_render_service.py:544`、`Test/backend/conftest.py` | conftest 补该路径，或 `raster_writer` 改相对导入 |
| B-N8 | D | P2 | 双 `algorithms` 包名遮蔽：`Test/algorithms/conftest.py` 将 `Code/algorithms/providers/Python` 插到 sys.path[0]，其本地 `algorithms` 包遮蔽 `Code` 根的 `algorithms.providers` → **Test/backend 与 Test/algorithms 不可在同一 pytest 会话收集**（22 文件 ImportError）；分开跑正常 | `Test/algorithms/conftest.py:35` | conftest 改 append 或去除 provider 本地包与顶层同名；CI 已分开跑故未暴露 |

---

## 4. 遗留清单终态矩阵（L-1 ~ L-15）

| # | 遗留项 | 终态 | 对应本次条目 |
|---|--------|------|--------------|
| L-1 | Layers store 拆分边界（`bindings.ts` 可变共享） | ⚠️ 仍存在，耦合可控 | —（P2 留痕，未单列） |
| L-2 | confirm Mercator 往返偏差 | ⚠️ 未复检 | N-11 |
| L-3 | timeline coverage 时区 | ⚠️ 未复检（timer 域已显式时区） | N-12 |
| L-4 | sync 无全局互斥 | ✅ **已闭环**：三入口共用锁 + B-R3 锁键归一化（消除顺序漂移漏互斥）；子集重叠见 B-N3 | §7.3 |
| L-5 | `_LOCAL_SYNC_JOBS` 内存态 | ⚠️ 仍存在 | N-7 / B-N1 |
| L-6 | `_store_path_manifest` ×4 重复 | ✅ **已闭环**（代码 0 匹配，已重构消失） | — |
| L-7 | remote browser 权限模型不一致 | ✅ **已闭环（W-A2）**：legacy 路由下线，统一 profile 体系 | §7.2 |
| L-8 | 双 AESGCM 实现 | ✅ **已闭环（W-A1）**：5 处内联收敛到 `secret_cipher.py` | §7.1 |
| L-9 | demo 可浏览目录结构 | ✅ 已闭环：browse 端点带 read 权限模型（`config_routes.py:677`）；交付前仍按 delivery-checklist 复核 | — |
| L-10 | 后端测试既有漂移失败 | ✅ **本次全部修复**（F3/F4/F5/F6/F7） | §2 |
| L-11 | basetemp junction 锁死 | ⚠️ 仍存在且本次复现（WinError 5） | N-8 |
| L-12 | CI pin sha / requirements / coverage 门 | ⚠️ 仍存在 | N-9/N-10 |
| L-13 | localStorage key + CSP + v-html | ◐ 部分闭环：CSP 本次已补（F8）；localStorage 设计仍在 | N-3 |
| L-14 | SQLite 单写者 + solo 池 | ⚠️ 仍存在（部署平台约束） | N-6 |
| L-15 | 仓库卫生（nul / .pytest_cache / 大日志） | ✅ **已闭环**（三项均不存在，`.data` 无 log 残留） | — |

---

## 5. 四域结论

| 域 | 修复前 | 修复后 | 依据 |
|----|--------|--------|------|
| A 安全专项 | ❌ P1 | ✅（余 P2：N-1/N-2/N-3） | CSP/代理断链修复（F8）；remote browser 实际有写权限门（复核降级）；L-9 闭环 |
| B 后端核心与并发 | ❌ P1 | ✅（余 P2：N-6/N-7） | validation 500 系统性缺陷（F5）与 settings split-brain（F6）修复；7z 解析（F1）修复 |
| C 前端状态与可维护性 | ❌ P1 | ✅（余 P2：N-4/N-5） | 401 会话过期对齐（F2）；遥测失真（F9）修复 |
| D 基础设施与治理 | ⚠️ P2/P3 | ⚠️（余 P2：N-8；P3：N-9/N-10） | 测试漂移 5 项修复；L-15 卫生已闭环；L-11 环境项仍在 |

## 6. 分级统计

| 级别 | 本次新增发现 | 已修复 | 留痕 |
|------|-------------|--------|------|
| P0 | 0 | — | — |
| P1 | 12（F1-F9 + B-R1~B-R3） | **12（100%）** | 0 |
| P2 | 13（N-1~N-8 + B-N1~B-N3、B-N7、B-N8） | 0（按策略不修；N-1/N-2 经 §7 专项闭环） | 13 |
| P3 | 8（N-9~N-12 + B-N4~B-N6） | 0 | 8 |

遗留终态：15 项中 **9 项闭环**（L-4、L-6、L-7、L-8、L-9、L-10、L-13 部分、L-15 及 L-5 降级注记）、4 项仍存在（并入 N/B-N 系列留痕）、2 项未复检（N-11/N-12）。

---

## 7. 增补：安全收敛 + 并发专项（2026-08-15 第二轮，W-A / W-B）

> 触发：`/plan ①（安全收敛）需要做；研究并行资源抢占、冲突、缓存覆盖等问题和相关bug。Use plugin: brooks-lint`
> 计划文档：`.trae/documents/2026-08-15-security-convergence-and-concurrency-hardening.md`。

### 7.1 W-A1 AESGCM 收敛（闭环 N-1/L-8）

- 新建 `Code/backend/app/services/secret_cipher.py`：`aesgcm_encrypt/aesgcm_decrypt` 原语 + `encrypt_secret/decrypt_secret` 统一三分支语义（无 key / ImportError / generic）；`rg "from cryptography"` 全仓仅此一处。
- 五仓库薄壳化（保留各自密文格式与列映射，零数据迁移、零格式变化）：⑤ `weather_providers_repository` → ① `api_keys_repository` → ④ `remote_storage_credentials_repository` → ② `gee_credentials_repository`（保留 `v1:` 前缀）→ ③ `portal_credentials`（保留 `iv="plain"` 标记）。
- 唯一行为变化：③④ 的异常路径纳入统一三分支（dev 明文回退、非 dev RuntimeError fail-closed）。
- 验证：`test_secret_cipher.py` 19 例 + `test_credential_roundtrip.py` 7 例 + `test_secrets_encryption.py` 5 例全绿。

### 7.2 W-A2 legacy remote browser 下线（闭环 N-2/L-7）

- 后端：删 `remote_browser_router.py` 及 `main.py`/`routers/__init__.py` 注册；删 `test_remote_browser_router.py`（5 例随路由消亡）；`openapi.json` 重生成（190 paths）。
- 前端：`RemoteDirBrowser.vue` → `browseRemoteStorage(profileId, path)`；`SshSyncForm.vue` → `testRemoteStorageProfile`，403 提示「仅管理员可测试连接」；`api-contracts.ts` 清 `/api/remote/servers` 声明。
- 验证：vitest 相关文件全绿 + `check:openapi` 零漂移；`rg "/api/remote"` 前后端源码零命中。

### 7.3 W-B 并发专项（brooks-lint：审查 8 面 → P1 修复）

**B1 定级**（8 面全读源，判据见计划文档 §2）：P1 ×3、P2 ×3、P3 ×2（详见计划文档 B1 表）。

**B2 修复（逐项 TDD 红→绿，`Test/backend/test_concurrency_hardening.py` 10/10）**：

| # | 问题（P1） | 修复 | 验证 |
|---|-----------|------|------|
| B-R1 | `redis_client.py` API 槽位 INCR+EXPIRE 非原子：INCR 后崩溃 → 计数器无 TTL 永久滞留 → 槽位慢性泄漏直至池耗尽（仅 `flush` 可救） | 新增 `_ACQUIRE_SLOT_LUA`：单次 eval 完成 INCR + TTL 补挂；`TTL==-1`（孤儿键）也补挂自愈 | 原子性 / 限额 / 孤儿补挂 3 测试 |
| B-R2 | `weatherengine/client.py` 缓存固定 `.tmp` 名：Redis 降级时 dedup 放行 → 并发写同一 tmp；实测 Windows 并发 `os.replace` 同目标报 PermissionError(13) | `unique_cache_tmp_path`（pid+tid+uuid8）+ `replace_with_retry`（指数退避×5），两处写点接入 | 唯一性 + 8 线程并发 replace 无异常、终态合法 JSON、无 tmp 残留 |
| B-R3 | `open_meteo_sync_tasks.py::_sync_lock_key` 原样拼 domains：`a,b` 与 `b,a` 不同锁 → 同域集合并发 sync 写同一 Docker volume（缓存覆盖根源） | 锁键归一化（排序+去重+去空白） | 参数化 4 组等价类 + 空/None 回退 |

**新增留痕**：B-N1（sync job dict 无锁迭代）、B-N2（retry reuse_output_dir 双跑）、B-N3（sync 锁子集重叠）、B-N4~N6（event 语义/池阻塞/槽位漂移）、B-N7（weatherengine 单文件运行需 PYTHONPATH）、B-N8（Test/backend 与 Test/algorithms 不可同会话收集——`algorithms` 包名遮蔽，分开跑正常）。
