# 工程收口仪表盘（现状 SSOT 入口）

最后更新：2026-08-12

本文件取代对 `项目任务清单.md`（2026-07-06）中已自相矛盾段落的依赖。  
**现状事实源优先**：`Code/backend/README.md`、`Code/docs/双通道接口设计总结.md`、`.ai/docs/design/工程决策纪要-配置瓦片与契约.md`、`.ai/docs/reference/天气渲染进度同步-2026-07-21.md`。  
**待完善排期 SSOT**：[`.ai/progress/2026-08-04-pending-tasks-audit.md`](../../progress/2026-08-04-pending-tasks-audit.md)。

## 已锁定原则

| 主题 | 结论 |
|------|------|
| 瓦片面 | 天气 `/weather/tiles`（可 `provider` 钉源）；底图 `/unified-tiles`；管理 `/runtime/tiles/*` |
| 配置真源 | env 冷启动 + DB 覆盖；出网走 `get_effective_*` / EffectiveSecrets |
| 事件 | JSON 轮询（不上 SSE） |
| Catalog | 后端 `/layers` 权威 |
| 算法 | Python package 为主；`lab_output` = sample |

## Phase 收口勾选

### Phase 0 — 安全与配置

- [x] `/config/*` / `/import/*` mutating 鉴权
- [x] `backend_auth` 走 EffectiveSecrets
- [x] 非 development 禁止明文密钥落盘
- [x] Runtime PATCH 幽灵 key 清零；`task_executor` / weather TTL 接线
- [x] 执行器与 Celery worker 名实不符时 status 标红

### Phase 1 — API / Legacy / 契约

- [x] Legacy workflow 默认关闭（`BACKEND_LEGACY_WORKFLOW_HANDLERS_ENABLED`）
- [x] `/demo/*` 默认 404（`BACKEND_DEMO_ROUTES_ENABLED`）
- [x] `POST /frontend/commands` → 410
- [x] OpenAPI drift check（`scripts/check_openapi_drift.py` / `npm run check:openapi`）
- [x] 双通道文档与瓦片叙事对齐

### Phase 2 — 前端领域

- [x] 天气能力静态白名单 + catalog reconcile
- [x] 仅天气层进入 tile manager
- [x] StatusPanel 作业与瓦片统计分离
- [x] Overlay dispose / merge cache / watch hash 收敛
- [x] 移除 lab-output 无 catalog 豁免
- [ ] Layers god store 完全拆分为多 store（**进度 2026-08-10**：已抽出 weather-session / weather-viewport / workspace-persist 纯函数，以及 **catalog-runtime / active-layers / run-layers / workspace-hydrate / weather-reconcile** 切片；`index.ts` 仍为组合入口）

### Phase 3 — 算法 / 文档 / 观测

- [x] lab_output 文档与 catalog 标明 sample
- [x] HTTP/MinIO materialize 不再返回假 deferred-ready
- [x] Redis `KEYS` → `SCAN`；API slot 本地有限降级
- [x] 底图 cache LRU + TTL 读 settings
- [x] 本仪表盘与决策纪要
- [ ] 选定一条真实课题组数据端到端门槛（需数据环境，见 `Code/docs/真实数据e2e门槛.md`；**实机 NAS 绿测仍待**）
- [x] NAS SMB/SFTP 产品化路径：`BACKEND_REMOTE_LAYER_DATA_URIS` / `BACKEND_REMOTE_MAX_BYTES` / 设置页对象 URI 测试 / e2e 文档 B 节（**实机 NAS 绿测仍待数据环境**）

### 远程存储扩展（2026-07-16 → 2026-07-17）

- [x] shared `remote_sources`（sftp/smb/ftp/gs）+ 加密凭证库 + `/config/remote-storage`
- [x] data_access + source_fetcher 双栈接线；`s3://` 与 Python 对齐
- [x] 设置页「远程存储」Tab；resolver 远端就绪语义
- [x] 说明文档：`.ai/docs/specs/远程存储接入说明.md`
- [x] 图层远端 URI 环境注入（不覆盖本地候选）+ 可配置下载上限 + 多机凭证说明

### 天气渲染几何与商业化视觉（2026-07-20 → 2026-07-21）

- [x] 全球对齐格网 + 半开瓦片格心归属（前后端一致）
- [x] 取消「格元裁进瓦片框」（消除横/纵空隙）；保留半开裁点防叠框
- [x] 父级 z 仅空洞垫底；合并缓存带覆盖签名
- [x] 风粒子 WebGL/Canvas 密度与拖尾；标量不透明度 / 降水 alpha
- [x] WebGL 标量 LUT + 时次交叉淡入 + 气压等压线
- 进度正文：`.ai/docs/reference/天气渲染进度同步-2026-07-21.md`；规范：`.ai/docs/specs/规范文档.md` §15.6

### 2026-07-21 之后里程碑

- [x] FY/SMAP 算法链路与 Matlab 对照 + UI 闭环（见 `.ai/progress/fy-smap-progress-tracker.md` / `2026-08-04-fy-smap-ui-closed-loop.md`）
- [x] 工作流调度 P0：cancel 旗标路径一致 + reuse_output_dir 多级解析（见 `.ai/docs/reference/workflow_scheduling_audit_report.md`；P1 仍开）
- [x] `Test/` 测试集中化（见 `.ai/progress/2026-08-04-test-reorganization.md`）
- [x] 后端 `Test/backend` 全绿：489 passed / 0 failed（shim 约定 + 本地 unrar + 测试对齐；见 `.ai/progress/2026-08-04-backend-32-fixes.md`）
- [x] Open-Meteo Phase B 前端模型贯通（`weather-engine` store + tile-manager / Dashboard）
- [x] Open-Meteo Phase C Sync 运维可观测（last_sync / trigger domains / sync_unavailable）
- [x] Open-Meteo Phase D 测试与回归（coverage/sync API/FE model 单测 + API 烟测；UI 风场可选）
- [x] FY/SMAP UI 人工全流程验证（见 `.ai/progress/ui-verification-steps.md`；导出点击命中待跟进）

### 2026-08-12 P2 快速项收口

- [x] **R2** 断路器 threshold 调参：代码已修复（`redis_client.py` threshold 1→3 + 指数退避 30→60→120s；集中式断路器默认 5；Open-Meteo 断路器 10）
- [x] **D-1** Task* 死契约：旧 Task* 模型已从 `api_contracts.py` 完全清除，`/tasks` 路由已从代码和 openapi.json 中移除
- [x] **D-2** OpenAPI security schemes：`main.py` 补充 `SessionAuth`（cookie）+ `BearerAuth`（HTTP Bearer）声明；`openapi.json` 已重导
- [x] **N-3** hPa 字段单位语义：`api_contracts.py` WeatherPointCurrent 9 个 hPa 字段添加 `Field(description=...)`（m/s, °, °C）
- [x] **N-4** 时间戳类型化：新增 `OpenMeteoSyncStatusResponse` 模型（`finished_at: datetime | None`）+ `response_model` 接线；`workflow_router.py` `str()` → `strftime()`
- [x] **L2** Ghost uvicorn 修复：`launch/commands.py` `cmd_stop()` pattern 列表补充 `spawn_main` + `uvicorn`

## 后续平台项（非本期）

- Cesium 3D 主链（依赖已引入，非默认主界面）、deck.gl、PostGIS、Nginx **全站**容器化（同域 Gateway 已为默认入口：`launch.py start`；全应用镜像化仍属后续）、SSE
