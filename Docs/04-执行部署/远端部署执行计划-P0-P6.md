# 远端 Win10 台式机 · 再开发与部署执行计划（P0–P6）

> 编制：2026-09-18 ｜ 执行完成：2026-09-18 18:5x
> 机器：`DESKTOP-DB0A8EO`（Win10 Pro 19045，管理员 `qiujianxiu`）
> 代码：`E:\Project\CGDA\Comprehensive Geographic Data Analysis system`（分支 **dev** @ `84c92445`）
> 数据：`E:\Project\CGDA\Data`
> 运维目录：`E:\Project\CGDA\_ops`（**在仓库之外**，放脚本/日志/凭据，不入库）
> 公网入口：**https://geoflow.cgdas.dpdns.org/** ｜ **https://api.cgdas.dpdns.org/**
> 接入：`ssh win11-lab`（Cloudflare 隧道，密码认证）

---

## 0. 决策记录（2026-09-18 主人确认）

| # | 议题 | 决策 |
|---|---|---|
| 1 | 数据落位 | **D → A1**。先落最小子集跑通；**当前不传全量**（部分核心内容由远端门户自行下载） |
| 2 | 凭据 | **保持默认**：用户名不变，口令维持 `cgda-dev-admin`（开发阶段）⇒ 不轮换 |
| 3 | 分支 | **远端永远推 `dev`**；`main` 做**手动阶段性合并** |
| 4 | 公网入口 | 隧道 token 模式；`:5175` → `geoflow`，`:8000` → `api` |
| 5 | 传输通道 | A1 = 本机 → 远端 SMB（需临时放行远端入站 445） |

> ⚠️ **本轮所有凭据均不入库**：隧道 token 只存在于远端 `E:\Project\CGDA\_ops\run_tunnel.ps1`（仓库外）。

---

## 1. 执行结果总览

| 阶段 | 内容 | 状态 | 关键证据 |
|---|---|---|---|
| **P0** | 远端 Git 身份 | ✅ | `user.name=li589` / `user.email=3119317786@qq.com`（`--local`） |
| **P1** | 工作树清理 + 切 dev | ✅ | dirty **669 → 0**；`Test/` 609、`Docs/` 238、`.github/` 1 已复原；HEAD `84c92445` |
| **P2** | 数据落位（D） | ✅ | `E:\Project\CGDA\Data` 16 个分类目录 + `_runtime` 6 子目录 + 13 个冒烟夹具（14 KB） |
| **P3** | `.env` 重指向 | ✅ | `BACKEND_DATA_ROOT=E:\Project\CGDA\Data`；备份 `.env.bak.p3-20260918-182808` |
| **P4** | 基础设施镜像 | ✅ | `cgda-redis` / `cgda-minio` **healthy**、`cgda-open-meteo` Up、`cgda-minio-init` 退出 0 |
| **P5** | 全栈启动 + 公网暴露 | ✅ | `:8000`（13 个 python 进程：FastAPI + 9 Worker + Beat）、`:5175` 网关；**公网 200** |
| **P6** | 收尾与自启 | ✅ | 计划任务 `CGDA-Stack` / `CGDA-Tunnel`（ONLOGON，带幂等守卫）；仓库 dirty=0 |

### 公网验收（本机实测）

| URL | 结果 |
|---|---|
| `https://geoflow.cgdas.dpdns.org/` | **200** — 返回真实前端 `index.html`（SGFS 系统） |
| `https://geoflow.cgdas.dpdns.org/cesium/Widgets/widgets.css` | **200** |
| `https://api.cgdas.dpdns.org/health` | **200** — `{"status":"ok","service":"Comprehensive Geographic Data Analysis System (CGDA) Backend","environment":"development"}` |
| `https://api.cgdas.dpdns.org/docs` | **200** |

---

## 2. 执行中发现的关键问题与处置（重要，复现时必看）

### 2.1 Docker Hub 在本网络不可达 ⇒ 必须用镜像源

远端 `registry-1.docker.io:443` / `auth.docker.io:443` **均不通**（无任何代理）。

| 源 | 可达 | 用途 |
|---|---|---|
| `public.ecr.aws` | ✅ | `open-meteo` 官方镜像（直接拉，未改动） |
| `docker.m.daocloud.io` | ✅ | `redis`、`nginx` |
| `quay.io` | ✅ | `minio/minio`、`minio/mc`（**Docker Hub 上的 minio 已 403**） |
| `docker.1ms.run` / `docker.nju.edu.cn` | ✅ | 备用源 |

**处置**：先拉镜像→`docker tag` 回 compose 里的规范名，compose 便不再联网取镜像。

```
docker pull docker.m.daocloud.io/library/redis:7.4.10-alpine
docker tag  docker.m.daocloud.io/library/redis:7.4.10-alpine redis:7.4.10-alpine
# minio 走 quay.io；nginx 同 redis 方式；open-meteo 走 public.ecr.aws 原引用
```

**并已为将来铺路**：`C:\Users\qiujianxiu\.docker\daemon.json` 已加入 `registry-mirrors`
（备份 `daemon.json.bak.p4`）——**下次 Docker Desktop 重启后自动生效**，届时无需再手工 retag。

### 2.2 非交互 SSH 会话下 Docker 凭据助手必失败

报错：
```
error getting credentials - err: exit status 1, out: `A specified logon session does not exist.`
```
原因：SSH 是**网络登录**会话，无交互登录会话 ⇒ `docker-credential-desktop` 访问 Windows 凭据管理器失败。
且 Docker Desktop 的 CLI 在 config **未指定 `credsStore` 时会默认用 `desktop`**（所以 `--config` 指向干净配置也照样报错）。

**处置**：仓库外建匿名凭据助手 + 专用 `DOCKER_CONFIG`（两者必须同时生效）：

| 文件 | 作用 |
|---|---|
| `E:\Project\CGDA\_ops\docker-cred\docker-credential-cgdaanon.cmd` | `get` 回 `{"Username":"","Secret":""}` ⇒ 视为无凭据 → 匿名拉取 |
| `E:\Project\CGDA\_ops\docker-config\config.json` | `{"auths":{},"credsStore":"cgdaanon"}` |

> **踩坑**：助手 `get` 若**什么都不输出**（exit 0），docker 会报 `EOF`；必须回**合法 JSON**。

### 2.3 服务进程不能靠 SSH 会话存活 ⇒ 用计划任务

`launch.py` 用 `CREATE_NO_WINDOW` 拉起子进程，**SSH 会话一断就被回收**（实测：detached 的 `Start-Process`/`cmd` 全部秒退）。
**处置**：用 Windows 计划任务把启动动作"脱离会话"：

| 计划任务 | 触发 | 幂等守卫 |
|---|---|---|
| `CGDA-Stack` | ONLOGON | 若 `:5175` 已在监听则直接退出 |
| `CGDA-Tunnel` | ONLOGON | 若已有含该 tunnel id 的 `cloudflared.exe` 则直接退出 |

手工拉起：`schtasks /Run /TN CGDA-Stack` / `schtasks /Run /TN CGDA-Tunnel`

### 2.4 公网入口用的是**另一个**隧道

远端常驻的 `Cloudflared` 服务跑的是 tunnel **`b13d…`**；而 `cgdas.dpdns.org` 的 ingress 在 tunnel
**`e9c34312-ea83-4674-84c4-15b08298e4e9`** 上——它原本**没在运行**，所以 `geoflow`/`api` 都是 Cloudflare **1033**。
**处置**：以独立进程额外拉起该隧道（不动原有服务）。其 dashboard ingress 已确认存在：

```
geoflow.cgdas.dpdns.org  ->  http://localhost:5175
api.cgdas.dpdns.org      ->  http://localhost:8000
（无匹配 -> http_status:404）
```

---

## 3. 日常运维速查

| 目的 | 命令（远端） |
|---|---|
| 看全栈状态 | `Env\Python312\python.exe launch.py status` |
| 改完 `.env` 生效 | `… launch.py restart backend` |
| 改前端后刷新 | `… launch.py restart --rebuild-frontend` |
| 看日志 | `… launch.py logs [component] -n 200` |
| 隧道日志 | `E:\Project\CGDA\_ops\logs\tunnel.log` |
| 启动日志 | `E:\Project\CGDA\_ops\logs\p5_stack.log` |
| 重启整套 | `schtasks /Run /TN CGDA-Stack` |

> 从 SSH 驱动 Docker 时，**必须先设** `DOCKER_CONFIG=E:\Project\CGDA\_ops\docker-config`
> 且把 `E:\Project\CGDA\_ops\docker-cred` 放进 `PATH`（`_ops\start_stack.ps1` 已内置）。

---

## 4. 再开发闭环

```
远端编辑代码 (codegraph)  →  git commit  →  git push origin dev  →  本地 git pull 校验
```

```bash
# 远端只推 dev
git checkout dev && git add <路径> && git commit -m "type: ..."
git push origin dev

# 阶段性发布：main 手动合并（不冲突；要留合并点就加 --no-ff）
git checkout main && git pull --ff-only origin main
git merge dev --no-edit && git push origin main && git checkout dev
```

- **严禁 `git add -A` / `git commit -a`**（会把未复制子树记成删除）。
- 提交信息用 Conventional Commits（pre-commit 强制）。

---

## 5. 遗留事项

| # | 事项 | 说明 |
|---|---|---|
| 1 | **A1 全量数据** | 待命。需临时放行远端入站 445（限 `192.168.1.4`）后 robocopy；用完即撤 |
| 2 | 天气数据 | `launch.py sync` 未执行（GB 级），当前天气图层无数据 |
| 3 | Docker 镜像源 | `daemon.json` 已写入 mirrors，**需重启 Docker Desktop 生效** |
| 4 | 凭据轮换 | 开发阶段按决策**不轮换**；对外正式交付前必须轮换（含 `cgda-dev-admin`） |
| 5 | 会话期临时文件 | 本机 `Temp\cgda_ssh\` 含 SSH 口令与隧道 token 的脚本，收工后应删除 |

---

## 6. 相关文档

- `Docs/04-执行部署/部署与再开发指南.md` —— 启停/端口/分支权威手册。
- `Docs/04-执行部署/远端再开发与部署规划.md` —— 规划期调研与方案对比。
- `Docs/04-执行部署/外网访问与Cloudflare隧道.md` —— 本机侧隧道与凭据轮换。
